# エージェント前提開発基準(v0.4)

コーディングエージェント(Claude Code、Codex 等)に実装を委譲することを前提としたプロジェクトにおける、理解・PR・マージ・レビューの運用基準。特定のエージェント製品には依存しない。

## 前提認識:なぜこの基準が要るのか

AI以前の開発では、実装に費やす時間が、書いた本人と周囲の**文脈理解の時間を兼ねていた**。エージェントが実装時間を消した結果、理解コストの請求だけが残り、レビューとマージの席に集中して届くようになった。

レビューが重くなったのではなく、これまで実装の中に隠れていたコストが一箇所に集まって見えるようになった。したがって対策は「レビューを頑張る」ではなく、理解コストを**先払いする(実装前の合意)・減らす(隔離と濃淡)・計上する(見積りに含める)**の三つになる。

---

## 1. 三つの判断を分離する

「AIレビューか人間レビューか」を一つの問いとして扱わない。次の三つは別の問いであり、別の方法で決める。

| 判断 | 決め方 |
|---|---|
| ① 検査(レビュー)をAIに委譲できるか | **実績**で決める(§7 権限移譲の原則) |
| ② 人間がそのコードを将来理解する必要があるか | **理解負債**で決める(可逆性・障害対応・ドメイン重要性) |
| ③ 何を・どこまで理解するか | **3段階**で決める(§2) |

- 人間の理解は**レビューのためではなく、将来の保守・障害対応・意思決定のため**にある。検査をAIに委譲しても、②③の義務は消えない。
- **理解負債 = 人間が将来必要になる理解を、現在獲得していない状態。** 再生成可能な形に隔離された100万行は負債ではない。誰も意味を説明できないDBのデータ変換ロジック100行は巨大な負債である。負債はコード行数ではなく「障害・変更・意思決定のときに人間が持っていない理解」で測る。
- この概念は英語圏で **comprehension debt** として先行して論じられている([Jason Gorman, 2025](https://codemanship.wordpress.com/2025/09/30/comprehension-debt-the-ticking-time-bomb-of-llm-generated-code/) / [Addy Osmani, 2026](https://addyosmani.com/blog/comprehension-debt/))。関連概念に Storey の [Triple Debt Model](https://queue.acm.org/detail.cfm?id=3807966)(technical / cognitive / intent debt)がある。本基準の「理解負債」はこれらの整理・訳語として使う。

## 2. AI生成コードの理解基準 — 3段階

「全行を理解してからマージする」は運用に耐えず、「テストが通ればよい」は障害時に破綻する。そこで「コードを1行ずつ理解する」のをやめ、**契約**を理解の最小単位にする。

ここでの契約は、契約書の契約とほぼ同じ意味である。当事者はコードを**呼び出す側**と**呼び出される側**。呼ぶ側は正しい入力を渡す義務を負い、呼ばれる側は決められた出力を返す義務——失敗するときも決められた形で失敗する義務——を負う。この取り決めを検証可能な文面にしたものがテストであり(§4「テストは契約書」)、バグとはどちらかの義務違反である。中身がどう書かれているかではなく、何を取り決めているかを理解し、対象によって濃淡をつける。

| レベル | 対象 | 要求される理解 |
|---|---|---|
| **契約理解**(最低線) | すべてのAI生成コード | 何をするものか一文で言える。入出力と失敗時の挙動を知っている。契約がテストで固定されている |
| **構造理解** | ドメインロジック。今後変更するコード | データの流れと状態変更を口頭で説明できる。設計を自分で再現できる |
| **行レベル理解** | 不可逆な副作用を持つもの(スキーマ変更・データ移行・課金・外部送信・削除)、セキュリティ境界、並行処理 | 一行ずつ説明でき、エッジケースを列挙できる。AI以前と同じ基準 |

判定軸は**影響範囲 × 可逆性(やり直せるか) × 変更頻度**。「被害の大きさ」の正体は影響範囲×戻せなさである。

- コードは再生成できるが、**データは再生成できない**。間違ったマイグレーションが流れた後の状態は、コードを作り直しても戻らない。「消耗品(再生成可能)として扱えるか」の境界線は、状態=不可逆な副作用を持つかどうか。
- DBを慎重に扱うのはAI以前からの常識だが、AI時代はそこを通過する変更の流量が桁で増えたため、重要度が上がった。
- 契約理解の「テストで固定」は**自動テストが前提**。手動のUI確認で担保されるのは見た経路(正常系)だけであり、AI生成コードの典型的失敗(Happy Path Bias:正常系しか動かない)を素通しする。
- 業務ルールの中核(ドメイン重要性が高い箇所)は、被害・頻度にかかわらず**構造理解以上**とする。実装詳細の理解は下げてよいが、業務判定の理解は下げない。

## 3. 二つのゲート — PRとマージ

| ゲート | 基準 | 意味 |
|---|---|---|
| **PR提出** | 説明可能性 | 「なぜこの設計か」「このエッジケースは?」に答えられる。求められるのは記憶ではなく説明可能性。著者はエージェントではなく自分 |
| **マージ** | トレース可能性 | 本番障害時に**どの箱が壊れたか特定できる**。箱の並びと繋がり(地図)は常に全員が理解。箱の中身は §2 の濃淡に従う |

マージ基準を「全コードをトレースできる」と字義通りに運用すると全行精読に戻り、誰も守らない基準になる。トレース責任は地図レベル(§10)に置き、中身を読まない箱は §5 の隔離を義務とする。

## 4. テストは契約書。ただし条文の妥当性判断は人間の仕事

> ⚠ エージェントは実装に合わせたテストを書く。「テストが通っている」は「契約が正しい」を意味しない。実装とテストを同じセッションが書いた場合、それは検証ではなく同語反復である。

**循環の断ち方:独立テスト生成**

- 実装とテストを**別セッション**(可能なら別ベンダーのモデル)で生成する。テスト側セッションには仕様とインターフェースのみを渡し、**実装コードを見せない**。実装を見せた時点で循環が復活する。
- 両者を突き合わせて実行する。これは仕様を2回独立に解釈させる二重実装であり、**落ちたテストは「仕様が2通りに読めた証拠」=仕様の曖昧さ検出器**として扱う。どちらの解釈が意図かの裁定は仕様の持ち主(人間)が行い、裁定結果は仕様に書き戻す。
- コストに見合う範囲に絞る:構造理解以上、特に不可逆系(DB・課金)に適用する。契約理解レベルのコードには過剰装備。
- 学術的な先行研究がある:自然言語仕様からLLMで差分テストを生成して実バグを発見した [DiffSpec(arXiv:2410.04249)](https://arxiv.org/abs/2410.04249)、LLMでN版プログラミングを自動化した [Galápagos(arXiv:2408.09536)](https://arxiv.org/abs/2408.09536)。本節はその実務向けの簡易形にあたる。

**それでも残る人間の仕事**

- 両セッションが**同じ読み間違いに合意**した場合、テストは緑のまま通る。不一致は検出できるが、一致の正しさは保証されない。
- どちらも思いつかなかったエッジケースは静かに欠落する。レビュー時に自分で追加ケースを1〜2個考えて足す運用は残す(思いつけないなら理解不足のシグナル)。
- 前提として、AIの間違いは仕様・コンテキスト層に移動する。人間の検証対象は「コードが仕様通りか」から「**仕様とテストが意図通りか**」へ移動しており、この層は委譲できない。

## 5. 隔離ルール — ライブラリ扱いの条件

AI生成コードは「ライブラリを使う感覚」で扱ってよい。ただしライブラリは、多数の利用者・メンテナ・安定した契約という生態系が理解の省略を肩代わりしている。AI生成コードにはそれがないため、**ライブラリ扱いするコードは実際にライブラリの形にする**:

1. 安定したインターフェースの後ろに置き、他のコードと絡ませない
2. テストを契約書として固定する(§4 の条件付き)
3. 中身をパッチで直さない。壊れたら**切り離す・戻す・丸ごと再生成する**

ドメインの中核に編み込まれ境界が引けないコードはライブラリ扱いできない(構造理解以上が必須)。理解しない範囲を「理解しなくても安全な形」に置けているかは、認知の問題ではなく設計の問題である。

## 6. この基準の根拠 — 障害対応

エージェントを障害対応に使うこと自体は有効。問題は「聞くしかない」状態=人間側に判断のフォールバックがないこと。

1. デバッグの本質は仮説検証で、仮説の質はシステム理解に依存する。理解がないと何を聞けばよいか分からない
2. 障害中は時間がなく、もっともらしい誤答を検証せずに実行しやすい。間違った修正は障害を二重にする
3. 理解があれば**緩和と修正の分離**(まず再起動で凌ぎ根本対応は後)ができる。理解ゼロだと原因究明が終わるまで何もできない
4. 外部LLMに接続できない状況で、システム理解が全部外部サービス側にある状態は単一障害点
5. 障害のたびにエージェント頼みだと理解はさらに減り、依存が加速する

> **原則:エージェントは優秀な同僚にはなれるが、オンコール責任者にはなれない。** マージ基準(§3)はこの一点から逆算されている。

## 7. レビューの重心移動と権限移譲

実装が安価になった以上、レビューの重心は「できたコードの検査」から「作る前の方針合意」に移す。

- **設計メモ:** 着手時に10〜15分、「この方針・この構造で書かせる」という数行のメモをチームに流し異論を確認。コードレビューは「方針通りか+危険箇所の精読」に縮む
- **レビューガイド:** PRに、背景(チケット外の前提含む)・採った/捨てたアプローチ・重点箇所・流し読みでよい箇所を必ず添える(テンプレートは §9)。下書きはエージェント生成、本人が直す
- **AI一次レビュー:** 人間の前に、規約・既知の落とし穴・テストと実装の整合を機械検査。人間は設計判断と契約の妥当性(§4)に集中
- **小さいPR:** レビュー時間はdiffサイズに対して急激に伸びる。チケット分割でPRサイズを抑える
- **ドメインオーナー:** 領域ごとの最終責任者を決める。全員が全領域を薄く見る体制は「誰も直せないコードベース」に直結する

**権限移譲の原則:実績が出た方向に権限を移す**

「人間レビューは必要」にも「AIに全部任せてよい」にも、前提としては立たない。AI一次レビューを出発点とし、実績が示された種類の変更から、人間レビューの範囲を段階的に外していく。

計測するもの:レビュー時間・リードタイム/AIが見つけた問題数/人間が追加で見つけた問題数/両方見落として本番で発覚した問題数/false positive率/**AIレビューのみで通したPRの障害率**。

2026年時点の外部証拠(判断の初期値。自チームの実績で更新する):

- AIレビュー提案の採用率は人間提案より大きく低い(採用16.6% vs 人間56.5% / 別研究で却下56.3%。主因はfalse positive・意図との不一致)— [arXiv:2603.15911](https://arxiv.org/abs/2603.15911)、[arXiv:2607.03316](https://arxiv.org/abs/2607.03316)(いずれもプレプリント)
- AIエージェントのレビューで判断は速くなるが、品質向上にはつながらない — [arXiv:2607.13196](https://arxiv.org/abs/2607.13196)(プレプリント)
- 一方、現実にはAI生成PRの多くがそもそも人間レビューを受けていない — [EASE 2026(査読済み)](https://arxiv.org/abs/2605.02273)。**基準を決めなければ、なし崩しに人間レビューが消えていく**
- AIレビューツールの性能数字は、測り方で大きく変わる。あるツールはベンダー自社ベンチマークで[検出率82%](https://www.greptile.com/benchmarks)を主張するが、[第三者ベンチマーク](https://tenki.cloud/benchmarks/code-reviewer)では29〜44%であり、独立した3つのベンチマークで勝者が3回変わった。**外部の数字は初期値にしかならない——自チームの実績で決めるしかない**ことの根拠でもある
- 関連研究:リスクに応じて人間の監督形態(in-the-loop / over-the-loop / 自動+監視)を段階化する提案がある([Graduated Human Oversight, arXiv:2606.22484](https://arxiv.org/pdf/2606.22484))。本基準は監督形態ではなく**理解の深さ**を軸に段階化する点で異なる

## 8. 責任の所在

エージェントとAIベンダーの責任はゼロである(利用規約上、出力の責任は利用者にあり、法的にもAIは責任主体になれない)。責任の総量は減らずに人間側に残るため、割り当てを明示する。

| 責任 | 誰が | 内容 |
|---|---|---|
| **説明責任** | PR著者 | このコードが何をするか・なぜこの設計かを説明する。エージェントに書かせても著者は自分 |
| **一次対応責任** | マージした人 | 本番で壊れたとき最初に動き、どの箱が壊れたか特定する(マージゲート §3 の実体) |
| **領域の最終責任** | ドメインオーナー | その領域の設計判断と、行レベル理解対象の最終判断 |
| **対外責任** | 組織 | 顧客・法に対する責任は常に組織。個人が対外的に負うものではない |

- **レビュー承認は責任の共有であって移転ではない。** 著者の説明責任は承認後も消えない。
- **責任の明確化は個人を吊るすためではない。** 障害時に誰が動くかを迷わないためのものであり、事後の振り返りは個人ではなく仕組みを対象にする(さもないと誰もマージしなくなり、基準ごと形骸化する)。
- **AI利用の開示が責任割り当ての前提になる。** 先行例として [FedoraのAI-Assisted Contributions Policy(2025年10月)](https://communityblog.fedoraproject.org/council-policy-proposal-policy-on-ai-assisted-contributions/)は、AIが大部分を無変更で生成した貢献に開示を義務付け、コミットトレーラー `Assisted-by:` を推奨している。本基準ではPRテンプレート(§9)のレビュー指定がこの開示の役割を兼ねる。

## 9. PRテンプレート — レビューガイドの実装

フォーマットの目的は理解を「伝える」ことではなく、**レビュアーの限られた注意力を正しい場所に配分する**こと。AIの冗長さはプロンプトの工夫ではなく構造的な制約(文字数上限・チェックボックス・書く欄がなければ書けない)で抑える。

テンプレート本体は [templates/pr-template.md](templates/pr-template.md)。

運用ルール:

- **AIが下書きし、著者が直す。** 下書きを直せないなら、それは理解していない(PRゲート §3 の実技試験を兼ねる)。
- 長い説明は `<details>` に畳み、本文は上限内に収める。読むかどうかはレビュアーが決める。
- 形骸化対策として、**本人の判断がないと埋まらないフィールド**を2つ仕込んである:「一番怪しい箇所」(AIは自分の出力を怪しいと思わないので、genericな回答はすぐ分かる。選べない=理解不足のシグナル)と「採らなかった選択肢」(採った理由より捨てた理由の方が判断が見える。情報密度が最も高い欄)。
- 「レビュアーへの質問」は、レビューを漫然とした通読から答え探しに変えるための欄。

## 10. カバレッジゲート — 床として使い、目標にしない

テスト未作成のコードを機械的に弾くため、CIにカバレッジゲートを置く。ただしカバレッジは「実行された」しか測らず、「検証された」は測らない。エージェントは数字だけ満たすアサーション無しテストを無料で量産できるため、目標にした瞬間に指標が腐る(グッドハートの法則)。

- **片方向の信号として使う**: 低い=テスト不在は確実に分かる。高い=品質は何も保証しない
- **全体%ではなくdiffカバレッジ**(変更行ベース)でゲートする。レガシー部分に人質を取られない
- **閾値は理解レベルと連動**: 行レベル対象(不可逆系)は高く、UIグルーは低く。ディレクトリ単位で設定
- **ラチェット方式**: 理想値を掲げず、現状実測を床にして「下がったら落ちる」から始める(理想値ゲートは導入日に全滅して即無効化されるのが定番の失敗)
- **テストの質はミューテーションテストで担保**(Stryker / mutmut 等)。アサーション無しテストはここで確実に死ぬ。重いので行レベル対象に絞る。独立テスト生成(§4)と併用すると、テストが仕様側と変異側の両面から検証される
- 「実行された」と「検証された」の差は実測で大きい:行カバレッジ100%のテストスイートがミューテーションテストでは[80%しか殺せなかった実例](https://codeheir.com/blog/2022/03/05/mutation-tests-100-code-coverage-is-not-enough/)があり、巧妙なミュータントに対してはテストの平均検出率が71%から40%まで落ちるという[ベンチマーク(SWE-Mutation, arXiv:2605.22175)](https://arxiv.org/abs/2605.22175)もある

## 11. 理解の地図 — 抽象化レイヤーと訓練

人間がAIと同じ方法(コードを読む)で理解しようとすると、量で負ける。人間は抽象度の高い層から降りる。マージゲート(§3)の「地図」の実体を、次のレイヤー構造として維持する。

| レベル | 内容 | 人間の持ち方 |
|---|---|---|
| 0: システム地図 | 何がどこにつながっているか | 常に頭にある |
| 1: 契約 | 各コンポーネントの入出力・副作用・エラー時挙動 | いつでも参照できる |
| 2: 不変条件 | 絶対に壊してはいけないこと | いつでも参照できる |
| 3: 危険領域 | 不可逆系(DB・課金・権限)の位置と、失敗時に何が起きるか | いつでも参照できる |
| 4: 実装 | コードそのもの | 必要時のみ、AIに絞り込ませて降りる |

運用:

- 地図の生成・更新はAIに委譲する(「このシステムを人間が保守するために必要な知識だけ抽出して」)。ただし**所有者は人間**。障害対応や大きな変更のたびに、判明した仕様・依存・注意点をAIに地図へ書き戻させる(§4 の「裁定を仕様に書き戻す」と同じ循環)
- 大量のドキュメント生成は解決策ではない。人間が将来判断するために必要な情報**だけ**を維持する。100ページの自動生成ドキュメントは、読まれない時点で理解負債の別の形にすぎない
- 原則: **コード量が増えても、人間が理解すべき情報量が増えない構造にする**

**訓練:使わない理解は腐る**

かつて理解は実装の摩擦から生まれていた。実装もレビューもAIに移した後、地図を「読むだけ」の理解は紙の上の理解であり、障害の夜には使えない。地図には運動を付ける。

実証的な根拠がある。AI補助で新しいライブラリを学習した開発者は、手書きで学習した群よりテスト得点が17ポイント低く(50% vs 67%、p=0.01)、**低下が最も大きかったのはデバッグ問題**だった——しかも時間短縮効果は有意でなかった([AnthropicのRCT, arXiv:2601.20245](https://arxiv.org/abs/2601.20245)、査読前)。また人は自分が書いたコードでも、2ヶ月後には識別子をほとんど思い出せない([Fekete & Porkoláb, 2023](https://www.cs.ubbcluj.ro/~studia-i/journal/journal/article/view/89)、小規模実験)。書かない時代の理解は、意図的な運動なしには形成も維持もされない。

- **障害シミュレーション**: 「決済が落ちた。どの箱か3分で特定せよ」を定期的にやる。マージゲート(§3)の実技試験を兼ねる
- **裁定の義務**: 独立テスト生成(§4)の不一致は人間が裁く。これが契約理解の維持運動になる
- **地図への尋問**: AIの説明を読むだけで済ませず、「ここを変更すると何が壊れる?」と質問で掘る

## 12. 導入の順序(推奨)

1. **ベースライン計測を始める。** レビュー実時間・リードタイムを記録(集計はエージェントに委譲)。改善はまだしない。数字がなければ、この後の何をやっても効果が示せない
2. **基準の合意と運用開始。** 3段階・2ゲート・隔離ルールをチームで合意し、レビューガイド+設計メモ+AI一次レビューを開始
3. **計上ルールの決定。** 実測値に基づき、レビュー時間の見積り計上方式(ポイントに含める/容量の一定割合を先取り)とPR分割の目安を決める
4. **差分の確認と改訂。** ベースラインとの差分(レビュー時間・リードタイム・障害件数)を確認して改訂する。守られていない項目は厳格化ではなく削除を検討する

---

*本基準自体も改訂対象。守られない基準は無いより悪い——重すぎる項目は運用に合わせて削る。*
