# エージェント前提開発基準(v0.36)

コーディングエージェント(Claude Code、Codex 等)に実装を任せる前提のプロジェクトで、理解・PR・マージ・レビューをどう運用するかを決める基準。中心にある問いは**どこまで理解してからPRを出し、どこまで理解してからマージするか**である。特定のエージェント製品には依存しない。

**読み手はこの基準を採用するかを判断する人間である。** エージェントが日常的に読むのは [skills/](skills/) の手順書で、この文書は節アンカー(`#s4` 等)で必要な節だけ参照される。だから本文には規範だけを置き、出典・先行研究は末尾の[参考文献](#refs)に寄せてある——本文を丸ごとエージェントに読ませる場合に、根拠の記述が文脈を食わないようにするためである([§7](#s7)の手順書肥大化の測定項目はこの文書自身にも当てはまる)。

## この基準を3行で

- 「全部読んでからマージ」は量的に無理。「読まずにマージ」は障害の夜に破綻する。この基準はその間に線を引く道具である。
- 人間が理解しておくのは**レビューのためではなく、後で保守・障害対応・意思決定をする自分のため**である。
- だから対策は「レビューを頑張る」ではない。理解の手間を**先払いする・減らす・工数に計上する**の三つである。

## 用語(この文書だけの言い方)

| 言葉 | 意味 |
|---|---|
| **理解負債** | 将来必要になる理解を、今持っていない状態。行数ではなく「障害・変更・判断のときに人間が持っていない理解」で測る |
| **契約** | 呼び出しの約束を書き出したもの。「この入力を渡せば、この出力が返る。失敗するときはこう失敗する」の3行(実例は[§2](#s2)冒頭) |
| **不可逆系** | やり直しが効かないもの。DBスキーマ変更、データ移行、課金、外部送信、削除 |
| **地図** | システム全体の構造・契約・不変条件・危険箇所を、人間が判断に使える短さでまとめたもの。全体像は文書を見ずに言える状態まで持つ([§11](#s11)) |
| **理解検査** | 著者が、実装の差分・テスト・AI一次レビューを開く**前**に契約ごとの期待値を書き出し、実装と突き合わせること([§4](#s4)) |
| **独立照合** | 実装を経由していない解釈——**実装方針を書いていない人**が先に書き出した期待値、または実装非開示セッションが書いたテスト——を、実装と突き合わせること([§4](#s4)) |
| **独立テスト生成** | 独立照合の自動版。実装を見せていない別セッションに、仕様とインターフェースだけを渡してテストを書かせること([§4](#s4)) |
| **裁定** | 独立照合で仕様の読みが2通りに割れたとき、どちらが意図だったかを人間が決めること([§4](#s4))。レベル分類の「判定」とは別物 |
| **出力との突き合わせ** | 計測・集計・エクスポート系の変更で、実データを1本通して値の分布と代表的な集計を実際に出し、読み手が正しい結論に至るかを確かめること([§4](#s4)) |

---

## なぜこの基準が要るのか

AI以前は、実装している時間がそのまま**理解する時間を兼ねていた**。書いた本人は手を動かしながら分かるし、周りも小さな差分を順に見ていけば追いつけた。実装コストと理解コストは、同じ時間の中で一緒に払われていた。

エージェントが実装の時間をほぼ消した。消えたのは実装の分だけで、**理解の分の請求書は消えていない**。しかもそれが、レビューとマージの席に一度にまとまって届くようになった。

つまりレビューが重くなったのではない。**これまで実装作業の中に隠れて見えなかったコストが、一箇所に集まって見えるようになった**だけである。

だから打ち手は「レビューを頑張る」ではなく、**先払いする**([§7](#s7))・**減らす**([§2](#s2)・[§5](#s5))・**計上する**([§12](#s12))の三つになる。

---

<a id="s1"></a>
## 1. 三つの問いを混ぜない

「AIレビューでいいのか、人間レビューが必要か」を一つの問いとして扱ってはいけない。次の三つは別の問いで、決め方も別である。

| 問い | 決め方 |
|---|---|
| ① レビュー(検査)をAIに任せられるか | **実績**で決める([§7](#s7)) |
| ② 人間がこのコードを将来理解する必要があるか | **理解負債**で決める(判定軸は[§2](#s2)に一本化する) |
| ③ 何を・どこまで理解するか | **3段階**で決める([§2](#s2)) |

ここで一番混同されやすいのは①と②である。**検査をAIに任せても、②③の義務は消えない。** 人間の理解はレビューを通すためのものではなく、半年後に障害を切り分けたり仕様変更を判断したりするためのものだからである。

理解負債(用語表)が行数で測れないことを、例で言い直しておく。再生成できる形にきちんと隔離された100万行は負債ではない。誰も意味を説明できないDBのデータ変換ロジック100行は巨大な負債である。

「理解負債」は英語圏の comprehension debt の整理と訳語である([参考文献 R1](#refs))。

<a id="s2"></a>
## 2. どこまで理解するか — 3段階

「全行を理解してからマージ」は誰も守れない。「テストが通ればOK」は障害のときに崩れる。そこで**コードを1行ずつ理解するのをやめて、「契約」を理解の単位にする。**

ここでいう契約は、**呼び出しの約束を書き出したもの**である。抽象的な話ではない。ユーザー作成の関数なら、契約はこの3行に収まる。

```
createUser(email, plan)
  成功 → 作られたユーザーを返す(id は新規発行)
  失敗 → メール重複なら DuplicateError。このとき DB には何も書かない
  前提 → email は検証済みのものを渡すこと
```

呼ぶ側は「検証済みの email を渡す」義務を負い、呼ばれる側は「重複なら DuplicateError を返し、かつ何も書き込まない」義務を負う。**バグとは、どちらかがこの義務を破ることである。**

この取り決めを検証できる文面にしたものがテストである([§4](#s4)「テストは契約書」)。

**中身がどう書かれているかではなく、この3行を理解する。** そして対象によって濃淡をつける。

| レベル | 対象 | 何ができれば「理解した」か |
|---|---|---|
| **契約理解**(最低ライン) | すべてのAI生成コード | 何をするものか一文で言える。入出力と、失敗したときどうなるかを知っている。契約がテストで固定されている |
| **構造理解** | ドメインロジック。今後も人が変更するコード | データがどう流れ、どこで状態が変わるかを口で説明できる。同じ設計を自分で書き直せる |
| **行レベル理解** | 不可逆系、セキュリティ境界、並行処理 | 一行ずつ説明でき、エッジケースを列挙できる。AI以前と同じ基準 |

**レベルの決め方**

1. まず**影響範囲 × 可逆性(やり直せるか)**で下限が決まる。「被害が大きい」の正体は、これが広くて戻せないことである。
2. そこから**変更頻度**と**ドメイン重要性**は、上げる方向にだけ働く。人が頻繁に触るコードほど深い理解が要る。
3. 複数の軸で違うレベルに当たったら、**一番高いレベルを採る**。

なお[§5](#s5)の「丸ごと再生成」は変更ではなく交換なので、変更頻度には数えない。

**補足**

- **コードは再生成できるが、データは再生成できない。** 間違ったマイグレーションが流れた後の状態は、コードを作り直しても戻らない。「消耗品として扱っていいか」の境界線は、**状態を持つか=不可逆な副作用があるか**である。
- 契約理解の「テストで固定」は**自動テストが前提**。手動のUI確認で守れるのは自分が見た経路(正常系)だけで、AI生成コードの典型的な失敗——**正常系しか動かない(Happy Path Bias)**——をそのまま通してしまう。
- 契約レベルの「固定」の目的は**回帰検知**であり、正しさの保証ではない。正しさは独立照合([§4](#s4))。契約レベルのテストの最低品質(アサーションの有無など)は[§10](#s10)で機械検査する。
- **業務ルールの中核(ドメイン重要性が高い箇所)は、被害の大きさや変更頻度にかかわらず構造理解以上とする。** 実装の詳細は理解を下げてよいが、業務上の判定ロジックの理解は下げない。
- レビュー指定(行/構造/契約のどれか)は著者が下書きする。ただし**低いレベルに倒すほど著者が得をする構造になっている**——低くすれば独立テストも精読もミューテーションテストも免除されるからである。だから**行レベル対象に当たるかと、ドメイン中核かの判定はドメインオーナーの確認事項とし([§8](#s8))、著者の自己申告では確定させない**。

<a id="s3"></a>
## 3. 二つのゲート — PRとマージ

| ゲート | 基準 | 意味 |
|---|---|---|
| **PRを出すとき** | 説明できるか | 「なぜこの設計にしたか」「このエッジケースはどうなるか」に答えられる。求めているのは暗記ではなく説明。**著者はエージェントではなく自分である** |
| **マージするとき** | 追跡できるか | 本番で壊れたときに、**地図([§11](#s11))を見てどの箱が壊れたか特定できる状態**でマージする。**要件は「触る領域の地図が検収済みであること」**(検収の定義と最低ライン——Level 2・3 の通読・修正——は[§11](#s11)) |

マージ基準を「全コードを追跡できること」と字義通りに運用すると全行精読に戻り、誰も守らない基準になる。だから**追跡の責任は地図のレベルに置き([§11](#s11))、中身を読まない箱には[§5](#s5)の隔離を義務づける**。

<a id="s4"></a>
## 4. テストは契約書。ただし条文が妥当かの判断は人間の仕事

> ⚠ **エージェントは、実装に合わせたテストを書く。** 「テストが通っている」は「契約が正しい」を意味しない。実装とテストを同じセッションが書いたなら、それは検証ではなく自作自演である。

### 自作自演を断つ:実装を経由していない解釈と突き合わせる

規範はこの一行である。**不可逆系の契約は、実装を経由していない解釈と突き合わせてから通す。** これを**独立照合**と呼ぶ。

**レビューはこれを代替しない。** レビュアーは実装を読んでからテストを読むので、読み方が「このテストは実装と合っているか」に固定される。仕様の別の読み方は原理的に出てこない——判断のアンカーが実装側にあるからである。必要なのはテストの本数ではなく、**実装を経由していない2つ目の解釈**である。

照合に使う解釈は、次の2つのどちらで作ってもよい。

- **人間版(独立照合の既定)** — **実装と並列に**(実装の完了を待たない)、**実装前に書かれた仕様とインターフェースだけ**を読み、期待する入出力と失敗時の挙動を書き出す。実装がまだ存在しない時点でやれば、隔離は指示ではなく構成で保証される。**書き出してからテストと実装を読む。** 人間自身が2つ目の解釈者になる。契約1本あたり数分で終わる。
  - **書き手は、実装方針を書いていない人である**(レビュアー。不可逆系ならドメインオーナー [§8](#s8))。著者は設計メモで実装の設計図を書いた当事者なので、実装を経由していない解釈を作れない。**著者が書くと、実施記録の付いた自己照合になる。**
- **自動版(独立テスト生成)** — 実装コードを見せない別セッションに、仕様とインターフェースだけを渡してテストを書かせる。可能なら別ベンダーのモデルを使う。プロンプトに「実装を読むな」と書いても防壁にはならないので、**ファイル読み取りツールを持たない実行形態(ツール制限)で起動する**(手順は [skills/crosscheck](skills/crosscheck/SKILL.md))。

**自動版に上げるのは、人間版で足りないときだけである**(条件の一覧は [skills/crosscheck](skills/crosscheck/SKILL.md) 手順0)。**第二解釈者を確保できない場合(一人運用)も「足りないとき」に含む**——不可逆系の独立照合は省略できないので、自動版(可能なら別ベンダーのモデル)で満たし、記録に「第二解釈者不在のため自動版」と残す。

**どちらの版でも守ること:**

- **解釈のもとにする仕様は「実装前に書かれたもの」に限る**——チケット、承認済み設計メモの要求部分(切り出しは [skills/crosscheck](skills/crosscheck/SKILL.md))。**契約を変える変更では、この「実装前に書かれた契約の記述」が存在しないことがある。** そのときは**設計メモに契約欄を持たせ、そこに書いて承認する**(手順は [skills/design-memo](skills/design-memo/SKILL.md))。
  - **地図([§11](#s11))に先に書くのではない。** 地図は**現実の記述**であり、月イチの矛盾検査はコードと突き合わせる。実装前の意図を地図に置くと、実装までの間、地図が現実と乖離する。地図が更新されるのは実装後、当該PRの中である。
  - 設計メモの契約(実装前の**意図**)と、PRで更新された地図の契約(実装後の**現実**)に差があれば、**それも裁定の対象**である。実装が逸れたのか、契約の書き方が曖昧だったのかを人間が裁く。
- 食い違いは修正対象ではなく**裁定**対象である。**落ちたテスト・合わなかった期待値は「仕様が2通りに読めた証拠」=仕様の曖昧さ検出器**として扱う。どちらの読みが意図だったかを裁くのは仕様の持ち主(人間)であり、**裁定の結果は仕様に書き戻す。**
- **書き戻す先は仕様だけではない。裁定前に書かれたテストにも書き戻す。** 実装と同じセッションが書いたテストは、裁定前の読みを固定している。仕様だけ直してテストを放置すると、**間違った読みが「守るべき回帰」として保護される**。契約を変えたなら地図([§11](#s11))にも書き戻す。
- 裁定する人の立場に注意する。「仕様の持ち主」が著者本人である場合、自分の解釈を自分で正解にできてしまう。**不可逆系の裁定にはドメインオーナー([§8](#s8))を加える。**

### 標準の検査:著者の理解検査

独立照合が要らない変更にも、既定で回す軽い検査を置く。**著者が、実装(エージェントの出力)の差分・テスト・AI一次レビューを開く前に、契約ごとに「その契約を自分の言葉で言い直したもの」([§2](#s2)の契約3行:成功したら・失敗したら・前提)を書き出し、実装と突き合わせる。** これを**理解検査**と呼ぶ。

検査しているのは仕様の曖昧さではなく、**著者の理解**である。著者は独立した第二解釈にはならない(上記)ので独立照合を名乗らない。それでも意味が残る理由は2つある。

- **理解の担保。** エージェント前提開発の最大のリスクは「著者が成果物を理解しないまま出す」ことである。期待値を自分の言葉で書けることが、[§3](#s3)のPRゲート「説明できるか」の実物証拠になる。**失格条件は明確である:書けない欄があること。** 埋められない欄は理解不足か仕様の曖昧さのシグナルで、仕様・設計メモに戻る(**器そのものが合っていない場合を除く**。判別は下記「空欄が器の不一致か、理解不足か」)。エージェントに埋めさせた時点で検査は消える。
- **部分的な検出力。** コードを書いたのは著者ではなくエージェントである。設計メモに書いた読みは共有されて盲点になるが、メモに書かなかった細部——エッジケース・失敗時の挙動・境界値——では、著者の読みとエージェントの読みは普通に食い違う。

**守ること:**

- **書いた期待値は実装エージェントの入力に入れない。** 設計メモに書いてエージェントに読ませると、エージェントが期待値に合わせて実装し、一致が自動生成されて検査がトートロジーになる。照合メモの置き場所と委譲の順序は [skills/flow](skills/flow/SKILL.md)「セッションの分離」。
- **成果指標は一致件数ではない。** 著者 vs エージェントでは一致して当然である。成果は、書けなかった欄の数(理解の穴)・「仕様に記述なし」の数(仕様の穴)・食い違いの数(実装の逸脱)。
- 突き合わせの結果は**種別で行き先を分ける**(様式は [skills/pr-guide](skills/pr-guide/SKILL.md) のテンプレート)。差には2種類ある——**A: 仕様が2通りに読めた**(裁定の対象。コードを見る価値がある)と、**B: 著者が単に知らなかった**(実装は設計どおりで、裁定するものが無い)。
- **種別AはPRの「確認してほしい項目」欄へ。** 裁定が要るので、判断の経緯が残る価値がある(転記の様式は [skills/pr-guide](skills/pr-guide/SKILL.md))。
- **種別Bの既定の行き先は、PR本文ではなくコード・コメント・地図([§11](#s11) Level 2〜3)への反映である。** PRに現れるのは反映の結果(コメントや地図の差分)だけ。Bは**著者の内面の記録**であって、レビューの判断材料ではない。**種別Bの価値は、PRに並べることではなく、反映先を探させることにある。**
- 実施の記録(段・版・**器**・A/B別の件数・一致の件数・**書けなかった欄の件数**)は**PRのテスト欄に1行で残す**。Aが0件なら「確認してほしい項目」の節ごと落とす——テスト欄の記録が実施の証明になる。書けなかった欄は失格条件の筆頭なので、節を落としてもテスト欄で必ず残る。
- **器は独立照合と共用せず、[§2](#s2)の契約3行にする**(様式は [skills/crosscheck](skills/crosscheck/SKILL.md))。**契約名は仕様の言葉で書く。不変条件は「その状況が起きたらどうなるか」の問いに変換し、Yes/Noで閉じない。**
- **成否の軸が無い変更(参照するデータの出どころだけを変える等)は1問1答を器にしてよい。** 判定は「**失敗したら、この答えは変わるか**」(実測: 契約3行では32欄中31欄が空のまま止まり、1問1答にして初めて書けた)。
- **書けない欄は、まず器の不一致を疑う**(全契約が同じ理由で空くなら器の問題。判別は [skills/crosscheck](skills/crosscheck/SKILL.md))。**採った器はテスト欄に1語で残す**(契約3行/1問1答)。
- **選択式は単独では理解検査を名乗れない。** 使う条件と表記は [skills/crosscheck](skills/crosscheck/SKILL.md)。
- 記録には理解検査・独立照合のどちらを実施したかを明記する(ラベルが実態より強く読まれるのを防ぐ)。

### 出力との突き合わせ:記録した値を読む人間が正しい結論に至るか

理解検査と独立照合はどちらも**期待値と実装**を突き合わせる。実装が設計どおりなら、何度やっても差は出ない。ところが**計測・集計・エクスポートを目的とする変更**では、実装が設計どおりでも成果物として失敗していることがある——記録された値が正しくても、その値を読んだ人間が誤った結論を引くなら目的を達していない。

実測(一人導入): 「稀な異常」としてコードに文書化された列挙値が、実データでは**全体の数割を占める通常経路**だった。その列で成功率を出すと、分母の取り方で数十ポイント変わる——値は正しく、嘘は書かれていない。**壊れていたのは、値を読んだ人間が引く結論である。** この形の欠陥は、理解検査・独立照合・テスト・地図のどれでも原理的に出ない(すべて実装か契約を検査していて、実装は設計どおりだった)。見つかったのは実データを出力して読んだからで、しかも見えたのは値の正しさではなく**分布**だった。

対象は「記録された値を人間が読んで判断する成果物」である。メトリクスの列、エクスポートの列、ダッシュボードの指標、監視アラートの条件。

**規範:これらの変更は、実データを1本通し、値の分布と、そこから引かれる代表的な集計を実際に出してから通す**(検証環境のデータでよい)。

- 見るのは値の正しさではなく**分布**である。
- 代表的な集計を1つ実際に計算する。成功率・失敗率・平均を、**分母を明示して**出す。分母の取り方で答えが変わるなら、その取り方を列のコメントに書く。
- **通すのは計測の利用が始まる前である。** 誤った分母で作られた集計やアラートは、後から「守るべき回帰」として保護される。
- **実データに現れなかった経路は未検証のまま残る。** 出た分布だけで「全経路を確認した」と書かない。現れなかった値を列挙して残す(理解検査で確認したつもりの設計が、実データでは一度も観測されていないことがある)。
- 食い違いが出たときの行動は種別Bと同じ——挙動の修正ではなく、命名・コメント・地図([§11](#s11))の修正である。値の意味自体を変えるかは、分布を見た後の判断にする。
- 記録はPRのテスト欄に(様式は [skills/pr-guide](skills/pr-guide/SKILL.md))。

書き手は著者でよい——独立照合と違い、実データの分布は解釈の余地が小さく、著者が見ても自分の読みを正解にはできない。実施コストはエクスポート1回分である。

**適用範囲は梯子である:**

| 段 | 対象 | 検査 | 書き手 |
|---|---|---|---|
| 標準 | 構造理解以上の全変更(実施時点では設計メモの提案レベルで判定する。確定はレビュー時 [§8](#s8)) | **理解検査** | 著者 |
| 追加(必須) | 計測・集計・エクスポート系(値を人間が読んで判断する成果物) | **出力との突き合わせ** | 著者 |
| 昇格(必須) | 不可逆系 | **独立照合** | 実装方針を書いていない人 |
| 任意 | 契約理解レベル | (テストの回帰固定で足りる) | — |

不可逆系以外の構造理解対象に独立照合を足すのは引き続き推奨(第二解釈者がいれば)。

自動版は先行研究の実務向け簡易版にあたる([参考文献 R2](#refs))。

### それでも人間に残る仕事

- **2つの解釈が同じ読み間違いで一致した場合、食い違いは出ない。** 食い違いは検出できるが、一致していることの正しさは保証されない。人間版でも自動版でも同じである。同じモデル・同じベンダーで揃えるとこれが起きやすい。
- **自動版は、曖昧な箇所を「幅を持たせたアサーション」に吸収してしまうことがある。** 検出力がゼロのまま「全件緑」という結果が出る。**緑の件数は独立照合の成果ではない**(成果指標は理解検査と同じ。対策は [skills/crosscheck](skills/crosscheck/SKILL.md) 手順B・守ること)。
- **人間版は、やったことを機械検査できない。** 期待値を先に書いたのか、テストを見てから書いたのかは自己申告でしか分からない。自動版はツール使用ログで隔離を確認できる(それでも隔離が破れたかは出力からは分からない)。**この差は、この規範を守らせる力が仕組みではなく規約にあることを意味する。**
- どちらも思いつかなかったエッジケースは、静かに抜け落ちる。レビューのときに自分でケースを1〜2個考えて足す運用は残す。
- 前提として、AIの間違いは**コードの層から仕様・コンテキストの層に移動している**。人間が検証する対象も「コードが仕様通りか」から「**仕様とテストが意図通りか**」に移った。この層は委譲できない。

<a id="s5"></a>
## 5. 隔離ルール — 「ライブラリ扱い」していい条件

AI生成コードを「ライブラリを使う感覚」で扱ってよい。ただし思い出してほしいのは、**ライブラリの中身を読まずに済むのは、多数の利用者・メンテナ・安定した契約という生態系が、理解の省略を肩代わりしてくれているから**である。AI生成コードにはそれがない。

だから、**ライブラリ扱いするコードは、実際にライブラリの形にする**:

1. 安定したインターフェースの後ろに置き、他のコードと絡ませない
2. テストを契約書として固定する([§4](#s4)の条件付き)
3. 中身をパッチで直さない。壊れたら**切り離す・戻す・丸ごと再生成する(作り直す)**

ドメインの中核に編み込まれて境界が引けないコードは、ライブラリ扱いできない(構造理解以上が必須)。**理解しない範囲を「理解しなくても安全な形」に置けているかは、頭の良さの問題ではなく設計の問題である。**

- **再生成は交換であって修正ではない。** 契約テストで固定していない挙動——出力の順序、丸め、フォーマット、エラーメッセージ——に、下流が依存していることがある(Hyrumの法則)。だから再生成したら、契約テストが通ることに加えて**旧実装との出力差分を確認する**。特に外に出る出力(ファイル・API応答・イベント)は疑う。

<a id="s6"></a>
## 6. この基準の根拠 — 障害対応で何が起きるか

エージェントを障害対応に使うのは有効である。問題は**「聞くしかない」状態**、つまり人間側に判断の逃げ道がないことである。

1. デバッグの本質は仮説検証で、仮説の質はシステム理解に依存する。理解がないと**何を聞けばいいか分からない**。
2. 障害中は時間がない。もっともらしい誤答を検証せずに実行しやすい。**間違った修正は障害を二重にする。**
3. 理解があれば**「まず止血、原因対応は後」の分離**ができる(とりあえず再起動で凌ぐ、など)。理解ゼロだと原因が分かるまで何もできない。
4. 外部LLMに繋がらない状況で、システム理解が全部外部サービス側にあるのは単一障害点である。
5. 障害のたびにエージェント頼みだと理解はさらに減り、依存が加速する。

> **原則:エージェントは優秀な同僚にはなれるが、オンコール責任者にはなれない。** マージ基準([§3](#s3))はこの一点から逆算されている。

<a id="s7"></a>
## 7. レビューの重心移動と権限移譲

実装が安くなったのだから、レビューの重心を「できたコードの検査」から**「作る前の方針合意」**に移す。

- **設計メモ** — 着手時に10〜15分で、「この方針・この構造で書かせる」という数行のメモをチームに流し、異論を確認する。レビュアーの注意が要るのは**採らない選択肢・懸念点・未決定事項**の3欄([skills/design-memo](skills/design-memo/SKILL.md)。整理は [pospome氏の記事](https://www.pospome.work/entry/2026/08/24/223309) に倣った)。メモには**返事の期限を明記する**(例:「本日中に異論なければこの方針で進めます」)。返事がなかったときの扱い(同意とみなすか)は、チームで先に合意しておく。**期限のないメモは合意形成ではなく着手通知に落ちる。** 不可逆系を含む場合は無返事のまま着手を認めず、明示的な承認を待つ。これでコードレビューは「方針通りか+危険箇所の精読」に縮む。
- **レビューガイド** — PRに、背景(チケットに書いていない前提も)・採ったアプローチと捨てたアプローチ・重点的に見てほしい箇所・流し読みでいい箇所を必ず添える(テンプレートは [skills/pr-guide](skills/pr-guide/SKILL.md))。下書きはエージェントに書かせ、本人が直す。
- **AI一次レビュー** — 人間が見る前に、規約・既知の落とし穴・テストと実装の整合を検査する。**AIの所見は人間の判断の代わりではなく候補として扱う**(出力の分離と別セッション実行は [skills/first-review](skills/first-review/SKILL.md))。人間は設計判断と契約の妥当性([§4](#s4))に集中する。
- **小さいPR** — レビュー時間はdiffサイズに対して急激に伸びる。チケットの分割でPRサイズを抑える。
- **ドメインオーナー** — 領域ごとの最終責任者を決める。全員が全領域を薄く見る体制は、「誰も直せないコードベース」に直結する。

これらは [skills/](skills/) にエージェント用の手順書(スキル)として実装してある。**下書きと機械検査だけを自動化し、判断は空欄として人間に残す**設計になっている。この空欄の一覧が、このフローにおけるHuman-in-the-Loopの定義そのものである。

**「人間が決める」項目でも、選択肢が実質1つしかない状況では、決定ではなく検査として運用してよい。** 一人運用の領域決定のように、どう決めても検収先・境界の持ち主が変わらない場合、人に問うのは空の工程になる(実測: 著者の反応は「決めるのはそちらの仕事ではないか」で、正当だった)。この場合は**機械が決めて根拠を記録し(「機械が決めた」と明記)、人間はその選択を見て違和感があれば止める**。決定としては空でも、検査としては生きる。選択肢が複数に戻ったら(人が増えて検収先が分かれたら)、決定に戻す。

### 権限移譲の原則:実績が出た方向に権限を移す

「人間レビューは必要」も「AIに全部任せてよい」も、**前提としては立たない**。AI一次レビューを出発点にして、実績が示された種類の変更から、人間レビューの範囲を段階的に外していく。

**測るもの**

- レビュー時間・リードタイム
- AIが見つけた問題の数
- 人間が追加で見つけた問題の数
- 両方が見落として本番で発覚した問題の数
- false positive率
- **AIレビューだけで通したPRの障害率**
- **AIの警告(不可逆系の誤分類など)を人間が却下したPRの障害率**
- **行/構造/契約の分類比率の推移**(契約側へじりじり流れていないかの監視)
- **障害対応時間・切り分け成功率**([§6](#s6)が守ろうとしている対応能力の直接測定。障害シミュレーション[§11](#s11)の結果を含む)
- **手順書(スキル・カスタム指示・地図)の分量と、エージェントの応答待ち時間・トークン消費の推移**(手順書自体の肥大化の監視。品質のために足した指示は、毎回読み込まれる文脈として積み上がり、いずれ手順書そのものがボトルネックになる——実運用の先行報告では、コンテキスト増・トークン増・応答遅延・「今の作業に関係ない指示も毎回読む」の4症状として現れ、役割ごとの分割で解いている(木村健一郎「[AI駆動開発をチームに根付かせる](https://speakerdeck.com/kenichirokimura/embedding-ai-driven-development-into-your-team)」2026-08-27。1チームの1年間の実運用報告であり、定量評価ではない)。[§12](#s12) 手順3の計上対象)

**権限を移す判断には、レビュー時間やリードタイムだけでなく、障害率と切り分け成功率を必ず含める。** 速度系の指標は、人間が理解を放棄すればするほど改善する。だから単独では権限移譲の根拠にならない。[§10](#s10)で警告するグッドハートの法則は、この測定リスト自身にも当てはまる。

**2026年時点の外部証拠**(判断の初期値。自チームの実績で上書きしていく)

- AIレビュー提案の採用率は人間の提案よりかなり低い(採用16.6% 対 人間56.5%/別研究では却下56.3%)。主な原因はfalse positiveと意図とのズレ — [arXiv:2603.15911](https://arxiv.org/abs/2603.15911)、[arXiv:2607.03316](https://arxiv.org/abs/2607.03316)(いずれもプレプリント)
- AIエージェントによるレビューで判断は速くなるが、品質向上には繋がらない — [arXiv:2607.13196](https://arxiv.org/abs/2607.13196)(プレプリント)
- 一方で現実には、AI生成PRの多くがそもそも人間レビューを受けていない — [EASE 2026(査読済み)](https://arxiv.org/abs/2605.02273)。**基準を決めなければ、なし崩しに人間レビューが消えていく。**
- AIレビューツールの性能数字は、測り方で大きく変わる。あるツールはベンダー自社ベンチマークで[検出率82%](https://www.greptile.com/benchmarks)を主張するが、[競合ベンダーのベンチマーク](https://tenki.cloud/benchmarks/code-reviewer)では同じツールが36.1%になり、そこでは測った側の自社ツールが1位になる。**各ベンダーが自社ベンチマークで勝つ**この現象は[第三者の整理](https://deepsource.com/blog/ai-code-review-benchmarks)がある。
- 関連研究として、リスクに応じて人間の監督の形を段階化する提案がある(in-the-loop / over-the-loop / 自動+監視の3段階。[Graduated Human Oversight, arXiv:2606.22484](https://arxiv.org/pdf/2606.22484))。この基準は監督の形ではなく**理解の深さ**を軸に段階化する点が違う。

<a id="s8"></a>
## 8. 責任の所在

**エージェントとAIベンダーの責任はゼロである。** 利用規約上、出力の責任は利用者にあり、法的にもAIは責任主体になれない。つまり責任の総量は減らないまま人間側に残る。だから割り当てを明示する。

| 責任 | 誰が | 内容 |
|---|---|---|
| **説明責任** | PR著者 | このコードが何をするか・なぜこの設計かを説明する。エージェントに書かせても、著者は自分である |
| **一次対応責任** | オンコール担当(ロール) | 本番で壊れたとき最初に動き、地図([§11](#s11))を頼りにどの箱が壊れたか特定する。**交代制のロールであり、マージした個人が変更ごとに永久に負うものではない** |
| **マージ確認責任** | マージした人 | マージ時点で、レビュー指定・PRガイド・地図が[§3](#s3)の基準を満たしていることの確認。**あわせてPRテンプレートの欄同士の整合(不可逆系→独立照合、計測系→出力との突き合わせ)を確認する**(手順は [skills/flow](skills/flow/SKILL.md))。これがマージゲート([§3](#s3))の実体である |
| **地図の維持責任** | ドメインオーナー(自領域) | 自領域の地図([§11](#s11) Level 0〜3)の初版検収と、記述が現実と合っているかの最終判断。**領域が2つ以上ある場合、全体の Level 0 に載る「境界をまたぐ契約」は、呼ばれる側の領域のドメインオーナーが持つ**([§2](#s2))。領域そのものの増減は、それを持ち込んだ変更のPRで更新する。月イチの矛盾検査そのものはオンコール輪番かCIジョブに置いてよいが、**齟齬の判定は委譲できない**(コードが正しく進化したのか、地図が正しくてコードが逸脱したのかの判断) |
| **領域の最終責任** | ドメインオーナー | その領域の設計判断、および「行レベル理解の対象か」「ドメイン中核か」の最終判断(著者の自己申告を確定させる側) |
| **対外責任** | 組織 | 顧客・法律に対する責任は常に組織が負う。個人が対外的に負うものではない |

- **レビュー承認は責任の共有であって移転ではない。** 承認をもらっても著者の説明責任は消えない。
- **新しい領域のドメインオーナーは、その領域を持ち込んだPRの著者が候補を提案し、チーム(一人運用なら著者自身)が確定する。** 領域が生まれる場所は初版生成のPR([§12](#s12) 手順2)なので、そこで決めないと `owner` の無い地図が生まれる。
- **責任を明確にするのは、個人を吊るすためではない。** 障害のときに誰が動くかで迷わないためである。事後の振り返りは個人ではなく仕組みを対象にする。そうしないと誰もマージしなくなり、基準ごと形骸化する。
- **AI利用の開示は、貢献の形で置き場所を分ける。** 先行例の [FedoraのAI-Assisted Contributions Policy](https://communityblog.fedoraproject.org/council-policy-proposal-policy-on-ai-assisted-contributions/)は、AIが大部分を無変更で生成した貢献に開示を義務づけ、コミットトレーラー `Assisted-by:` を推奨する。**外部貢献を受けるリポジトリ**では、CONTRIBUTINGとコミットトレーラーで開示を求める。**チーム内部の開発では、PRごとの開示欄を置かず、リポジトリ単位で一度宣言する**(READMEかCONTRIBUTINGに「実装は原則エージェントに委譲し、著者が検証する」)——毎回同じ値の欄は情報を持たず形骸化の供給源になる([§12](#s12))。**開示はレビューの深さを変える材料ではない**——レビュー指定(理解の深さ)とAI生成の範囲は別の情報で、前者は後者の代わりにならない。
- **実装の委譲を「原則」にする理由は、開示の簡略化だけではない——手順書の欠陥を露呈させるためである。** エージェントの実装がうまくいかないとき、著者が「自分で書いた方が早い」と手で書き換えると、その場では片付くが、欠陥は手順書(スキル・カスタム指示・地図・チケットの切り方)に残ったまま次の人に同じ形で出る。だから**手で書く前に、疑う順番を決めておく**:①指示(スキル・カスタム指示)が悪い ②渡した文脈(設計メモ・地図・チケット本文)が足りない ③採った設計に問題がある ④タスクの切り方(チケット・PRサイズ)が悪い ⑤エージェントの役割を分割すべき。どれに当たったかを記録し、直すのは手順書の側である(記録の置き場は [skills/flow](skills/flow/SKILL.md))。**手で書くこと自体は禁止しない**——行レベル理解の対象([§2](#s2))で著者が自分で書くのは正当で、書く/書かないと理解の深さは独立の軸である。禁止するのは、記録の無い手書きで欠陥を隠すことだけ(木村健一郎「[AI駆動開発をチームに根付かせる](https://speakerdeck.com/kenichirokimura/embedding-ai-driven-development-into-your-team)」2026-08-27。1チームの1年間の実運用報告であり、定量評価ではない)。

<a id="s9"></a>
## 9. PRテンプレート — レビューガイドの実装

フォーマットの目的は、理解を「伝える」ことではない。**レビュアーの限られた注意力を、正しい場所に配分すること**である。AIの冗長さはプロンプトの工夫では抑えられない。文字数上限・チェックボックス・そもそも書く欄がないという**構造**で抑える。

テンプレート本体は [skills/pr-guide/assets/pr-template.md](skills/pr-guide/assets/pr-template.md)。

**運用ルール**

- **AIが下書きし、著者が直す。** 下書きを直せないなら、それは理解していないということ(PRゲート[§3](#s3)の実技試験を兼ねる)。
- 長い説明は `<details>` に畳み、本文は上限内に収める。読むかどうかはレビュアーが決める。
- 形骸化対策として、**本人の判断を要求する欄**を残してある(代表は「採らなかった選択肢」の選別。ほかに裁定・レビュアーへの質問)。
  - **「採らなかった選択肢」** — 採った理由より捨てた理由の方が判断が見える。情報密度が最も高い欄。**著者本人の発言からの逐語転記は可**(出所明記)。**転記は機械の仕事で、著者に可否を聞かない。選別は著者がする**(選別が判断そのもの)。**AIによる生成・整形は不可**(様式は [skills/pr-guide](skills/pr-guide/SKILL.md))
- ただし転記の出所と逐語性は**ツールでは守れない**(理由と限界は [skills/README](skills/README.md) の「空欄がHuman-in-the-Loopの定義」)。**実効性はテンプレートではなく口頭確認に置く**:レビューや振り返りで著者に「なぜその案を捨てたのか」を聞く。生成された欄は一往復で剥がれ、それ自体が理解不足のシグナルとして機能する。
- **PR説明の欄はレビュー時点でしか効かない。** PR description は検索されない——到達経路は `git blame` → コミット → PR で、それは「この行がなぜこうなったか」を追うときにしか通らない。**将来の参照に残したい事実(怪しさが実証された箇所・判明した依存・危険領域)は、地図([§11](#s11) Level 3)かコードコメントに書き戻す。**「PRに書けば将来の資産になる」という説明は誤りである。
- **「レビュアーへの質問」** 欄は、レビューを漫然とした通読から「答え探し」に変えるためのもの。**設計メモの先払い([§7](#s7))が機能しているほど、この欄は空になる**——方針レベルの未決はPRより前に潰れているはずで、残るのは設計メモの粒度に載らない実装の細部(命名を既存に揃えるか、例外を握るか投げるか)だけである。**無ければ空でよい。** 空が続くなら、偽のHITLポイントかの判定([skills/README](skills/README.md))に回して削除を検討する。

<a id="s10"></a>
## 10. カバレッジゲート — 床として使い、目標にしない

テストが書かれていないコードを機械的に弾くため、CIにカバレッジゲートを置く。ただし**カバレッジは「実行された」しか測れず、「検証された」は測れない**。エージェントは数字だけ満たすアサーション無しテストを無料で量産できるので、**目標にした瞬間に指標が腐る**(グッドハートの法則)。

- **片方向の信号として使う** — 低い=テストが無い。高い=何も保証しない。
- **全体%ではなくdiffカバレッジ**(変更行ベース)でゲートする。レガシー部分に人質を取られない。
- **閾値は理解レベルと連動させる** — 行レベル対象(不可逆系)は高く、UIグルーコードは低く。ディレクトリ単位で設定する。
- **ラチェット方式で始める** — 理想値を掲げず、現状の実測値を床にして「下がったら落ちる」から始める。理想値ゲートは導入日に全滅して即無効化されるのが定番の失敗である。
- **テストの質はミューテーションテストで担保する**(Stryker / mutmut 等)。アサーション無しテストはここで確実に死ぬ。重いので行レベル対象に絞る。独立照合([§4](#s4))と併用すると、テストが仕様側と変異側の両面から検証される。
- **ミューテーションを回さない範囲(契約・構造レベル)でも、アサーションがあるかどうかだけは全レベルで機械検査する**(lintルールまたはAI一次レビュー[§7](#s7))。ラチェットが下がったとき最も安い回復手段は「実行するだけのテスト」の量産であり、この検査がないとカバレッジという計器が恒久的に嘘をつき始める。
- 巧妙なミュータントに対しては**LLM生成テストスイート**の平均検出率が71%から40%まで落ちる([SWE-Mutation, arXiv:2605.22175](https://arxiv.org/abs/2605.22175))。

<a id="s11"></a>
## 11. 理解の地図と訓練 — 上の層から降りる

人間がAIと同じやり方(コードを読む)で理解しようとすると、量で負ける。**人間は抽象度の高い層から降りる。** マージゲート([§3](#s3))でいう「地図」の実体は、次のレイヤー構造である。

| レベル | 内容 | 人間の持ち方 |
|---|---|---|
| 0: システム地図 | 何がどこに繋がっているか | 常に頭にある |
| 1: 契約 | 各コンポーネントの入出力・副作用・エラー時の挙動 | いつでも参照できる |
| 2: 不変条件 | 絶対に壊してはいけないこと | いつでも参照できる |
| 3: 危険領域 | 不可逆系(DB・課金・権限)の位置と、失敗したら何が起きるか | いつでも参照できる |
| 4: 実装 | コードそのもの | 必要なときだけ、AIに絞り込ませて降りる |

**運用**

- 地図の生成・更新はAIに任せる。ただし**所有者は人間**。障害対応や大きな変更のたびに、判明した仕様・依存・注意点をAIに地図へ書き戻させる。実装は [skills/update-map](skills/update-map/SKILL.md)(初版生成・更新・コードとの矛盾検査)。
- **初版検収の最低ラインは Level 2(不変条件)・Level 3(危険領域)の通読・修正である。** 障害時に切り分けの根拠になるのはここで、誤りが通過すると障害時に確実に誤った切り分けをする。全通読を要求して検収が形骸化するより、最低ラインを確実に守らせる。機械検証を前処理に使ってよいが、**検証は検収の代替ではない——`reviewed` を書くのは人間だけ**(手順は [skills/update-map](skills/update-map/SKILL.md))。
- **地図の維持を善意に頼らせない。** レイヤー表の「常に頭にある」「いつでも参照できる」を本人任せにせず、フローの強制点に接続する:契約・不変条件・危険領域を変えるPRに地図の更新が含まれているかをAI一次レビュー([§7](#s7))が検査し、ズレは月イチの矛盾検査で拾う。**地図の更新は当該PRに同梱する**(マージ後に回すと[§3](#s3)のマージ基準を確認できない)。障害対応で判明した事実の書き戻しだけが、事後の更新になる。
- **地図は `reviewed` 日付を持ち、鮮度を機械判定できるようにする**(様式は [skills/update-map](skills/update-map/SKILL.md)。g3docのfreshness運用に倣う)。
- **地図は領域ごとに持つ**(ドメインオーナーの担当単位。[§8](#s8))。**分量の上限は領域単位**(値は [skills/update-map](skills/update-map/SKILL.md))。領域が2つ以上あるなら、全体の Level 0 には**領域の境界と、境界をまたぐ契約だけ**を置く。**「同じファイルを触るから同じ領域」では切らない**——手掛かりは、その地図の `description` 1行で今回の変更も説明できるか、更新条件が混ざらないか、の2つ。
- **領域はリポジトリをまたがない。** 地図ファイルは1つのリポジトリにしか置けないので、またぐ切り方をすると、もう一方のリポジトリのPRが「地図の更新は当該PRに同梱する」を満たす手段を失う。システムが複数リポジトリに分かれる場合は、**各リポジトリが自リポ視点の Level 0 を持つ**(2枚の Level 0 の重複を恐れない)。リポジトリ横断の単一「全体 Level 0 ファイル」は作らない。境界をまたぐ契約は、実物(OpenAPIスキーマ等)を呼ばれる側のリポジトリに置いて双方から参照する。**両リポジトリの Level 0 間の齟齬は、月イチの矛盾検査の対象に含める。**
- **大量のドキュメント生成は解決策ではない。** 人間が将来判断するために必要な情報**だけ**を維持する。100ページの自動生成ドキュメントは、読まれない時点で理解負債の別の形にすぎない。
- 原則:**コード量が増えても、人間が理解すべき情報量が増えない構造にする。**

### 根拠と先行例:地図をリポジトリにコミットするのは新発明ではない

「なぜドキュメントをリポジトリにコミットするのか」への答えは1つで足りる——**コードと同じPRで更新されるドキュメントだけが、鮮度を保てる**。別管理のWikiは「コードが変わったのに直っていない」状態を検出できず、リポジトリ同梱なら更新漏れをAI一次レビュー([§7](#s7))が機械的に指摘できる。この置き方は既に大規模に実践されている。

- **Google g3doc** — 同一リポ・レビュー経由・freshness([『Software Engineering at Google』10章](https://abseil.io/resources/swe-book/html/ch10.html))。「コードディレクトリの隣に置く」は既定にしないが、領域がディレクトリ部分木と一致するなら隣接配置も本基準と両立する
- **Spotify Backstage** — 所有者・依存関係・ライフサイクルを [`catalog-info.yaml`](https://github.com/backstage/backstage/blob/master/catalog-info.yaml) としてコードと同じリポジトリにコミットして保守する。Level 0 とほぼ同型
- **AGENTS.md** — リポジトリ同梱の「機械と人間のための案内図」。2025年に仕様化され Linux Foundation 傘下、6万超のOSSプロジェクトが採用
- **SREのプレイブック** — Googleは「プレイブックの整備でMTTRがその場しのぎ比で約3倍改善する」と述べている([SRE本・序章](https://sre.google/sre-book/introduction/)。Google自身の推定値でありRCTではない)。Level 3(危険領域の位置と戻し方)の先行形

反対側の実証も踏まえている。GitHub上の900超リポジトリを対象としたADRの調査([arXiv:2604.27333](https://arxiv.org/html/2604.27333v1))では、**導入したリポジトリの約50%が5件未満で更新が止まり、25件超の継続例は2%未満**——善意ベースのドキュメント保守は高確率で死ぬ。上の運用(PR同梱・AI一次レビューの検査・freshnessヘッダー)は、この実証済みの失敗モードへの対策として置いてある。なお「Level 0〜3 の地図をAI生成・人間検収・PR同梱」という**この形そのものの実証は存在しない**。

### 訓練:使わない理解は腐る

かつて理解は、実装の摩擦から自然に生まれていた。実装もレビューもAIに移した後で、地図を「読むだけ」の理解は紙の上の理解であり、障害の夜には使えない。**地図には運動を付ける。**

実証的な根拠がある。AI補助で新しいライブラリを学習した開発者は、手書きで学習した群よりテストの得点が17ポイント低く(50% 対 67%、p=0.01)、**低下が最も大きかったのはデバッグ問題**だった。しかも時間短縮の効果は有意ではなかった([AnthropicのRCT, arXiv:2601.20245](https://arxiv.org/abs/2601.20245)、査読前)。また人は自分が書いたコードでも、2ヶ月後には識別子をほとんど思い出せない([Fekete & Porkoláb, 2023](https://www.cs.ubbcluj.ro/~studia-i/journal/journal/article/view/89)、小規模実験)。**書かない時代の理解は、意図的な運動なしには作られも保たれもしない。**

- **障害シミュレーション** — 「決済が落ちた。どの箱か3分で特定せよ」を定期的にやる。マージゲート([§3](#s3))の実技試験を兼ねる。
- **裁定の義務** — 独立照合([§4](#s4))の食い違いは人間が裁く。これが契約理解の維持運動になる。
- **地図への尋問** — AIの説明を読むだけで済ませず、「ここを変更すると何が壊れる?」と質問で掘る。
- **新規参加者による障害シミュレーション** — 途中参加者には、地図(Level 0〜3)と手順書(スキル)だけを渡して、上と同じ問いを出す。通れば地図と手順書が現実を写している証拠になり、通らなければ**欠落は地図の側にある**(新人の力量の問題にしない)。既存メンバーは地図に無い知識を頭の中から補えてしまうので、地図の欠落を検出できるのは、その知識をまだ持たない人だけである。月イチの矛盾検査・定期の障害シミュレーションに並ぶ、地図の第三の検査手段。合流までの所要も記録する(先行報告では「同じ環境を渡すだけで短期間で合流」が観測されている(木村健一郎「[AI駆動開発をチームに根付かせる](https://speakerdeck.com/kenichirokimura/embedding-ai-driven-development-into-your-team)」2026-08-27。1チームの1年間の実運用報告であり、定量評価ではない))。

<a id="s12"></a>
## 12. 導入の順序(推奨)

1. **ベースラインを測り始める。** レビューの実時間とリードタイムを記録する。改善はまだしない。**滞留時間・自己申告・レビュー品質(障害率)を混同せず、別の系列として持つ。**
2. **基準を合意して運用を始める。** 3段階・2ゲート・隔離ルールをチームで合意し、レビューガイド+設計メモ+AI一次レビューを始める。**マージゲート([§3](#s3))は地図の初版検収をもって、領域ごとに発効する。地図を先に用意する工程は置かない**——ある領域に初めて触るPRの中で初版を作り、ドメインオーナーが検収する。あわせて**月イチの矛盾検査の担当と実行手段**(カレンダーかCIジョブ)を決める——担当を決めない定期検査は発火しない。**カバレッジゲート([§10](#s10))はこの段でラチェット方式で置く**——現状の実測値を床にするだけなので、基準の合意と同時に入れられる。ミューテーションテストは行レベル対象が出た時点で足す。
3. **計上ルールを決める。** 実測値に基づいて、レビュー時間を見積りにどう入れるか(ポイントに含める/容量の一定割合を先取りする)と、PR分割の目安を決める。**この基準自身の運用コストも計上対象に含める。人間の時間(設計メモ・裁定・訓練・地図維持)と、エージェントの文脈(手順書の分量・トークン消費・応答待ち時間。[§7](#s7)の測定項目)の2系列で持つ**——「計上する」を掲げる基準が自分を計上しないと、レビュー時間の減少だけが観測されて、総コストが増えていても反証できなくなる。
4. **差分を確認して改訂する。** ベースラインとの差分(レビュー時間・リードタイム・障害件数・切り分け成功率・分類比率)を見て改訂する。**守られていない項目は、厳格化ではなく削除を検討する。**

### 削除の手続き

「重すぎて守れない」と「守りたくないから守らない」を区別するため、削除には次を要する。

- (a) 守られない原因の確認(コストなのか、動機なのか)
- (b) チームの明示的な合意(なし崩しの死文化は削除ではない)
- (c) 再追加する条件の記録(例:分類比率が契約側に流れたら、障害で露呈したら)

また削除できるのは運用項目だけである。**[§6](#s6)のオンコール原則・[§2](#s2)の行レベル対象・[§8](#s8)の責任割当は、削除ではなく改訂のみ。** これらは基準が存在する理由そのものである。

---

<a id="refs"></a>
## 参考文献(規範ではない。本文の根拠と出自)

- **R1 理解負債の出自**([§1](#s1))— comprehension debt: [Jason Gorman, 2025](https://codemanship.wordpress.com/2025/09/30/comprehension-debt-the-ticking-time-bomb-of-llm-generated-code/) / [Addy Osmani, 2026](https://addyosmani.com/blog/comprehension-debt/)。近い概念として Storey の [Triple Debt Model](https://queue.acm.org/detail.cfm?id=3807966)(technical / cognitive / intent debt)。
- **R2 独立照合・自動版の先行研究**([§4](#s4))— 自然言語の仕様からLLMで差分テストを生成して実バグを見つけた [DiffSpec(arXiv:2410.04249)](https://arxiv.org/abs/2410.04249)、LLMでN版プログラミングを自動化した [Galápagos(arXiv:2408.09536)](https://arxiv.org/abs/2408.09536)。
- **地図をリポジトリに置く先行例と反証**は [§11](#s11) 内に残している。運用の互換条件(隣接配置の可否・ADR調査への対策)が根拠と分離できないため。
- OSSのAIポリシー・実務報告・関連研究の一覧は [README](README.md#関連する先行事例研究2026年8月調査) にある。

---

*この基準自体も改訂の対象である([§12](#s12))。*
