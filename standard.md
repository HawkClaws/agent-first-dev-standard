# エージェント前提開発基準(v0.9)

コーディングエージェント(Claude Code、Codex 等)に実装を任せる前提のプロジェクトで、理解・PR・マージ・レビューをどう運用するかを決める基準。中心にある問いは**どこまで理解してからPRを出し、どこまで理解してからマージするか**である。特定のエージェント製品には依存しない。

## この基準を3行で

- 「全部読んでからマージ」は量的に無理。「読まずにマージ」は障害の夜に破綻する。この基準はその間に線を引く道具である。
- 人間が理解しておくのは**レビューのためではなく、後で保守・障害対応・意思決定をする自分のため**である。
- だから対策は「レビューを頑張る」ではない。理解の手間を**先払いする・減らす・工数に計上する**の三つである。

## 用語(この文書だけの言い方)

| 言葉 | 意味 |
|---|---|
| **理解負債** | 将来必要になる理解を、今持っていない状態。行数ではなく「障害・変更・判断のときに人間が持っていない理解」で測る |
| **契約** | 呼び出しの約束を書き出したもの。「この入力を渡せば、この出力が返る。失敗するときはこう失敗する」の3行(実例は§2冒頭) |
| **不可逆系** | やり直しが効かないもの。DBスキーマ変更、データ移行、課金、外部送信、削除 |
| **地図** | システム全体の構造・契約・不変条件・危険箇所を、人間が判断に使える短さでまとめたもの。全体像は文書を見ずに言える状態まで持つ(§11) |
| **独立照合** | 実装を経由していない解釈——人間が先に書き出した期待値、または実装非開示セッションが書いたテスト——を、実装と突き合わせること(§4) |
| **独立テスト生成** | 独立照合の自動版。実装を見せていない別セッションに、仕様とインターフェースだけを渡してテストを書かせること(§4) |
| **裁定** | 独立照合で仕様の読みが2通りに割れたとき、どちらが意図だったかを人間が決めること(§4)。レベル分類の「判定」とは別物 |

---

## なぜこの基準が要るのか

AI以前は、実装している時間がそのまま**理解する時間を兼ねていた**。書いた本人は手を動かしながら分かるし、周りも小さな差分を順に見ていけば追いつけた。実装コストと理解コストは、同じ時間の中で一緒に払われていた。

エージェントが実装の時間をほぼ消した。消えたのは実装の分だけで、**理解の分の請求書は消えていない**。しかもそれが、レビューとマージの席に一度にまとまって届くようになった。

つまりレビューが重くなったのではない。**これまで実装作業の中に隠れて見えなかったコストが、一箇所に集まって見えるようになった**だけである。

だから打ち手は「レビューを頑張る」ではなく、次の三つになる。

- **先払いする** — 実装前に方針を合意しておく(§7)
- **減らす** — 深く理解する範囲を絞り、それ以外は隔離する(§2・§5)
- **計上する** — レビューと理解の時間を見積りに入れる(§12)

---

## 1. 三つの問いを混ぜない

「AIレビューでいいのか、人間レビューが必要か」を一つの問いとして扱ってはいけない。次の三つは別の問いで、決め方も別である。

| 問い | 決め方 |
|---|---|
| ① レビュー(検査)をAIに任せられるか | **実績**で決める(§7) |
| ② 人間がこのコードを将来理解する必要があるか | **理解負債**で決める(判定軸は§2に一本化する) |
| ③ 何を・どこまで理解するか | **3段階**で決める(§2) |

ここで一番混同されやすいのは①と②である。**検査をAIに任せても、②③の義務は消えない。** 人間の理解はレビューを通すためのものではなく、半年後に障害を切り分けたり仕様変更を判断したりするためのものだからである。

**理解負債とは、将来必要になる理解を今持っていない状態のこと。** 再生成できる形にきちんと隔離された100万行は負債ではない。誰も意味を説明できないDBのデータ変換ロジック100行は巨大な負債である。**負債は行数では測れない。「障害・変更・判断のときに人間が持っていない理解」で測る。**

> この考え方は英語圏で **comprehension debt** として先に論じられている([Jason Gorman, 2025](https://codemanship.wordpress.com/2025/09/30/comprehension-debt-the-ticking-time-bomb-of-llm-generated-code/) / [Addy Osmani, 2026](https://addyosmani.com/blog/comprehension-debt/))。近い概念として Storey の [Triple Debt Model](https://queue.acm.org/detail.cfm?id=3807966)(technical / cognitive / intent debt)がある。この文書の「理解負債」は、それらの整理と訳語である。

## 2. どこまで理解するか — 3段階

「全行を理解してからマージ」は誰も守れない。「テストが通ればOK」は障害のときに崩れる。そこで**コードを1行ずつ理解するのをやめて、「契約」を理解の単位にする。**

ここでいう契約は、**呼び出しの約束を書き出したもの**である。抽象的な話ではない。ユーザー作成の関数なら、契約はこの3行に収まる。

```
createUser(email, plan)
  成功 → 作られたユーザーを返す(id は新規発行)
  失敗 → メール重複なら DuplicateError。このとき DB には何も書かない
  前提 → email は検証済みのものを渡すこと
```

呼ぶ側は「検証済みの email を渡す」義務を負い、呼ばれる側は「重複なら DuplicateError を返し、かつ何も書き込まない」義務を負う。**バグとは、どちらかがこの義務を破ることである。**

契約書の契約とほぼ同じ意味で使っている。当事者は**呼ぶ側**と**呼ばれる側**で、義務の中身が入力と出力——失敗するときも決められた形で失敗すること——にあたる。この取り決めを、検証できる文面にしたものがテストである(§4「テストは契約書」)。

**中身がどう書かれているかではなく、この3行を理解する。** そして対象によって濃淡をつける。

| レベル | 対象 | 何ができれば「理解した」か |
|---|---|---|
| **契約理解**(最低ライン) | すべてのAI生成コード | 何をするものか一文で言える。入出力と、失敗したときどうなるかを知っている。契約がテストで固定されている |
| **構造理解** | ドメインロジック。今後も人が変更するコード | データがどう流れ、どこで状態が変わるかを口で説明できる。同じ設計を自分で書き直せる |
| **行レベル理解** | 不可逆系(スキーマ変更・データ移行・課金・外部送信・削除)、セキュリティ境界、並行処理 | 一行ずつ説明でき、エッジケースを列挙できる。AI以前と同じ基準 |

**レベルの決め方**

1. まず**影響範囲 × 可逆性(やり直せるか)**で下限が決まる。「被害が大きい」の正体は、これが広くて戻せないことである。
2. そこから**変更頻度**と**ドメイン重要性**は、上げる方向にだけ働く。人が頻繁に触るコードほど深い理解が要る。
3. 複数の軸で違うレベルに当たったら、**一番高いレベルを採る**。

なお§5の「丸ごと再生成」は変更ではなく交換なので、変更頻度には数えない。

**補足**

- **コードは再生成できるが、データは再生成できない。** 間違ったマイグレーションが流れた後の状態は、コードを作り直しても戻らない。「消耗品として扱っていいか」の境界線は、**状態を持つか=不可逆な副作用があるか**である。
- DBを慎重に扱うのはAI以前からの常識だが、AI時代はそこを通る変更の量が桁違いに増えたので、相対的な重要度が上がった。
- 契約理解の「テストで固定」は**自動テストが前提**。手動のUI確認で守れるのは自分が見た経路(正常系)だけで、AI生成コードの典型的な失敗——**正常系しか動かない(Happy Path Bias)**——をそのまま通してしまう。
- ただし契約レベルの「固定」の目的は**回帰検知**、つまり契約が変わったら気づけることである。**契約が正しいことの保証ではない。** 実装と同じセッションが書いたテストは自作自演になりうる(§4)。正しさの検証(独立照合)は構造理解以上でやる。契約レベルのテストの最低品質——アサーションがあるか、実装の挙動をなぞっただけでないか——は、AI一次レビュー(§7)が機械的に検査する。
- **業務ルールの中核(ドメイン重要性が高い箇所)は、被害の大きさや変更頻度にかかわらず構造理解以上とする。** 実装の詳細は理解を下げてよいが、業務上の判定ロジックの理解は下げない。
- レビュー指定(行/構造/契約のどれか)は著者が下書きする。ただし**低いレベルに倒すほど著者が得をする構造になっている**——低くすれば独立テストも精読もミューテーションテストも免除されるからである。だから**行レベル対象に当たるかと、ドメイン中核かの判定はドメインオーナーの確認事項とし(§8)、著者の自己申告では確定させない**。分類の分布はKPIとして監視する(§7)。

## 3. 二つのゲート — PRとマージ

| ゲート | 基準 | 意味 |
|---|---|---|
| **PRを出すとき** | 説明できるか | 「なぜこの設計にしたか」「このエッジケースはどうなるか」に答えられる。求めているのは暗記ではなく説明。**著者はエージェントではなく自分である** |
| **マージするとき** | 追跡できるか | 本番で壊れたときに、**地図(§11)を見てどの箱が壊れたか特定できる状態**でマージする。特定するのはオンコール担当(§8)であり、マージした人の記憶に頼らない。箱の並びと繋がり(=地図)は全員が常に理解している。箱の中身は§2の濃淡に従う |

マージ基準を「全コードを追跡できること」と字義通りに運用すると全行精読に戻り、誰も守らない基準になる。だから**追跡の責任は地図のレベルに置き(§11)、中身を読まない箱には§5の隔離を義務づける**。

## 4. テストは契約書。ただし条文が妥当かの判断は人間の仕事

> ⚠ **エージェントは、実装に合わせたテストを書く。** 「テストが通っている」は「契約が正しい」を意味しない。実装とテストを同じセッションが書いたなら、それは検証ではなく自作自演である。

### 自作自演を断つ:実装を経由していない解釈と突き合わせる

規範はこの一行である。**不可逆系の契約は、実装を経由していない解釈と突き合わせてから通す。** これを**独立照合**と呼ぶ。

**レビューはこれを代替しない。** レビュアーは実装を読んでからテストを読むので、読み方が「このテストは実装と合っているか」に固定される。仕様の別の読み方は原理的に出てこない——判断のアンカーが実装側にあるからである。必要なのはテストの本数ではなく、**実装を経由していない2つ目の解釈**である。

照合に使う解釈は、次の2つのどちらで作ってもよい。

- **人間版(既定)** — レビューに入る前に、**実装前に書かれた仕様とインターフェースだけ**を読み、期待する入出力と失敗時の挙動を自分で書き出す。**書き出してからテストと実装を読む。** 人間自身が2つ目の解釈者になる。契約1本あたり数分で終わる。
- **自動版(独立テスト生成)** — 実装コードを見せない別セッションに、仕様とインターフェースだけを渡してテストを書かせる。可能なら別ベンダーのモデルを使う(同じモデル同士は同じ読み間違いをしやすい)。プロンプトに「実装を読むな」と書いても防壁にはならないので、**ファイル読み取りツールを持たない実行形態(ツール制限)で起動する**(手順は [skills/gen-tests](skills/gen-tests/SKILL.md))。

**自動版に上げる条件**(人間版で足りないとき):

- 契約の数が多く、人間の先読みが全件に回らない
- 仕様が長い・入り組んでいて、読んでも期待値を書き出せない(書き出せないなら、それは理解が足りないというシグナルでもある)
- 同種の変更で過去に裁定が発生した領域(曖昧さの再発が見込める)

**どちらの版でも守ること:**

- **解釈のもとにする仕様は「実装前に書かれたもの」に限る**——チケット、承認済み設計メモの要求部分。実装後にAIに書かせた仕様サマリを使えば、仕様を経由して自作自演に戻る。設計メモを使うときも要求と契約の記述だけを見て、**方針や触る範囲(=実装の設計図)は見ない・渡さない**。
- 食い違いは修正対象ではなく**裁定**対象である。**落ちたテスト・合わなかった期待値は「仕様が2通りに読めた証拠」=仕様の曖昧さ検出器**として扱う。どちらの読みが意図だったかを裁くのは仕様の持ち主(人間)であり、**裁定の結果は仕様に書き戻す。**
- 裁定する人の立場に注意する。「仕様の持ち主」が著者本人(=実装方針を書いた当事者)である場合、自分の解釈を自分で正解にできてしまう。**不可逆系の裁定にはドメインオーナー(§8)を加える。**

**適用範囲:不可逆系(DB・課金)には必須。それ以外の構造理解対象には推奨。契約理解レベルには過剰装備。** 必須なのは独立照合であって、自動版ではない。

> 自動版には学術的な先行研究がある。自然言語の仕様からLLMで差分テストを生成して実バグを見つけた [DiffSpec(arXiv:2410.04249)](https://arxiv.org/abs/2410.04249)、LLMでN版プログラミングを自動化した [Galápagos(arXiv:2408.09536)](https://arxiv.org/abs/2408.09536)。この節はその実務向けの簡易版にあたる。

### それでも人間に残る仕事

- **2つの解釈が同じ読み間違いで一致した場合、食い違いは出ない。** 食い違いは検出できるが、一致していることの正しさは保証されない。人間版でも自動版でも同じである。同じモデル・同じベンダーで揃えるとこれが起きやすい。
- **自動版は、曖昧な箇所を「幅を持たせたアサーション」に吸収してしまうことがある。** 曖昧さがテストの中に埋め込まれると、どちらの解釈でも緑になり、検出力がゼロになったまま「全件緑」という強い見た目の結果だけが残る。**緑の件数は独立照合の成果ではない。** 人間版は期待値に一つの値を書かされるので、この逃げ道がない。
- **人間版は、やったことを機械検査できない。** 期待値を先に書いたのか、テストを見てから書いたのかは自己申告でしか分からない。自動版はツール使用ログで隔離を確認できる(それでも隔離が破れたかは出力からは分からない)。**この差は、この規範を守らせる力が仕組みではなく規約にあることを意味する。**
- どちらも思いつかなかったエッジケースは、静かに抜け落ちる。レビューのときに自分でケースを1〜2個考えて足す運用は残す。**思いつけないなら、それは理解が足りないというシグナルである。**
- 前提として、AIの間違いは**コードの層から仕様・コンテキストの層に移動している**。人間が検証する対象も「コードが仕様通りか」から「**仕様とテストが意図通りか**」に移った。この層は委譲できない。

## 5. 隔離ルール — 「ライブラリ扱い」していい条件

AI生成コードを「ライブラリを使う感覚」で扱ってよい。ただし思い出してほしいのは、**ライブラリの中身を読まずに済むのは、多数の利用者・メンテナ・安定した契約という生態系が、理解の省略を肩代わりしてくれているから**である。AI生成コードにはそれがない。

だから、**ライブラリ扱いするコードは、実際にライブラリの形にする**:

1. 安定したインターフェースの後ろに置き、他のコードと絡ませない
2. テストを契約書として固定する(§4の条件付き)
3. 中身をパッチで直さない。壊れたら**切り離す・戻す・丸ごと再生成する(作り直す)**

ドメインの中核に編み込まれて境界が引けないコードは、ライブラリ扱いできない(構造理解以上が必須)。**理解しない範囲を「理解しなくても安全な形」に置けているかは、頭の良さの問題ではなく設計の問題である。**

- **隔離してもドメイン中核の理解義務は下がらない。** きれいに隔離された課金判定ライブラリでも、業務判定の理解は§2により構造理解以上のままである。隔離で下げられるのは非中核コードの理解義務だけ(§2が優先)。
- **再生成は交換であって修正ではない。** 契約テストで固定していない挙動——出力の順序、丸め、フォーマット、エラーメッセージ——に、下流が依存していることがある(Hyrumの法則)。だから再生成したら、契約テストが通ることに加えて**旧実装との出力差分を確認する**。特に外に出る出力(ファイル・API応答・イベント)は疑う。契約テストは「一致の正しさは保証しない」品質のものなので(§4)、それだけでは同じ動作の証明にならない。

## 6. この基準の根拠 — 障害対応で何が起きるか

エージェントを障害対応に使うのは有効である。問題は**「聞くしかない」状態**、つまり人間側に判断の逃げ道がないことである。

1. デバッグの本質は仮説検証で、仮説の質はシステム理解に依存する。理解がないと**何を聞けばいいか分からない**。
2. 障害中は時間がない。もっともらしい誤答を検証せずに実行しやすい。**間違った修正は障害を二重にする。**
3. 理解があれば**「まず止血、原因対応は後」の分離**ができる(とりあえず再起動で凌ぐ、など)。理解ゼロだと原因が分かるまで何もできない。
4. 外部LLMに繋がらない状況で、システム理解が全部外部サービス側にあるのは単一障害点である。
5. 障害のたびにエージェント頼みだと理解はさらに減り、依存が加速する。

> **原則:エージェントは優秀な同僚にはなれるが、オンコール責任者にはなれない。** マージ基準(§3)はこの一点から逆算されている。

## 7. レビューの重心移動と権限移譲

実装が安くなったのだから、レビューの重心を「できたコードの検査」から**「作る前の方針合意」**に移す。

- **設計メモ** — 着手時に10〜15分で、「この方針・この構造で書かせる」という数行のメモをチームに流し、異論を確認する。メモには**返事の期限を明記する**(例:「本日中に異論なければこの方針で進めます」)。返事がなかったときの扱い(同意とみなすか)は、チームで先に合意しておく。**期限のないメモは合意形成ではなく着手通知に落ちる。** 不可逆系を含む場合は無返事のまま着手を認めず、明示的な承認を待つ。これでコードレビューは「方針通りか+危険箇所の精読」に縮む。
- **レビューガイド** — PRに、背景(チケットに書いていない前提も)・採ったアプローチと捨てたアプローチ・重点的に見てほしい箇所・流し読みでいい箇所を必ず添える(テンプレートは§9)。下書きはエージェントに書かせ、本人が直す。
- **AI一次レビュー** — 人間が見る前に、規約・既知の落とし穴・テストと実装の整合を検査する。ただし全部が「機械検査」ではない。lint的に白黒つく指摘と、AIの**所見**(このテストは契約を表しているか等、本来は判断に属するもの)を出力で区別し、**所見は人間の判断の代わりではなく候補として扱う**。不可逆系の誤分類警告は他の指摘と混ぜず、先頭に単独で出す(警報疲れで一番重要な警告が流れるのを防ぐ)。実装を書いたセッションとは**別セッション**で実行する(同じセッションでは自分の出力を自分で検査することになる)。人間は設計判断と契約の妥当性(§4)に集中する。
- **小さいPR** — レビュー時間はdiffサイズに対して急激に伸びる。チケットの分割でPRサイズを抑える。
- **ドメインオーナー** — 領域ごとの最終責任者を決める。全員が全領域を薄く見る体制は、「誰も直せないコードベース」に直結する。

これらは [skills/](skills/) にエージェント用の手順書(スキル)として実装してある。**下書きと機械検査だけを自動化し、判断は空欄として人間に残す**設計になっている。この空欄の一覧が、このフローにおけるHuman-in-the-Loopの定義そのものである。

### 権限移譲の原則:実績が出た方向に権限を移す

「人間レビューは必要」も「AIに全部任せてよい」も、**前提としては立たない**。AI一次レビューを出発点にして、実績が示された種類の変更から、人間レビューの範囲を段階的に外していく。

**測るもの**

- レビュー時間・リードタイム
- AIが見つけた問題の数
- 人間が追加で見つけた問題の数
- 両方が見落として本番で発覚した問題の数
- false positive率
- **AIレビューだけで通したPRの障害率**
- **AIの警告(不可逆系の誤分類など)を人間が却下したPRの障害率**
- **行/構造/契約の分類比率の推移**(契約側へじりじり流れていないかの監視)
- **障害対応時間・切り分け成功率**(§6が守ろうとしている対応能力の直接測定。障害シミュレーション§11の結果を含む)

**権限を移す判断には、レビュー時間やリードタイムだけでなく、障害率と切り分け成功率を必ず含める。** 速度系の指標は、人間が理解を放棄すればするほど改善する。だから単独では権限移譲の根拠にならない。§10で警告するグッドハートの法則(指標を目標にすると指標が腐る)は、この測定リスト自身にも当てはまる。

**2026年時点の外部証拠**(判断の初期値。自チームの実績で上書きしていく)

- AIレビュー提案の採用率は人間の提案よりかなり低い(採用16.6% 対 人間56.5%/別研究では却下56.3%)。主な原因はfalse positiveと意図とのズレ — [arXiv:2603.15911](https://arxiv.org/abs/2603.15911)、[arXiv:2607.03316](https://arxiv.org/abs/2607.03316)(いずれもプレプリント)
- AIエージェントによるレビューで判断は速くなるが、品質向上には繋がらない — [arXiv:2607.13196](https://arxiv.org/abs/2607.13196)(プレプリント)
- 一方で現実には、AI生成PRの多くがそもそも人間レビューを受けていない — [EASE 2026(査読済み)](https://arxiv.org/abs/2605.02273)。**基準を決めなければ、なし崩しに人間レビューが消えていく。**
- AIレビューツールの性能数字は、測り方で大きく変わる。あるツールはベンダー自社ベンチマークで[検出率82%](https://www.greptile.com/benchmarks)を主張するが、[競合ベンダーのベンチマーク](https://tenki.cloud/benchmarks/code-reviewer)では同じツールが36.1%になり、そこでは測った側の自社ツールが1位になる。**各ベンダーが自社ベンチマークで勝つ**この現象は[第三者の整理](https://deepsource.com/blog/ai-code-review-benchmarks)がある。これは**外部の数字は初期値にしかならない=自チームの実績で決めるしかない**ことの根拠でもある。
- 関連研究として、リスクに応じて人間の監督の形を段階化する提案がある(in-the-loop / over-the-loop / 自動+監視の3段階。[Graduated Human Oversight, arXiv:2606.22484](https://arxiv.org/pdf/2606.22484))。この基準は監督の形ではなく**理解の深さ**を軸に段階化する点が違う。

## 8. 責任の所在

**エージェントとAIベンダーの責任はゼロである。** 利用規約上、出力の責任は利用者にあり、法的にもAIは責任主体になれない。つまり責任の総量は減らないまま人間側に残る。だから割り当てを明示する。

| 責任 | 誰が | 内容 |
|---|---|---|
| **説明責任** | PR著者 | このコードが何をするか・なぜこの設計かを説明する。エージェントに書かせても、著者は自分である |
| **一次対応責任** | オンコール担当(ロール) | 本番で壊れたとき最初に動き、地図(§11)を頼りにどの箱が壊れたか特定する。**交代制のロールであり、マージした個人が変更ごとに永久に負うものではない**(退職・異動・時間経過で個人の記憶は消える。§11の通り、自分が書いたコードでも2ヶ月で忘れる) |
| **マージ確認責任** | マージした人 | マージ時点で、レビュー指定・PRガイド・地図が§3の基準を満たしていることの確認。これがマージゲート(§3)の実体である。**壊れた後の対応はオンコールの仕事で、マージした人を深夜に呼ぶ制度ではない** |
| **地図の維持責任** | ドメインオーナー(自領域) | 自領域の地図(§11 Level 1〜3)の初版検収と、記述が現実と合っているかの最終判断。月イチの矛盾検査そのものはオンコール輪番かCIジョブに置いてよいが、**齟齬の判定は委譲できない**(コードが正しく進化したのか、地図が正しくてコードが逸脱したのかの判断) |
| **領域の最終責任** | ドメインオーナー | その領域の設計判断、および「行レベル理解の対象か」「ドメイン中核か」の最終判断(著者の自己申告を確定させる側) |
| **対外責任** | 組織 | 顧客・法律に対する責任は常に組織が負う。個人が対外的に負うものではない |

- **レビュー承認は責任の共有であって移転ではない。** 承認をもらっても著者の説明責任は消えない。
- **責任を明確にするのは、個人を吊るすためではない。** 障害のときに誰が動くかで迷わないためである。事後の振り返りは個人ではなく仕組みを対象にする。そうしないと誰もマージしなくなり、基準ごと形骸化する。
- **AI利用の開示が、責任割り当ての前提になる。** 先行例として [FedoraのAI-Assisted Contributions Policy](https://communityblog.fedoraproject.org/council-policy-proposal-policy-on-ai-assisted-contributions/)(提案は2025年10月に[承認](https://lwn.net/Articles/1042947/))があり、AIが大部分を無変更で生成した貢献に開示を義務づけ、コミットトレーラー `Assisted-by:` を推奨している。この基準ではPRテンプレート(§9)に**AI利用の開示欄**を置く。**レビュー指定(理解の深さ)とAI生成の範囲は別の情報で、前者は後者の代わりにならない。**

## 9. PRテンプレート — レビューガイドの実装

フォーマットの目的は、理解を「伝える」ことではない。**レビュアーの限られた注意力を、正しい場所に配分すること**である。AIの冗長さはプロンプトの工夫では抑えられない。文字数上限・チェックボックス・そもそも書く欄がないという**構造**で抑える。

テンプレート本体は [templates/pr-template.md](templates/pr-template.md)。

**運用ルール**

- **AIが下書きし、著者が直す。** 下書きを直せないなら、それは理解していないということ(PRゲート§3の実技試験を兼ねる)。
- 長い説明は `<details>` に畳み、本文は上限内に収める。読むかどうかはレビュアーが決める。
- 形骸化対策として、**本人の判断を要求する欄**を2つ仕込んである。
  - **「一番怪しい箇所」** — 選べないこと自体が理解不足のシグナルになる
  - **「採らなかった選択肢」** — 採った理由より捨てた理由の方が判断が見える。情報密度が最も高い欄
- ただしこの2欄は**ツールでは守れない**。スキル(pr-guide)は埋めない仕様にしてあるが、著者が別のAIに書かせることや、first-reviewの指摘・設計メモの下書きを転記することは検知できず、形だけは整ってしまう。**実効性はテンプレートではなく口頭確認に置く**:レビューや振り返りで著者に「なぜそこが一番怪しいのか」を聞く。転記しただけの欄は一往復で剥がれ、それ自体が理解不足のシグナルとして機能する。
- **「レビュアーへの質問」** 欄は、レビューを漫然とした通読から「答え探し」に変えるためのもの。

## 10. カバレッジゲート — 床として使い、目標にしない

テストが書かれていないコードを機械的に弾くため、CIにカバレッジゲートを置く。ただし**カバレッジは「実行された」しか測れず、「検証された」は測れない**。エージェントは数字だけ満たすアサーション無しテストを無料で量産できるので、**目標にした瞬間に指標が腐る**(グッドハートの法則)。

- **片方向の信号として使う** — 低い=テストが無いことは確実に分かる。高い=品質は何も保証しない。
- **全体%ではなくdiffカバレッジ**(変更行ベース)でゲートする。レガシー部分に人質を取られない。
- **閾値は理解レベルと連動させる** — 行レベル対象(不可逆系)は高く、UIグルーコードは低く。ディレクトリ単位で設定する。
- **ラチェット方式で始める** — 理想値を掲げず、現状の実測値を床にして「下がったら落ちる」から始める。理想値ゲートは導入日に全滅して即無効化されるのが定番の失敗である。
- **テストの質はミューテーションテストで担保する**(Stryker / mutmut 等)。アサーション無しテストはここで確実に死ぬ。重いので行レベル対象に絞る。独立照合(§4)と併用すると、テストが仕様側と変異側の両面から検証される。
- **ミューテーションを回さない範囲(契約・構造レベル)でも、アサーションがあるかどうかだけは全レベルで機械検査する**(lintルールまたはAI一次レビュー§7)。ラチェットが下がったとき最も安い回復手段は「実行するだけのテスト」の量産であり、この検査がないとカバレッジという計器が恒久的に嘘をつき始める。
- 「実行された」と「検証された」の差は実測でも大きい。行カバレッジ100%のテストスイートがミューテーションテストでは[80%しか殺せなかった実例](https://codeheir.com/blog/2022/03/05/mutation-tests-100-code-coverage-is-not-enough/)があり、巧妙なミュータントに対しては**LLM生成テストスイート**の平均検出率が71%から40%まで落ちるという[ベンチマーク(SWE-Mutation, arXiv:2605.22175)](https://arxiv.org/abs/2605.22175)もある。

## 11. 理解の地図と訓練 — 上の層から降りる

人間がAIと同じやり方(コードを読む)で理解しようとすると、量で負ける。**人間は抽象度の高い層から降りる。** マージゲート(§3)でいう「地図」の実体は、次のレイヤー構造である。

| レベル | 内容 | 人間の持ち方 |
|---|---|---|
| 0: システム地図 | 何がどこに繋がっているか | 常に頭にある |
| 1: 契約 | 各コンポーネントの入出力・副作用・エラー時の挙動 | いつでも参照できる |
| 2: 不変条件 | 絶対に壊してはいけないこと | いつでも参照できる |
| 3: 危険領域 | 不可逆系(DB・課金・権限)の位置と、失敗したら何が起きるか | いつでも参照できる |
| 4: 実装 | コードそのもの | 必要なときだけ、AIに絞り込ませて降りる |

**運用**

- 地図の生成・更新はAIに任せる(「このシステムを人間が保守するために必要な知識だけ抽出して」)。ただし**所有者は人間**。障害対応や大きな変更のたびに、判明した仕様・依存・注意点をAIに地図へ書き戻させる(§4の「裁定の結果は仕様に書き戻す」と同じ循環)。実装は [skills/update-map](skills/update-map/SKILL.md)(初版生成・更新・コードとの矛盾検査)。
- **地図の維持を善意に頼らせない。** レイヤー表の「常に頭にある」「いつでも参照できる」を本人任せにせず、フローの強制点に接続する:契約・不変条件・危険領域を変えるPRに地図の更新が含まれているかをAI一次レビュー(§7)が検査し、ズレは月イチの矛盾検査で拾う。**地図の更新は当該PRに同梱する**——マージ後に回すと、マージ時点で「地図で追跡できる状態」を確認できず、§3のゲートが空文になる。障害対応で判明した事実の書き戻しだけが、事後の更新になる。
- **大量のドキュメント生成は解決策ではない。** 人間が将来判断するために必要な情報**だけ**を維持する。100ページの自動生成ドキュメントは、読まれない時点で理解負債の別の形にすぎない。
- 原則:**コード量が増えても、人間が理解すべき情報量が増えない構造にする。**

### 訓練:使わない理解は腐る

かつて理解は、実装の摩擦から自然に生まれていた。実装もレビューもAIに移した後で、地図を「読むだけ」の理解は紙の上の理解であり、障害の夜には使えない。**地図には運動を付ける。**

実証的な根拠がある。AI補助で新しいライブラリを学習した開発者は、手書きで学習した群よりテストの得点が17ポイント低く(50% 対 67%、p=0.01)、**低下が最も大きかったのはデバッグ問題**だった。しかも時間短縮の効果は有意ではなかった([AnthropicのRCT, arXiv:2601.20245](https://arxiv.org/abs/2601.20245)、査読前)。また人は自分が書いたコードでも、2ヶ月後には識別子をほとんど思い出せない([Fekete & Porkoláb, 2023](https://www.cs.ubbcluj.ro/~studia-i/journal/journal/article/view/89)、小規模実験)。**書かない時代の理解は、意図的な運動なしには作られも保たれもしない。**

- **障害シミュレーション** — 「決済が落ちた。どの箱か3分で特定せよ」を定期的にやる。マージゲート(§3)の実技試験を兼ねる。
- **裁定の義務** — 独立照合(§4)の食い違いは人間が裁く。これが契約理解の維持運動になる。
- **地図への尋問** — AIの説明を読むだけで済ませず、「ここを変更すると何が壊れる?」と質問で掘る。

## 12. 導入の順序(推奨)

1. **ベースラインを測り始める。** レビューの実時間とリードタイムを記録する(集計はエージェントに任せる)。改善はまだしない。**数字がなければ、この後に何をやっても効果を示せない。** ただしタイムスタンプから取れるのは**滞留時間**であって注意を向けた時間ではない。しかも滞留の短さは品質と逆相関しうる(即ラバースタンプが一番短い)。**滞留時間・自己申告・レビュー品質(障害率)を混同せず、別の系列として持つ。**
2. **地図の初版を作る。** [skills/update-map](skills/update-map/SKILL.md) の初版生成で下書きし、**人間が通読・修正して検収する**(無修正の受け入れは読まれていない疑いがある)。領域ごとの検収者はドメインオーナー(§8)。**この工程を飛ばすと、次の手順でマージゲートが成立しない**——§3のマージ基準は「地図を見てどの箱が壊れたか特定できる状態」を要求しており、地図が無いチームは最初のマージから規範に違反する。
3. **基準を合意して運用を始める。** 3段階・2ゲート・隔離ルールをチームで合意し、レビューガイド+設計メモ+AI一次レビューを始める。**マージゲート(§3)は地図の初版検収をもって発効する**(手順2が終わるまでは、地図に関する確認義務は保留と明示する。なし崩しの棚上げと、明示した保留を区別する)。あわせて**月イチの矛盾検査の担当と実行手段**(カレンダーかCIジョブ)を決める——担当を決めない定期検査は発火しない。
4. **計上ルールを決める。** 実測値に基づいて、レビュー時間を見積りにどう入れるか(ポイントに含める/容量の一定割合を先取りする)と、PR分割の目安を決める。**この基準自身の運用コスト(設計メモ・裁定・訓練・地図維持の時間)も計上対象に含める**——「計上する」を掲げる基準が自分を計上しないと、レビュー時間の減少だけが観測されて、総コストが増えていても反証できなくなる。
5. **差分を確認して改訂する。** ベースラインとの差分(レビュー時間・リードタイム・障害件数・切り分け成功率・分類比率)を見て改訂する。**守られていない項目は、厳格化ではなく削除を検討する。**

### 削除の手続き

「重すぎて守れない」と「守りたくないから守らない」を区別するため、削除には次を要する。

- (a) 守られない原因の確認(コストなのか、動機なのか)
- (b) チームの明示的な合意(なし崩しの死文化は削除ではない)
- (c) 再追加する条件の記録(例:分類比率が契約側に流れたら、障害で露呈したら)

また削除できるのは運用項目だけである。**§6のオンコール原則・§2の行レベル対象・§8の責任割当は、削除ではなく改訂のみ。** これらは基準が存在する理由そのものであり、これらが「守られない」状態は、削除ではなく§6を読み直すことで対処する。

---

*この基準自体も改訂の対象である。守られない基準は無いより悪い——重すぎる項目は運用に合わせて削る(手続きは§12)。*
