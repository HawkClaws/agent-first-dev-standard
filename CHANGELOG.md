# Changelog

## §参照をリンクにした + 地図の発効条件と所有者を確定(版番号なし・2026-08-23)

### §参照をすべてリンクにした

`§3` `§8` のような参照が112箇所あり、**どの節を指しているのか本文を探さないと分からなかった**。すべてクリック可能にした。

- standard.md の各節見出しの直前に `<a id="sN"></a>` を挿入した。GitHub の自動アンカーは日本語の句読点の扱いが実装依存なので、**明示アンカーにして見出しの改称でリンクが切れないようにした**(v0.7 で節タイトルを7件改称した実績があるため)
- standard.md 内は同一ページ内リンク、skills/ と README からは `standard.md#sN` への相対リンク

### 採らなかった提案とその理由(記録)

**提案**: 導入時の地図の一括生成を **Level 0 だけ**に縮め、Level 1〜3 は「その領域に触る最初のPR」で起こして育てる。

**動機**: 大規模で一括生成が重い。「全体の地図ができるまで全PRがゲート未発効」を避けたい。

**採らなかった。** 独立検証(サブエージェント。同一ベンダー・指示ベースの隔離)で、根拠3つのうち2つが事実に反し、1つは解決済みだと分かった。

- ❌ **「更新モードが既にPRごとに育てるので、一括生成は重複」** — 更新モードが拾うのは導入後に**変わった**事実だけである。領域全体を調査して起こす手順は初版生成モードにしかない。変わらないまま残る既存コード(§1が負債の典型に挙げる「誰も意味を説明できないDBのデータ変換ロジック100行」)は更新モードでは永遠に地図に載らない。**重複ではなく相補だった**
- ❌ **「一括生成の中身はAI推測で信頼度が最低」** — Level 別に見ると逆。Level 3(不可逆系の位置)はスキーマ変更・課金呼び出し・外部送信の機械的検出でほぼ確実に拾えるので、**地図の中で最も生成信頼度が高い層**である。推測が混ざるのは Level 1(契約)
- ❌ **「大規模でゲート未発効になる問題が消える」** — 領域単位化(前エントリ)で既に解決済みだった。**解決済みの問題を解こうとしていた**
- ⚠ **提案が依拠した非対称が成立しない** — 「障害時に必要なものは網羅が要るから Level 0 は前払い」という論法は、Level 2(不変条件)と Level 3(危険領域の位置)にも同じく当てはまる。障害の夜に要るのは「触った不可逆系」ではなく「全部の不可逆系がどこにあり、どう戻すか」である。**提案は地図が最も要る場所(長く触られていない領域)から順に地図を欠落させる分布を作る**
- ⚠ **ゲート述語が循環する** — 「そのPRが触る範囲の Level 1〜3 があるか」を判定するには、触っている範囲を知る必要があり、その判定に使う道具が地図である。**ゲートが最も必要な場所でちょうど判定不能になる**

**検証が掘り当てた実在の穴3件は、提案とは独立に塞いだ**(下記)。

### 塞いだ穴3件

1. **マージゲートの発効条件を確定した(発効条件の明示)。** `skills/update-map` が「マージゲートも領域ごとに発効する(基準§12 手順2)」と書いていたが、**§12 にその規定は無かった**——§12 の「領域ごと」は検収者の文脈だけで、発効は「手順2が終わるまで」= 全領域の完了待ちだった。**スキルが規範に無いことを規範の名で語っていた。** 領域ごと発効を §12 手順3 に明記し、スキルの参照先を手順3に直した
2. **全体 Level 0 の所有者を明示した(実行主体の明示)。** §8 の地図の維持責任は「**自領域**の地図(§11 **Level 1〜3**)」で、領域が2つ以上あるときの全体 Level 0(`docs/map/README.md`)は**誰の自領域でもなく、Level の範囲からも字義上外れていた**。境界をまたぐ契約は「**呼ばれる側の領域のドメインオーナー**が持つ」と定めた——契約の当事者は呼ぶ側と呼ばれる側で、決められた出力を返す義務を負うのは呼ばれる側だから(§2 の契約の定義に従う)。責任範囲も Level 0〜3 に直した
3. **マージゲートの地図要件を一本化した(正本の一本化)。** §3 は「地図(§11)を見てどの箱が壊れたか特定できる状態」、§11 の表は Level 1〜3 を「いつでも参照できる」と定めており、**どちらがゲート要件の正本か曖昧だった**(上の提案はこの曖昧さの産物である)。§3 に「要件は**触る領域の地図が Level 0〜3 まで検収済みであること**」と明記した

### あわせて2件

- **領域に属さないコード(共通基盤・共有ライブラリ・認証ミドルウェア)も1つの領域として扱い、ドメインオーナーを決める**ことを `skills/update-map` に明記。担当が決まらないコードは領域ファイルを持たないので、first-review の検査項目6が恒久的に「未検査(地図なし)」を吐き続ける——**領域の切り方の漏れが、沈黙ではなく反復として出る**
- flow のテンプレートの「リポジトリに一度だけ」を「**領域ごとに一度だけ**」に修正(領域単位化の追随漏れ)

## 地図を領域単位にした(版番号なし・2026-08-23)

**矛盾の修正。** §8:219 は「**自領域の**地図(Level 1〜3)の初版検収」、§12:287 は「**領域ごとの**検収者はドメインオーナー」と、規範側は**領域単位**を前提にしていた。一方 `skills/update-map` は「置き場所: `docs/map.md`」「分量: **全体で**200行以内」と**単一ファイル・プロジェクト全体**を指定していた。別の単位を前提にした記述が両方あった。

きっかけは「巨大なプロジェクトでも200行でまとまるのか」という質問。まとまらない——Level 1 が「主要コンポーネントごとに1〜3行」なので、コンポーネントが100個あればそれだけで上限を超える。規範側に揃えた。

- **置き場所を `docs/map/<領域>.md` に変更**(領域=ドメインオーナーの担当単位)。領域が1つのプロジェクトでは `docs/map/` にファイル1枚でよい
- **分量は1領域あたり200行以内。** プロジェクト全体で200行ではない
- **上限を設計の計器として使う**ことを明記: 「ある領域の地図が上限に収まらないなら、それは**領域の切り方が大きすぎるというシグナル**である(1人が担当しきれない範囲になっている)」
- 領域が2つ以上あるなら、全体の Level 0 には**領域の境界と、境界をまたぐ契約だけ**を置く。領域の内部は各領域の地図へ
- **初版生成を領域単位にした**: 「まず領域を決める。**全領域を一度に作らない**」。領域を1つずつ生成・検収でき、マージゲートも領域ごとに発効する。大規模で「全体の地図ができるまで全PRがゲート未発効」になるのを避けるため
- 検収者を「その領域のドメインオーナー」に明示
- 参照を6箇所で差し替え(first-review / pr-guide / design-memo / PRテンプレート / flow テンプレート / flow-comparison.html)

## ライセンスを CC BY 4.0 に確定(2026-08-23)

README の「TODO: 公開時に決定」を解消した。リポジトリは既に公開されているので、TODO自体が古かった。

- `LICENSE` を新設(CC BY 4.0、SPDX 識別子つき)。著作権者は `HawkClaws`——Contributing が「特定の組織が識別できる情報は書かない」と定めているため、公開リポジトリの同一性で表記する
- README の License 節に**帰属の書き方の例**を追加(社内ドキュメントへの取り込み / スキルを改変した場合 / 記事での引用)。「CC BY 4.0 です」だけでは、社内で使う人が判断できない
- **実務上の注意を明記**: 社内の非公開ドキュメントに取り込んで自チームで使うだけなら再配布に当たらないので帰属義務は発生しない。それでも出典と版を1行書くことを勧める——義務ではなく、**改訂に追従するかを後から判断できる**ようにするため。スキルは `cp -r` で他リポジトリに入る前提なので、この一文が無いと導入時に必ず止まる

## スキル追加: /flow(版番号なし・2026-08-23)

**6本目のスキル `flow` を追加した。規範はゼロ個追加していない。** 他の5本を基準が定めた順序で呼び、状態を1つのファイルに記録する進行係である。

**動機**: スキルを1本ずつ手で呼ぶ運用は、忙しい日に最初に消える。`/flow <チケットID>` は機械でできる工程(下書き・機械検査・突き合わせ・記録)を連続実行し、**人間の判断が必要な項目で止まる**。人間が空欄を埋めてもう一度呼べば続きが走る。1チケットあたりの呼び出し回数が15回以上から**最大8回**(人間のゲートの数)になる。

- 状態は `docs/flow/<チケットID>.md` のチェックリストに持つ。**この `👤` 付き項目の一覧が、そのチケットにおける Human-in-the-Loop の定義そのもの**である(§7・skills/README の「空欄」の議論を、チケット単位で実体化したもの)
- **絶対にやらないこと**を明記した: `👤` にチェックを入れない・埋めない / 不可逆系のゲートで「無返事なら進む」をしない / 設計メモの合意より前に実装を走らせない / **スキップの判断も人間のものなので勝手に `[~]` にしない** / マージ・プッシュ・デプロイをしない
- **スキップは削除せず `[~]` + 理由**を表に残す。理由は「重すぎた/不要だった/やる気がなかった」に寄せて書く——これが §12 の削除手続き (a)「守られない原因の確認(コストなのか動機なのか)」の材料になる。**この表が溜まらないと、重すぎる項目を削る根拠が永久に手に入らない**
- 実測表(各工程の所要・人間の待ち時間)も状態ファイルに持つ。§12 手順1 が要求するベースラインの実体
- flow-comparison.html に「進め方」の段を追加

**追加してよいと判断した理由**: 直前のレビュー3本が「規範を足すのをやめろ(純削減になるまで)」と勧告している。`flow` は義務・閾値・責任割当を一つも増やしていない実行層であり、かつ**実測が溜まらないと §12 の削除が永久にできない**ため、これは削るための道具でもある。

**あわせて §4 の欠落を1件修正した(実走で確認済み)**: 「裁定の結果は仕様に書き戻す」しか書いていなかったが、書き戻し先は3つある——仕様/設計メモ、**裁定前に書かれたテスト**、地図 Level 1〜2(契約を変えた場合)。実装と同じセッションが書いたテストは裁定前の読みを固定しているので、仕様だけ直すと**間違った読みが「守るべき回帰」として保護される**(§2 でそのテストの目的を回帰検知と定めているため)。基準を1周実走したときに実際に起きた(83件のテストが裁定前の読みを固定していた)。crosscheck 手順C と flow テンプレートも揃えた。

## スキル改名: gen-tests → crosscheck(版番号なし・2026-08-23)

**`skills/gen-tests` を `skills/crosscheck` に改名した。コマンドは `/gen-tests` → `/crosscheck`。**

**移行**: 既にスキルをコピーしている場合は、`skills/gen-tests` ディレクトリを `skills/crosscheck` に置き換える(SKILL.md の `name:` も `crosscheck` に変わっている)。中身の手順は変わっていない。

理由:

- **既定パスがテストを1本も生成しない。** v0.8 で人間版を既定にした時点で、名前は少数派の経路(自動版)だけを指すようになっていた。この時点では同期コストを理由に据え置いたが、それは判断ミスだった
- **その2の変更で、名前が積極的に誤解を招く状態になった。** 人間版をレビュー段に移した結果、このスキルは**2つの段で、2人の別人に呼ばれる**ようになった——自動版は実装・テスト段で著者が、人間版はレビュー段でレビュアーが呼ぶ。「テストを生成するスキル」という名前では、レビュアーが自分の呼ぶものだと分からない
- 分割案(人間版と自動版を別スキルに)は採らなかった。共通の「仕様の切り出し(汚染遮断)」と「手順C 裁定」が2ファイルに重複し、その1で排除した重複を再導入することになる

あわせて SKILL.md の冒頭に、**このスキルが2つの段で2人に呼ばれること**と、**既定の人間版はテストを生成しないこと**を明記した。description にも実行主体と段を入れた(スキル一覧から選ぶ側に届くのは description なので)。

## 構成改訂 その4(版番号なし・2026-08-23)

**追随漏れの機械検査を回して、残っていた4件を直した。** §参照の宛先(全12節・宛先なしゼロ)、用語の残存、PRテンプレートの欄と参照側の一致、図と本文のフロー一致を検査した。

- **図の用語集が「濃淡」を用語として定義していた。** standard.md の用語表には無い語であり、同じ概念に「理解レベル」「レビュー指定」と合わせて3つの名前があった。図の定義を「理解レベル(=レビュー指定)」に統一し、本文中の使用箇所も揃えた(§3 の「§2の濃淡に従う」→「§2の理解レベルに従う」)
- **図の障害対応段の注記が古かった**(「地図の生成・更新・矛盾検査はスキル化済み」)。初版生成は導入時の工程として分離済みなので、この段は障害で判明した事実の書き戻しだけになった。あわせて**月イチの矛盾検査が図のどこにも出ていなかった**ので、この段の注記に入れた(担当と実行手段をカレンダーかCIで決めることも)
- **update-map の汚染経路の記述が自動版限定だった。** CLAUDE.md にアーキテクチャ記述を置くと汚染されるのは、自動版のサブエージェントだけでなく**人間版で人間が読む材料も同じ**である。「独立テスト生成の汚染経路」→「独立照合の汚染経路」に直した
- skills/README のフロー図の段名を図と揃えた(「実装」→「実装・テスト」)

## 構成改訂 その3(版番号なし・2026-08-23)

**flow-comparison.html を本文のフローに追随させた。** その2で standard.md と skills/ は直したが、図が古い状態のまま残っていた。

- **「導入時(一度だけ)」の工程を新設した。** 地図の初版生成・検収が、図では**マージ段の注記1行**にしか存在せず、`/update-map` のチップは障害対応段にしかなかった。つまり図として読むと依然「地図は障害対応のときに触るもの」だった。v0.9 で standard.md §12 に手順を足したのに、入口文書である図には反映していなかった
- **着手段に契約記述の前倒し承認を反映**(その2の変更)。`/update-map` が図に4箇所現れるようになり、スキルのモード数と対応した
- **テスト段を自動版だけにし、人間版をレビュー段に移した**(その2の変更)。図はまだ「期待値の書き出し」をテスト段(=著者のセッション)に置いていて、その2で直した「書き手は実装方針を書いていない人」と矛盾していた
- マージ段に、独立照合の実施確認(不可逆な副作用が「なし」以外なら「未実施」でないこと)を追記
- 「濃淡」の残りを「レビュー指定」に統一

**反省として記録**: その1・その2で本文とスキルだけ直して図を後回しにした。この図は README の入口に置かれた導入判断用の文書なので、**本文より先に読まれる**。追随漏れは本文の矛盾より読者に届く。

## 構成改訂 その2(版番号なし・2026-08-23)

**独立照合(§4)の周辺に残っていた「守られなくなる構造」を4件閉じた。** 規範の追加・削除はゼロ。使ったのは「正本の一本化」「順序の変更」「実行主体の明示」だけである。v0.9 で地図について塞いだ穴(前提物なき義務・順序の矛盾)と同型のものが、§4 の側に残っていた。

**1. 規範とスキルが正反対だった(正本の一本化)**

- §2 は「行レベル対象に当たるかと、ドメイン中核かの判定はドメインオーナーの確認事項とし、**著者の自己申告では確定させない**」と定めているのに、`skills/pr-guide` は「**最終決定は著者**」と書いていた。日常的に実行されるのはスキルなので、ドメインオーナー確認は誰のフローにも現れていなかった
- §2 自身が「低いレベルに倒すほど著者が得をする構造」と特定している通り、これは注意力ではなくインセンティブの問題で、唯一の歯止めがスキル文言で外れていた
- pr-guide と design-memo を規範に揃えた

**2. 独立照合の材料を作る工程が無かった(順序の変更)**

- §4 は照合の材料を「チケット、承認済み設計メモの**要求・契約の記述部分**」に限っているが、`skills/design-memo` の様式にその欄は無く、あるのは方針・触る範囲(=独立照合が汚染源として遮断する側)だけだった。チケットが薄いと、**実装前に書かれた契約の記述がどこにも存在しないまま、不可逆系の必須義務が発効する**
- 契約・不変条件・危険領域を変えるチケットでは、**地図 Level 1 の契約記述を設計メモと一緒に承認する**ことにした。地図の更新はもともとPRに同梱する既存の工程なので、**前倒しするのは下書きの時期だけ**である
- gen-tests の「使うもの」にこれを明示し、材料が空のときは「照合は成立しない。設計メモ側に戻す」と定めた(全欄「仕様に記述なし」の照合は実施ではない)

**3. 人間版の書き手が、フロー上で著者に落ちていた(実行主体の明示+順序の変更)**

- §4 は人間版を「レビューに入る前に」と書いていたが、`skills/README` のフロー図は `/gen-tests` を**実装段=著者のセッション**に置いていた。フロー通りに回すと書き手が著者になり、**正規フローを忠実に実行するほど独立照合が「実施記録付きの自己照合」に縮退する**
- 人間版の書き手を「**実装方針を書いていない人**(レビュアー。不可逆系ならドメインオーナー)」と明示した。著者は設計メモで実装の設計図を書いた当事者なので、実装を経由していない解釈を作れない
- フロー図で人間版をレビュー段の冒頭に移した(自動版はツール制限で独立性が担保されるので実装段のまま)
- これは基準を1周実走したときに実際にぶつかった摩擦でもある(著者である自分が人間版を実行できず、別セッションに渡した)

**4. 最重量の義務に、実施を確認するゲートが無かった(実行主体の明示)**

- 「不可逆系の独立照合は必須」は、§3 のマージ基準にも first-review の検査項目にも §12 の測定リストにも現れていなかった。しかも PRテンプレートの「未実施」は非不可逆PRでは正しい正常値なので、大多数のPRで並んで正規化され、**不可逆PRの「未実施」が異常として浮かばない**
- 摩擦が最大(人間の書き出し待ちでブロックする唯一の工程)で検査ゼロという組み合わせだった
- §8 の**マージ確認責任の対象に、既存の欄同士の突き合わせを追加**した——「不可逆な副作用」が「なし」以外なら、テスト欄の独立照合が「未実施」でないこと。新しい検査項目ではなく、既にテンプレートにある2欄の整合確認を既存の責任に割り当てただけである
- first-review の BLOCKER 枠には足さなかった。単独枠の設計意図(最重要の警告を1つだけ置く)を薄めるため

**未対応(記録)**: 同レビューは他に5件を挙げている——裁定結果の書き戻し先が3箇所あって正本が未指定/不可逆系設計メモの承認者が「チーム」=誰でもない/「採らなかった選択肢」の二重記入/同一判定がPRで3〜4回走る警報疲れ/導入先の実PRテンプレートとの三重化。**これらは「回したらこうなる」の予測であって、実運用で確認したものではない**ため、§12 の削除手続きと同じ考えで実測を待つ。

## 構成改訂(版番号なし・2026-08-23)

**版番号を進めない。** 規範の追加はゼロ。実運用の実測が一つも無いまま42時間で9版まで進んだこと自体が、§10で警告したグッドハートの法則(指標を目標にすると腐る)の自己適用漏れだったため。§12手順1「数字がなければ、この後に何をやっても効果を示せない」に従い、実測が出るまで版は据え置く。

**内容は「全体の見直し」で、骨格は変えていない。** 15節構成・節番号・すべての規範をそのまま維持し、重複を正本1箇所に寄せ、矛盾を潰した。

**矛盾の修正:**

- `flow-comparison.html` の「裁定」の定義が**自動版限定に狭まっていた**(「実装を見せずに別セッションが書いたテストが落ちたとき」)。v0.8 で人間版を既定にしたのに、図では既定側の裁定が定義から漏れていた。用語表の文言に一致させた
- 同図の「濃淡」を「レビュー指定」に統一。同一概念に4つの呼称(レベル/レビュー指定/理解レベルの提案/濃淡)があった

**重複の解消(規範は削っていない。正本1箇所+参照にした):**

- §4 の「自動版に上げる条件」3項が skills/gen-tests 手順0 と逐語だった → gen-tests を正本に
- §4 の汚染ルールの詳細・幅アサーション対策の詳細 → gen-tests の該当節を正本に
- §9 の「なぜツールでは守れないか」の説明が skills/README・pr-guide と三重 → skills/README「空欄がHuman-in-the-Loopの定義」を正本に。§9には規範(実効性は口頭確認に置く)だけを残した
- §11 の「§3のゲートが空文になる」理由句 → §3への参照に(同梱の規範は維持)
- §2 の「テストの最低品質を機械検査する」→ §10 を正本に
- §5 の §4 結論の再説明 → 参照に
- skills/update-map の警報疲れ理由句 → first-review を正本に
- §11 冒頭の理解負債の定義が用語表と二重 → 用語表を正本に(実例と出典は本文に残した)

**§12型の削除を初めて実行した(1件):**

- **PRテンプレートの二重管理をやめた。** `templates/pr-template.md` を削除し、`skills/pr-guide/assets/pr-template.md` を唯一の正本にした。「編集時は両方を同期すること」という**手動同期の義務そのものを削除した**——(a)守られない原因=コスト(v0.8 で実際に同期漏れが起き、別コミットで追い直している)、(b)記録はこのエントリ、(c)再追加条件=リポジトリ直下にテンプレート置き場が必要になる要望が出たとき

**表記の撤回:**

- v0.9 のエントリから「**独立**監査」の語を撤回した。全コミットが単一著者・PRなし・人間レビュアーなしで、検証は著者が起動した同一ベンダーのサブエージェントである。skills/gen-tests 自身が「ツール制限なし・ログ確認なしの場合は『独立性は指示ベース』と明記する」を課しているので、その表記に従う

**分かったこと:** 重複排除では量は減らない(16,621字 → 16,541字、0.5%)。2日で5.4倍になった原因は重複ではなく内容の蓄積であり、量を減らすには規範か論拠のどちらかを削るしかない。

## v0.9 (2026-08-23)

**地図(§11)のフローに空いていた穴を塞いだ。** 規範の追加を含む(§8 に責任の行が1つ増え、§12 に手順が1つ増えた)。

きっかけは「地図って障害時に作るの? なんで障害時だけでいいの?」という質問。調べたら**その読みが正しかった**——地図は §3 マージゲートの実体・§8 一次対応の前提・§11 の中心概念として最も多く参照される成果物なのに、**その出生(初版生成)だけがどの文書のフローにも存在しなかった**。v0.6 で update-map を新設したとき「書き戻す手段が無い」穴を塞ぎ、「最初に作る工程が無い」穴が残っていた。サブエージェントによるレビュー(著者が起動。同一ベンダーのモデルで、隔離は指示ベース)で7件を検出し、全件を修正した。**これは独立した検証ではない**——skills/gen-tests 自身が「ツール制限なし・ログ確認なしの場合は『独立性は指示ベース』と明記する」を課しているため、その表記に従う。

- **§12 に手順2「地図の初版を作る」を新設**(以降の手順を繰り下げ)。初版生成 → 人間の通読・修正による検収まで。検収者は領域ごとのドメインオーナー
- **§12 手順3にマージゲートの発効条件を明記**: 「マージゲート(§3)は地図の初版検収をもって発効する」。地図が無いまま2ゲートの運用を始めると、**基準に従うほど規範違反になる**状態(最初のマージから §3 違反)だった。**なし崩しの棚上げと、明示した保留を区別する**ことを条件にした
- **§12 手順3に月イチ矛盾検査の担当決めを追加**。担当を決めない定期検査は発火しない
- **§8 の責任表に「地図の維持責任」を新設**(ドメインオーナー)。マージ確認責任が「地図が§3の基準を満たしていることの確認」を要求する一方、**地図を維持する責任者がどの行にもいなかった**。全員が確認して誰も所有しない状態=§11 が排除したはずの「善意に頼る」の再発。月イチ検査の実施はオンコール輪番やCIに置いてよいが、**齟齬の判定は委譲できない**と明記
- **地図更新のタイミングの矛盾を解消**: 規範(§11)は「変えるPRに地図の更新が含まれているか」= マージ前を要求していたのに、`skills/README.md` のフロー図と `update-map` のトリガー文言は「変更の後」= マージ後だった。**規範側に統一**し、「地図の更新は当該PRに同梱する。マージ後に回すとマージ時点で追跡可能性を確認できず §3 が空文になる」を §11・update-map・skills/README・flow-comparison.html に明記。事後更新になるのは**障害対応で判明した事実の書き戻しだけ**
  - フロー図通り(マージ後更新)に運用すると、契約を変えるPRが**毎回** first-review 項目6の指摘を受け、常態化して無視される——このリポジトリ自身が BLOCKER 枠で防ごうとしている警報疲れと同型だった
- **skills/README のフロー図に3モードを配置**: 導入時(初版生成)/ PR作成段(更新・同梱)/ マージ後(障害対応の書き戻し・月イチ矛盾検査)。update-map は3モード持つのに、フロー図の登場は1箇所だけだった
- **skills/update-map**: description に導入時の初版生成を追加。モード1・2の見出しにトリガーを明記
- **skills/first-review**: 「地図なし」を報告するとき、初版生成から始められる旨を添えるようにした。**「地図なし」を正常系として黙って通すと、地図が無い状態から抜けるきっかけがフローのどこにも無くなる**
- **PRテンプレート**(`templates/` と `skills/pr-guide/assets/` の2箇所): 「地図(docs/map.md)」欄を新設(更新不要 / このPRに含む)。マージ確認責任と first-review 項目6の両方が地図更新の有無を問うのに、**著者が申告する欄が無かった**(不可逆な副作用にはチェックボックスがあるのと非対称)
- **skills/pr-guide**: 機械記入欄に地図の判定・提案を追加
- **flow-comparison.html**: PR作成段に `/update-map` と同梱の注記、マージ段に「初版は導入時に検収済み」の前提を追記。版表記を v0.9 に

## v0.8 (2026-08-23)

**§4 の規範を「別セッションでテストを生成する」から「実装を経由していない解釈と突き合わせる」に一段抽象化した。** 規範内容の変更を含む(必須項目の実装手段が2択になった)。

きっかけは「普通に実装を依頼すればテストまで作られるし、そこはレビューで担保する部分ではないか」という指摘。検討の結果、**コスト論は妥当だが「レビューで代替できる」は成立しない**と判断した。レビュアーは実装を読んでからテストを読むため、読み方が「テストは実装と合っているか」に固定され、仕様の別の読み方は原理的に出てこない(判断のアンカーが実装側にある)。必要なのはテストの本数ではなく**実装を経由していない2つ目の解釈**であり、それは人間が作ってもよい。§7 の外部証拠(AIレビュー提案の採用率16.6%、判断は速くなるが品質は上がらない)も「レビューで担保」の側に不利である。

- **§4**: 規範を一行に置き直した——「不可逆系の契約は、実装を経由していない解釈と突き合わせてから通す」。これを**独立照合**と呼び、実装手段を2つ示した
  - **人間版(新設・既定)**: レビュー前に実装前仕様とインターフェースだけを読み、期待する入出力と失敗時挙動を**自分で書き出してから**テストと実装を読む。契約1本あたり数分
  - **自動版(旧・独立テスト生成)**: 実装非開示のツール制限セッションにテストを生成させる。**自動版に上げる条件を明記**(契約数が多い / 仕様が長く期待値を書き出せない / 過去に裁定が発生した領域)
  - 適用範囲の文は維持(不可逆系は必須・構造理解以上は推奨・契約理解は過剰装備)。ただし**「必須なのは独立照合であって、自動版ではない」**と明記。これは v0.7 までの「不可逆系には自動版が必須」の実質的な緩和である
  - 汚染ルール(実装前仕様に限る・設計メモの方針欄は使わない)を両版共通の節に集約
  - 「それでも人間に残る仕事」に**人間版固有の弱点**を追加:実施したかを機械検査できない(自動版はツール使用ログで確認できる)。守らせる力が仕組みではなく規約にあることを明記
- **用語表**: 「独立照合」を新設。「独立テスト生成」はその自動版と定義し直した。「裁定」の定義を独立照合基準に合わせた
- **§2 / §10 / §11**: 「独立テスト生成」への参照を「独立照合」に変更(§2:95 正しさの検証、§10:251 ミューテーションテストとの併用、§11:281 裁定の義務)
- **skills/gen-tests**: 手順を再構成した。手順0(版の選択)→ 共通(仕様の切り出し)→ 手順A(人間版)/ 手順B(自動版)→ 手順C(裁定)。人間版では**仕様面だけを提示して期待値メモを空欄で返す**(実装・テスト・挙動説明を出力に含めない)のがスキルの仕事になる
- **PRテンプレート**(`templates/` と `skills/pr-guide/assets/` の2箇所): テスト欄を「独立照合(§4): 未実施 / 人間版 / 自動版 ・ 食い違い ___ 件 ・ 裁定」に変更。**人間版で通した場合が「未実施」に見えない**ようにした
- **skills/README・skills/pr-guide・flow-comparison.html**: 語と既定版の記述を追随。flow図のテスト段に人間の空欄「期待値の書き出し」を追加
- **skills/first-review**: 「このスキルは独立照合ではない」を明記。検査項目2(テストと実装の整合)は**実装を読んだうえで**テストを見ているのでアンカーが実装側にあり、別セッションで実行してもこの性質は変わらない。不可逆系で first-review を回したことを独立照合の実施と取り違えないため

**改訂後にデモ実行して判明した追記(同日):** 不可逆系(課金の日割り返金)を題材に、実装 / 人間版 / 自動版を別セッションで走らせた。テストの赤は0件(3セッションが4つの曖昧点で同じ読みをした)だったが、人間版の「決められなかった点」から**仕様の内部矛盾**(税抜の月額を日割りした額を、税込で請求済みの refund API へ渡す=約10%の過少返金)が出た。テストでは原理的に出ない種類の欠陥である。同時に、**自動版が曖昧な箇所に幅を持たせたアサーション**(`expected - 1日分 <= result <= expected + 1`)を書き、曖昧さをテストの中に吸収して検出力を失っていた。これを §4「それでも人間に残る仕事」と skills/gen-tests に反映した(自動版には「幅を持たせるな、決められないならリストに出せ」を明示指示する)。

## v0.7 (2026-08-22)

standard.md の**表現のみ**の改訂。規範内容(義務・閾値・責任割当・出典)の変更はなし。基準本文は人間が読む文書であり、v0.6 までの圧縮された書き方が理解の障壁になっていたため。全 URL・全数値は旧版とバイト単位で一致(サブエージェント2本で機械照合済み)。

- 漢語の名詞化を動詞に開いた(「理解コストの請求だけが残り」→「理解の分の請求書は消えていない」等)。1文1論点に分割
- 造語・難語に説明を付けた:**用語表を冒頭に新設**(理解負債/契約/不可逆系/地図/独立テスト生成/裁定)。「同語反復」→「自作自演」、「利益相反」→「自分の解釈を自分で正解にできてしまう」、グッドハートの法則に一行説明
- 冒頭に **「この基準を3行で」** を追加(README の思想3行を踏まえた3点。3行目は§12・末尾イタリックの「先払い・減らす・計上」側を採った)
- 長い箇条書きを見出し付きの塊に分割(§4「自作自演を断つ」/「それでも人間に残る仕事」、§7 の測定項目をリスト化、§12 の削除手続きを小見出しに)
- 表の見出し語とセルを平易化(§2 見出し「要求される理解」→「何ができれば『理解した』か」、§3 セル「説明可能性/トレース可能性」→「説明できるか/追跡できるか」、行ラベル「PR提出/マージ」→「PRを出すとき/マージするとき」)
- §2 のレベル判定を、混在した1段落から**番号付きの3ステップ**に整理
- **§3 の参照ミスを修正**(「トレース責任は地図レベル(§10)」→ §11。§10 はカバレッジゲート)
- 「トレース可能性」を「追跡できるか」に、「重心移動」の説明を平易化。§1 の三つの問いに「①と②が最も混同されやすい」の一文を追加
- **節タイトルを7件改称**(§1「三つの判断を分離する」→「三つの問いを混ぜない」、§2「AI生成コードの理解基準」→「どこまで理解するか」、§5「ライブラリ扱いの条件」→「『ライブラリ扱い』していい条件」等)。旧タイトルを参照する他ファイルは無く、参照切れなし
- 「権限移譲の原則」「訓練:使わない理解は腐る」を太字段落から `###` 見出しに昇格(`skills/README.md` の引用が実見出しと一致するようになった)
- **「裁定」は用語表に残した。** 一度「判定」に統合したが、§2 のレベル判定と二義になり、かつ PR テンプレートの欄名(`templates/pr-template.md`)ほか計14箇所が語の裏付けを失うため撤回。用語表で「レベル分類の『判定』とは別物」と明示する形にした
- 他ファイルの語を standard.md に追随:`flow-comparison.html`(同語反復→自作自演 2件、トレース→追跡 1件)、`skills/first-review`・`skills/gen-tests`(同語反復→自作自演)

**flow-comparison.html の可読性改善:**

- **冒頭の図を作り直した。** 等サイズの箱を並べた SVG では、見出し(「理解コストの引っ越し」)の主題である**コストの量**が見えていなかった。棒の高さ=そこで人間が払う理解コストとし、従来(レビューとマージに集中)と導入後(着手・PR・レビュー・マージに分散)を同じ時間軸で並べる形に変更。**総量は減らない**ことを図中に明記
- 実装を SVG から HTML+CSS に変更。860幅・11px の固定 viewBox がスマホで縮小されて判読不能だった問題を解消(文字が実テキストになり、狭い幅では図だけが横スクロール)
- **「この図で使う言葉(5つ)」を折りたたみで追加**(契約/濃淡/不可逆系/地図/裁定)。README でこのページを入口に昇格させたため、基準本文を読む前に単体で読めるようにした
- 走り書き状態だった凡例(痛点/skill/人間/変更なし)を、説明付きの凡例リストに分離
- **図を再度作り直した(1回目の失敗)。** 段階ごとに棒を立てる形にしたところ、従来2本・導入後4本になり「導入後の方がコストが増えている」と読めてしまった(実際に長さも 118 対 122 で導入後が長かった)。配分を語る図が量の比較に見えるのは失敗なので、**合計を両方100に固定し、色で「見積りに入っている作業/見積り外の不意打ち」を見せる形**に変更。従来=計上6・見積り外94、導入後=計上92・見積り外8
- 図に **「合計を同じ長さにしてあるのは、減ると主張できる実測がないから」** と明記し、短期では +20分前後で増えることを図中から「正直な収支」へ繋げた(§12 の反証可能性の要求に合わせた)
- **§2 の「契約」に実例を置いた。** 用語表と抽象的な説明だけでは初見の壁が残っていたため、`createUser(email, plan)` の成功/失敗/前提の3行を先に見せ、「中身ではなくこの3行を理解する」に繋げる形に。語は残した(§4「条文の妥当性」の比喩と、skills/テンプレート14箇所の語彙を壊さないため)

## v0.6 (2026-08-22)

敵対的検証([reviews/adversarial-review-v0.5.md](reviews/adversarial-review-v0.5.md)、4方向・重大6件+中位9件+引用3件)への対応。

**中核メカニズムの修正:**

- §2: 契約レベルの「テストで固定」の目的を**回帰検知**と明記(正しさの保証ではない——§4の同語反復テーゼとの矛盾を解消)。アサーション有無の機械検査を全レベルに適用(§10にも追記)
- §2: レビュー指定の自己申告に**下方分類のインセンティブ**があることを明記し、行レベル該当・ドメイン中核判定をドメインオーナーの確認事項に。分類比率をKPI化
- §2: 判定軸を一本化(影響範囲×可逆性が下限、変更頻度・ドメイン重要性は引き上げのみ、複数該当は最高レベル)。§1の別軸リストを削除
- §4/gen-tests: 隔離の汚染経路を明示(実装後生成の仕様・設計メモの方針欄・CLAUDE.md)。仕様は**実装前に書かれたもの**に限定。プロンプト指示は防壁でないため**ツール制限付きサブエージェント定義**(例を同梱)を要求。ツール制限なしの場合は「独立性は指示ベース」とPRに明記。裁定者の利益相反(仕様の持ち主=著者)に対処
- §5: 隔離してもドメイン中核の理解義務は下がらない(§2優先)ことを明記。再生成後の**出力差分確認**(Hyrumの法則)を追加
- §8: 一次対応責任を「マージした人」から**オンコール(ロール)**に変更。マージ者の責任は「マージ時点の確認」に限定(§3も同期)。AI利用の**開示欄をPRテンプレートに新設**(レビュー指定は開示を兼ねない)
- §7/§12: KPIに分類比率・AI警告却下PRの障害率・障害対応時間/切り分け成功率を追加。**権限移譲の判断に速度系指標単独を認めない**。基準自身の運用コストを計上対象に。滞留時間≠注意時間の注記。**削除の手続き**(原因確認・明示的合意・再追加条件)と削除不能な憲法級項目を明定

**スキルの修正:**

- pr-guide: テンプレートを `assets/` に同梱(コピー後のリンク切れ解消)。設計メモの下書きを「採らなかった選択肢」欄へ転記させない(洗浄経路の遮断)。「埋めない」が強制でなく規約であることを明記
- first-review: 出力を**BLOCKER(不可逆系誤分類・単独枠)/機械的指摘/所見**の3ブロックに分離(警報疲れ対策・「機械検査」の看板と実態の乖離解消)。**実装とは別セッションで実行**を必須化。「指摘なし」と「未検査」の区別
- design-memo: 応答期限欄を追加(合意の着手通知化を防ぐ。§7にSLAルール追記)。触る範囲のパス実在確認・調査予算(5ファイル目安)・10行は「目安」に
- skills/README: 「物理的に人間に残す」→ 空欄は**規約であり強制ではない**と正直に記述。実効性の所在(口頭確認)を明示
- **update-map を新設**(5本目): 理解の地図(§11)の初版生成・更新・矛盾検査。docs/map.md 配置(CLAUDE.md 直埋めは gen-tests の汚染経路になるためリンクのみ)、全体200行以内、追加時は削除候補を同時提示、Level 3 の齟齬は単独枠で報告。フロー図が地図への書き戻しを要求するのに生成手段が無かったギャップを解消
- first-review: 検査項目6「地図との整合」を追加(Level 1〜3を変えるdiffに地図更新が無ければ指摘。地図が無ければ「未検査(地図なし)」)

**引用の修正:**

- §7: tenki.cloud を「第三者ベンチマーク」から「競合ベンダーのベンチマーク」に訂正、存在しない数値「29〜44%」を実測値36.1%に差し替え、「各ベンダーが自社ベンチで勝つ」現象の出典(DeepSource)を追加
- §10: SWE-Mutation の対象を「LLM生成テストスイート」と限定
- §8: Fedoraポリシーの承認報道(LWN)へのリンク追加(リンク先が提案版のため)
- flow-comparison.html: 本文にない義務(incidents.md)の削除、gen-tests適用範囲・「のみ」表現・マージ責任の同期、運用コストの正直化

## v0.5 (2026-08-22)

- `skills/` を新設: 基準の運用部分をエージェント用スキル4本として実装(design-memo / gen-tests / pr-guide / first-review)
- 設計思想「空欄=Human-in-the-Loopの定義」を skills/README.md に明文化。スキルは準備と機械検査のみを自動化し、判断(合意・怪しい箇所の名指し・裁定・分類の確定・マージ)は空欄として人間に残す
- 素通りされる空欄の観察による権限移譲の運用(偽のHITLか、萎え始めた本物かの判定)を追加
- 基準本文は§7にスキルへの参照を追加したのみ

## v0.4 (2026-08-22)

- 理解負債に先行概念(comprehension debt / Triple Debt Model)への参照を追加
- 独立テスト生成に学術先行研究(DiffSpec / Galápagos)を引用
- 権限移譲の外部証拠を拡充:ベンダーベンチマークと第三者評価の乖離、段階的オーバーサイト(arXiv:2606.22484)との違い
- 責任の所在にAI利用の開示(FedoraのAssisted-by慣行)を追加
- カバレッジゲートに実測例(カバレッジ100%でミューテーション80%、SWE-Mutation 71%→40%)を追加
- 訓練の節に実証根拠(AnthropicのRCT:AI補助学習で17ポイント低下・デバッグ最弱/Fekete & Porkoláb:2ヶ月で自分のコードも忘れる)を追加
- READMEに「関連する先行事例・研究」を新設(OSSポリシー6件の比較と調査結果)
- 引用はすべて一次ソースとの突き合わせ検証済み

## v0.3 (2026-08-22)

- 「三つの判断の分離」(検査の委譲=実績/理解の必要性=理解負債/理解のレベル=3段階)と**理解負債**の定義を新設
- 判定軸にドメイン重要性を追加
- 権限移譲の原則(実績が出た方向に権限を移す)と、2026年の実証研究群を外部証拠として追加
- カバレッジゲート(diffカバレッジ・ラチェット・ミューテーションテスト)を新設
- 理解の地図(抽象化レイヤー Level 0-4 と訓練)を新設
- 計測項目(KPI)を追加

## v0.2 (2026-08-22)

- 判定軸に可逆性を追加し、行レベル対象を「不可逆な副作用」で一般化
- Happy Path Bias(手動UI確認の限界)を注記
- 独立テスト生成(別セッション・実装非開示・不一致=仕様曖昧さ検出器)を追加
- 責任の所在、PRテンプレートを新設

## v0.1 (2026-08-22)

- 初版:前提認識(実装時間は理解の時間を兼ねていた)、理解基準3段階、2ゲート、テストの循環問題、隔離ルール、オンコール原則、レビュー運用、導入の順序
