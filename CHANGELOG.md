# Changelog

## v0.39: 委譲の範囲を結合テストまで広げる+設計メモの置き場所と承認の記録を決める(2026-09-04)

2件とも、基準が**前提にしていたのに規定していなかった**ものの明文化である。規範の新設は結合テストの1件、もう1件は既存規範(§4「承認済み設計メモ」)が成立するための置き場所の決定。

### 1. 委譲の範囲: 結合テストまでを機械が書く(§4 新節)

**穴:** §3 のマージゲートは「本番で壊れたとき、どの箱が壊れたか特定できる」ことを要求し、§5 は箱の隔離を義務づけるが、**箱と箱の間を機械が検知する手段が基準のどこにも無かった。** 単体テストは箱の内側しか固定せず、地図 Level 0 の線は描かれているだけで実行できない。

**決めたこと:**

- 委譲の範囲は**実装・単体テスト・結合テスト**まで。人間はテストを書かない——書くのは期待値(§4)である
- **結合テスト**の定義を用語表に追加: 箱の境界を越える契約(地図 Level 0 の線・他の箱を呼ぶ Level 1 の契約)を、**依存の実物か実物相当**(テスト用DB・コンテナ・ローカル起動した依存)を通して固定するテスト。**依存をすべてテストダブルに置き換えたものは結合テストではなく、名前の違う単体テスト**——境界は固定されていない
- **必須になるのは変更が箱の境界を越えるとき**(判定材料は diff。設計メモの「触る範囲」は仮の材料——領域の仮決めと同じ扱い §7)。1箱に閉じる変更は対象外
- **実行手段はリポジトリの既存の手段に従い、委譲の中で新設しない**(基盤整備は PR を触る領域の外へ膨らませる。§7「小さいPR」)。無ければ**「未実施(実行手段なし)」**と記録し、整備は別チケット。「未実施」が続くのは §12 の削除の材料ではなく**整備の材料**(守られていないのではなく、守る手段が無い)
- 結合テストも**回帰検知であって正しさの保証ではない**(実装と同じセッションが書く。自作自演の性質は単体テストと同じ)。正しさは照合で担保し、境界をまたぐ契約は理解検査・独立照合の対象に含める
- 「どの境界を固定し、どの境界を固定しなかったか」は実装したエージェントが完了報告に残し、著者が停止2の先頭で読む(作業から得られる知識はやった側の申告——skills/README の担当表)

**フローへの接続:** 停止1で結合テストの実行手段を確認しヘッダに書く(知識の申告。👤 に数えない)→ 委譲プロンプトに範囲と実行手段を明記 → 完了確認で diff と完了報告から有無を確認し、越えるのに無ければ「未実施(理由)」を停止2の提示に1行(判定ゼロの計器。「対象外」に書き換えない)→ PR テスト欄を単体・結合に分け、結合の行は 対象外/未実施(理由)/実施(固定した境界・固定しなかった境界と理由・実行手段)→ 欄の突き合わせ検査③(境界を越えるなら「対象外」でない)→ first-review 検査項目3 が有無と「依存の実物を通しているか」を機械検査。

**弱まるもの(隠さない):** 実装の完了確認の見込み初期値(4分)は結合テストを含まない時点の実測で、境界を越える変更ではこれより伸びる。実測が出たら差し替える(flow・README に注記)。👤 ゲートは10個のまま。

### 2. 設計メモの置き場所と「承認済み」の記録(design-memo・flow・§7)

**穴:** §4 と crosscheck は「承認済み設計メモ」の要求・契約欄を独立照合・理解検査の材料にし、flow は委譲プロンプトに「承認済み設計メモの所在」を書かせる。しかし design-memo の出力はテキストで、**置き場所も「承認済み」が何を指すかも規定していなかった。** チャットに流して合意した場合、契約欄は後から「実装前に書かれた記述」として引用できず、独立照合の正本が消える。

**決めたこと:**

- **置くのは著者、置き場所はチケットと同じ場所**(本文への追記かコメント)。理由: 設計メモは「実装前に書かれた意図」で、チケット本文と同じ性質の材料。同じ在り処に揃えれば後段のスキルは1つの場所から両方を辿れる。**リポジトリの `docs/` に意図の文書として独立に置かない**——地図(実装後の現実。PR同梱)と寿命・読み手が違い、PR同梱の意図文書は更新が止まることが実証済み(§11 ADR調査)。チケットが閉じれば設計メモも役目を終える
- **承認の記録:** 合意が成立したらメモの末尾に `承認: YYYY-MM-DD <承認者の役割>` を1行追記する。これが「承認済み設計メモ」の実体。以後の書き換えは裁定の書き戻し(crosscheck 手順C)だけで、**上書きではなく追記**(裁定前の記述が消えると「仕様が2通りに読めた証拠」が消える)
- **flow の状態ファイルのヘッダに「設計メモの在り処 / 承認」を追加**し、停止1の合意の直後に著者が申告する(知識の申告。👤 に数えない——チケットの在り処・実データの所在と同じ扱い)。**未記入のまま委譲しない**(承認前の実装着手は既存の「絶対にやらないこと」。ヘッダの空欄がその検査になる)

**採らなかった選択肢:**

- **リポ内 `docs/design/<TICKET>.md` にPR同梱** → 意図と現実が同じ場所に並び、§4「地図に先に書くのではない」と同型の乖離が起きる。ADR調査の失敗モードそのもの
- **flow の状態ファイルに本文を持つ** → リポ外・著者のローカルで、チームの合意対象を置く場所ではない。flow 自身が「状態ファイルに設計判断や仕様を書かない」と定めている
- **design-memo にファイル書き込みを持たせる** → チケットの置き場所を規定しない基準で、書き込み先を1つに決められない。置くのは人間の操作でよい(1回)

**弱まるもの(隠さない):** 「承認: 」の追記と追記のみの規律は指示ベースで、機械検査できない(人間版の独立照合と同じ脆さ)。ヘッダの空欄検査が拾えるのは「未記入」だけで、承認の真偽は拾えない。

変更ファイル: standard.md(用語表・§3・§4 新節「委譲の範囲」・§5・§7・版表記)、skills/design-memo(新節「出力の置き場所と承認の記録」)、skills/flow(SKILL: 手順6・委譲プロンプト・実行手段の扱い / テンプレート: ヘッダ2行・停止1に2項目・完了確認・提示順・欄の突き合わせ③)、skills/crosscheck(共通節の在り処)、skills/pr-guide(テスト欄の単体・結合分離)、skills/pr-guide/assets/pr-template(結合テストの行)、skills/first-review(検査項目3)、skills/README(図・担当表)、README.md(使い方・実測表・版表記)、flow-comparison.html(委譲セル・版表記)。

## v0.38: PR欄「レビュアーへの質問」を削除(2026-09-03)

**一人導入の判断: この欄は無駄。** 空のまま素通りしており、埋まる見込みも無い。§9 自身がこの欄に削除条件を書いていた——「設計メモの先払い(§7)が機能しているほどこの欄は空になる。空が続くなら、偽のHITLポイントかの判定(skills/README)に回して削除を検討する」。**その条件に到達した。** 予告どおり先払いが効いているので、判定は「偽のHITLポイント」ではなく**役目を終えた欄**である。v0.36(「一番怪しい箇所」)と同型だが向きが逆で、あちらは「書かれるが読まれない」、本版は「そもそも書くことが残らない」。

**削除の手続き(§12):**

- **(a) 原因** — 動機でもコストでもなく、**入力が来ない**。方針レベルの未決は設計メモの合意(§7)で消え、実装細部の未決は完了報告と first-review の側に出る。欄が空でも失われる情報が無い
- **(b) 合意** — 一人運用のため著者の判断。チーム導入時は再確認の対象
- **(c) 再追加条件** — 設計メモの先払いを省く運用に戻ったとき、または「聞きたかったことをPRに書く場所が無かった」がレビューで実際に起きたとき

**代替は置かない。** 実装細部の質問は、欄にせずレビュー依頼のメッセージで直接聞く(欄にすると毎回の空欄コストだけが残り、§8「毎回同じ値の欄は情報を持たない」に戻る)。

**弱まるもの(隠さない):** レビューを漫然とした通読から「答え探し」に変える仕掛けが、テンプレートから1つ減る。残るのは「レビュー指定」(どこをどのレベルで読むか)と「確認してほしい項目」(種別A=仕様が2通りに読めた箇所)の2つで、どちらも答え探しの誘導としては質問欄より強い(範囲と争点が具体的に指定される)。

追随: standard.md §9(欄の説明を削除、「本人の判断を要求する欄」の列挙から除去)、pr-guide(description と手順4——手順4は「確認してほしい項目の期待値を生成しない」だけになった)、pr-template(節ごと削除)、版表記3箇所(standard.md・README・flow-comparison.html)。**👤ゲートは10個のまま**——この欄はゲートに数えていない(著者が自分のPRに書く欄で、停止を作らない)。

## v0.37: 停止の3回化——👤ゲートを減らさず、聞く場所を束ねる(2026-09-02)

一人導入の体感「👤でちょこちょこ止まる」を受けて、v0.28「変えなかったもの」で実測待ちにしていた停止統合を実行した。設計は v0.35 と同じく**敵対的検証(反証専任3体: 盲検性・汚染/順序依存と例外ケース/基準内整合と波及漏れ)**にかけ、成立15件・不成立9件をすべて反映した。

### 判断の根拠と、v0.28・v0.29 との整合(正直に書く)

- **v0.28 が要求した実測(「止まった回数・各停止で人間が要した分」)は存在しない。** 分の計測手段(`date +%s`)は v0.35 で初めて入り、それ以降フローを1周回した記録が無い。**本版は v0.28 の判断条件を体感によって明示的に上書きする。**
- **v0.29(実測ゼロの規範変更を反証で却下)との違い:** あちらは検査の検出力を削る変更(欄の上限)だった。本版は👤ゲートを1つも削らず、検査規範(§4)を維持したまま**停止(呼び出し)だけを束ねる**。弱まる箇所は下の「弱まるもの」に明記した。この区別が立たないなら本版も却下対象だった。
- **差し戻し条件:** 導入後3チケットで、停止内の聞き取りをゲート単位で計時(下記)し、「書けなかった欄の数」「食い違いの件数」が統合前の感触より悪化したら停止2を分解して戻す。**本版の「人間の待ち」はゲート単位の粒度になり、v0.36 以前の記録と直接比較できない**(以前は1停止≒1ゲート)。

### 変えたもの

1. **停止は3回(標準ケース=不可逆系なし)。** 停止1=着手(①設計メモ合意→検査の判定→**委譲の送信**→②理解検査の聞き取り)/停止2=PR確認(種別確定とBの反映先・選別・理解レベル・領域・地図検収)/停止3=マージ。独立照合(人間版)の第二解釈者の停止は別に立つ(著者の停止に混ぜない)。**委譲→聞き取りの順序は現行のまま**なので、「委譲時点で期待値が存在しない」構成保証・「実装と並列」・クリティカルパス0分はすべて無傷——変わったのは②が①と同じ呼び出しの中で続くことだけ。
2. **委譲の完了待ちは呼び出しを切る。** 停止1は聞き取りが済んだら終わり、実装完了後に**判断ゼロの継続呼び出し**(完了通知が届く環境では自動)が「完了確認→領域の仮決め→初版生成→突き合わせ→出力との突き合わせ→PR下書き→first-review」を停止2まで一気に走る。上限超過の検出(v0.28)は継続呼び出しの先頭で従来どおり発火する。反証で「一気走りは超過検出を殺すか、呼び直し4回で看板が崩れる」の二律背反が成立したため、**呼び出し=停止3回+継続1回**と正直に数える。
3. **停止2の提示順を固定した**: 完了報告の「迷った箇所」→突き合わせ結果→種別確定→Bの反映先→選別→理解レベル→領域と地図→**記録を済ませてから first-review の出力を開示**。反証: first-review の所見(実装の挙動の詳述)を先に読むと種別確定が「実装は筋が通っている=B」に倒れ、裁定が発火しないまま実装側の読みが地図に固定される。この開示順は構成では守れない**指示ベース**の規律であることを crosscheck「守ること」に明記した。
4. **👤を1つ明示化した(9→10): 「種別(A/B)の確定とBの反映先の指定」(著者)。** 仕事は新設ではない——§4 が最初から著者に割り当てていた確定と反映が、テンプレートでは機械項目「突き合わせと結果の反映」に埋まっていた(standard §4 は「著者が反映する」、テンプレは👤なし——既存の内部矛盾)。反証: Bの価値は**反映先を探す行為**にあり(§4)、機械化すると著者が実装差分を一度も開かずマージまで到達できる。よって著者が反映先を指定し、機械は指定を受けた転記のみ(crosscheck U-5)。
5. **理解レベルの確定を「レビュー時」から「PR確認時(裁定が発生したら裁定後)」に変更**(standard §4 梯子表・design-memo・比較図)。裁定の書き戻しが確定済みレベルの前提を崩す反証があるため、A>0 のときは停止2で確定せずレビュー段へ送る。
6. **領域は機械が diff から仮決めし、停止2でドメインオーナーが確定する。** 判定材料は現行と同じ diff(設計メモの「触る範囲」からの先決めは反証成立——共有基盤への波及で偽になる)。設計メモと diff の食い違いは「領域差分」として停止2に必ず載せる(判定ゼロの計器。v0.35「未マージ:」と同じ設計)。
7. **実データの所在は停止1で聞いてヘッダに書く**(途中の想定外停止を消す)。ただし決め打ちにしない——突き合わせの前に diff の実際の参照先と照合し、不一致なら聞き直す(反証: 着手時の申告は実装後に陳腐化し、黙って古い所在で集計すると検査自身が§4の欠陥を生産する。_active.md と同型の代理指標)。👤には数えない(判断ではなく知識の申告)。
8. **照合メモの置き場を強化**: 委譲プロンプトに現れるどのパスの祖先でもないディレクトリ(例 `~/.flow-crosscheck/<リポ名>/`)。反証: 完了報告の置き場(状態ディレクトリ配下)は委譲プロンプトに書かれるため、同居する照合メモに実装サブエージェントの探索が届きうる。
9. **停止2の確定を受けた機械工程を新設**: Bの転記・**PR欄の再生成**(テスト欄のA/B件数・「確認してほしい項目」・選択肢欄。反証: 停止2で種別が覆ると pr-guide「件数は列挙数と一致」が破れたまま停止3に到達する)・欄の突き合わせ検査。停止3はこの差分込みで見る。
10. **実測の粒度**: 停止内の聞き取りはゲートの区切りごとに `date +%s` を挟み、実測表にゲート単位で1行(§12 で欄単位に削る根拠はこの粒度でしか取れない)。
11. **完了報告の「迷った箇所」の読み手を「実装の完了確認」から「停止2の先頭」に付け替え**(v0.36 の付け替え先が機械工程になったため)。

### 弱まるもの(隠さない)

- 停止2内の「記録してから first-review を開く」は指示ベース(crosscheck A-1「普通にPRを開くと踏む」と同型の脆さ)。確定の時刻を実測表に残して監査可能にするまで。
- 待ちの実測が「1停止≒1ゲート」でなくなり、旧記録と直接比較不可。
- 露出回数が減る計器(未マージ行など)は提示機会が5回→3回に減る。

### 変えなかったもの

- 👤ゲートの削除はゼロ。マージの決断・不可逆系の明示承認・「代表データを自作しない」・機械が👤欄を埋めない、はすべて現行のまま。§7「空欄の一覧が HITL の定義」も無傷。
- 「配置保証」という中間の記録語の新設(検討→**反証成立で不採用**: 委譲→聞き取りの順序を保てば構成保証がそのまま成り立ち、中間語は保証の格上げ詐称にしかならない)。
- 停止2と停止3の統合(5→2回)。マージ決断はB転記・欄再生成の差分込みの最終PRを見る必要があり、実測後に判断(v0.28 と同じ扱い)。

変更ファイル: skills/flow(SKILL・テンプレート全面)、skills/crosscheck(U-5・守ること)、skills/pr-guide(件数の再生成)、skills/design-memo(手順3の後段の決め手)、skills/README(図・冒頭)、standard.md(§4 梯子表・版表記)、README.md(使い方・並行手順・10個表・版表記)、flow-comparison.html(ゲート一覧・PR作成/レビュー段・版表記)。

## v0.36: PR欄「一番怪しい箇所」を削除(2026-09-01)

**一人導入の実測: この欄は使われていない。** v0.22 で「守られない欄は無いより悪い(§12)——自動化か削除かの二択」から自動化(機械転記)を選んだが、自動化が解決したのは「書かれない」だけで、「読まれない」は解決しなかった。読まれない欄は転記コストだけを毎回払う形骸化の供給源であり、§8「毎回同じ値の欄は情報を持たない」と同型。§9 自身が既に「PR説明の欄はレビュー時点でしか効かない」(v0.28)と記録していた——レビュー時点でも使われていないなら、残す根拠が無い。

**転記元の2記録は消えない。中継だけが消える:**

- **実装側の申告**(完了報告の「迷った箇所・仕様が薄く補完した箇所」)は維持。読み手を PR のレビュアーから**著者の完了確認**に付け替えた(flow「セッションの分離」)。将来も参照したい事実は §9 の既存規範どおり地図かコードコメントへ
- **設計時の懸念**(設計メモの「懸念点」欄)は維持。読まれる場所は元々メモの合意時(§7)で、そちらの用途は無傷

追随: standard.md §9 の転記規範を削除、pr-guide(description・手順4を削除し以降を繰り上げ)、pr-template の TL;DR 行、design-memo(懸念点の転記先注記)、flow(完了報告の目的の付け替え)、skills/README の4区分表の例、README、flow-comparison.html(実装段の注記・PR作成段の箇条書き)。v0.22 で口頭確認の問いは「なぜその案を捨てたのか」に移行済みのため変更なし。

## v0.35: 同時進行の判定機構を全廃。flow 全体の安定化と比較図の全面改訂(2026-09-01)

「フローが安定しない」(v0.29→v0.34 が同日に5版)を受けて、flow スキル全体と flow-comparison.html を**敵対的検証(反証専任エージェント3体: 同時進行機構/flow全体/比較図)**にかけた。**規範の追加・削除ゼロ**(実行機構の削除と記録方法の修正)。

### 1. 同時進行の判定機構(`_active.md`)を削除した

不安定の根本原因の診断: 「実装中か・マージ済みか・放置か」は人間の運用状態であり、git の観測値(status・祖先関係・先端位置)はその**代理指標**にすぎない。各版は代理指標のエイリアシングを1つ潰すたびに、別の git 運用前提——未追跡ゼロ(v0.30)・既定ブランチ(v0.31)・マージ方式(v0.34)——を暗黙に決め打ちした。**判定する機構である限り第五・第六の誤判定が続く。** 4案を反証にかけた:

- **案A(v0.34 維持): 反証成立(git 実測)。** GitHub の **Squash and merge / Rebase and merge** では、マージ後も `git merge-base --is-ancestor <ブランチ> <切り元>` が**永久に偽**(squash は main 側に別コミットを作るだけで、ブランチ先端は main の祖先にならない。実測で確認)。ローカルブランチは残り(`git branch -d` は "not fully merged" で拒否される——これも実測)、マージ済みの行が永久に残って以後の委譲が毎回止まる。人間は毎回「放置なので消す」と誤ったラベルで答える——偽のHITLそのもの。マージ3方式中2つで壊れる**マージ方式の決め打ち**であり、v0.31 が v0.30 に貼った「ブランチモデルの決め打ち」と同型の欠陥を v0.34 自身が踏んでいた。ほかに同名リポジトリの2クローンで実装中の行が「ブランチ削除済み=マージ済み扱い」により黙って消える経路も成立。
- **案B(worktree 必須化で検査を全廃): 必須化としては反証成立。** 未追跡ファイル(.env)と依存物(venv 等)は worktree に来ない——環境差からサブエージェントの「仕様の勝手な補完」が誘発される。worktree を知らない人間の `git checkout <実装中ブランチ>` は拒否され(実測)、`worktree remove --force` で作業ごと消える。直列運用(一人運用の既定)では起きない問題のために毎チケット税を払う。
- **案C(全廃・人間の規約へ): ほぼ反証不成立。** 破れるシナリオ(未マージのブランチ上に誤って委譲)は書けるが、(a) PR の diff で自己顕在化し (b) rebase で可逆で (c) v0.30 自身が「事故は未発生」と記録した仮説上の事故。**仮説上の事故0件 対 実測の機構欠陥多数**——§12 に照らして削る根拠は機構の側にある。
- **案D(採用): C+判定ゼロの計器1行+並列選択時のみ worktree。** `_active.md` と委譲直前検査を削除。止まって報告するたび、現在地に `git branch --no-merged <切り元>` の出力を「未マージ:」として写す——**判定しない・消さない・これを理由に止まらない**(答えを要求しないので、無視されても偽のHITLにならない)。委譲は設計メモ合意(👤)の直後なので、人間は委譲前に必ずこの行を見る位置にいる。人間が実装の並列を明示的に選んだときだけ `git worktree add -b <ブランチ> <切り元>` で隔離してから委譲する(切り元が申告から実行になる。v0.29 の README 規約の実行係化)。

残る限界: 忘れは防がれず報告されるだけ(事故が実測されたら worktree 既定化を再検討——そのとき案Bの反証が再び立つ)。squash 運用では計器の行が掃除されないブランチで埋まり価値が減衰する(掃除は人間の `git branch -D`)。2セッション同時委譲の原子性は誰も持たない(v0.33 から不変)。

### 2. flow 全体の安定化(反証で成立した欠陥の修正)

1. **実測「分」を測る手段が無かった**——LLM は時計を持たず、エージェントは毎回もっともらしい分数を**推定で書く**。「基準を削る根拠は実測だけ」(§12)の入力全体が虚構になる。→ 工程の前後に `date +%s` を実行し差から書く。推定で分を書かない。「人間の待ち」はヘッダの「👤で止めた時刻」と次の呼び出しの差(経過時間であって作業時間ではない、と備考に明記)。
2. **委譲の成果物の届け先が未定義**——次の呼び出しが「実装の完了確認」を実行する手段が無かった。→ ヘッダに「実装の委譲先」(ブランチ・切り元・完了報告の置き場所・見込み)を持たせた。
3. **待ちの上限監視が実行不能**——同期待ちの最中に時計を見られる LLM はいない。→ 「次に呼ばれたとき経過を見て報告」に格下げ。
4. **👤で「止まる」と「聞き取りをする」の二重定義**——どちらも SKILL に根拠があり挙動が割れていた。→ 既定は止まる。聞き取りは人間がその場で求めたときだけ、と確定。
5. **対象外の👤で毎回止まる偽のHITL**——手順0で理解検査と判定されても「👤 独立照合」の項目が残り、機械はスキップを禁じられていた。→ 記法 `[-]`(対象外。判定の帰結であり👤のスキップではない)を追加。
6. **再呼び出し時にチケット本文へ戻る経路が無い**——毎ゲートで人間に聞き直すか、記憶で補完(禁止行為)するかの二択だった。→ ヘッダに「チケットの在り処」を追加。
7. **6.記録が発火しない**——マージ後に `/flow` をもう一度呼ぶ動機が無い(v0.31 が `_active.md` に指摘した欠陥と同型が本体に残っていた)。→ 段6を削除。実測は各工程で追記済みで、マージの👤で止まった時点で機械の記録は終わっている。「人間のゲートで止まる回数だけ呼べばよい」が算術どおりになった。
8. **見積もり機構の縮小**——他チケット中央値の参照・根拠1語・10分予告を削除(毎停止で全実測表をパースするコストが、n=1 架空題材由来の精度に見合わない)。参照値は本チケット実測か初期値のみ。**現在地の報告例から古い数値(全29項目・👤11——現行は26項目・👤9)を消し、型だけにした。**
9. **出力との突き合わせで実データの所在が不明なら止まる**を明文化——聞かずに進むエージェントは代表データを**自作**する。それは§4がこの工程で検出しようとした欠陥(分布で壊れる)の再生産である。
10. 状態ファイルをリポ外に置く理由等の設計根拠プローズを CHANGELOG 参照に置換、テンプレートと SKILL の重複注記を SKILL 側に一本化(実例の数字が既にドリフトしていた)。SKILL 116行→84行、テンプレート 91行→83行。**§7 が警告する「手順書自体の肥大化」の第一号案件はこのスキル自身だった。**

### 3. flow-comparison.html の全面改訂とタイトル変更

- **タイトル「理解コストの引っ越し」→「不意打ちを、見積りに変える」。** 旧タイトルはページ自身の lede が宣言する2軸(理解コストの場所+人間の判断の場所)の半分しか表しておらず狭い。「敵対的検証」をタイトルにする案も検証したが不採用——7段中2段しか覆わず(狭さの軸が変わるだけ)、本文にも出ない語で、正しい置き場所は節である。新タイトルはページ自身の結論(「斜線が色に反転すること、それだけが導入の効果」)の一軸に理解コスト・判断・収支の全部が載る。
- **「この基準自身も、敵対的検証を通している」節を新設**(収支の後)。反証つきで採らなかった案が CHANGELOG に残ること・v0.35 で自分の機構を反証により削除したこと・実測の無い効果は主張しないこと。導入判断者にとって「この基準は自分の主張をどう検証しているのか」は採否の材料そのもの。
- **冒頭に「なぜ」の前提文**(実装の時間は理解の時間を兼ねていた→エージェントが前者だけ消した→請求書がレビューとマージに一括で届く)。基準を知らない読者は、これ無しに配分図の「不意打ち」を読めなかった。
- **事実ドリフト11件を修正**。主なもの: /flow の止まり方が v0.27 以前の説明のまま(現在地報告・非👤停止が無い)/実装段「変更なし」が自己矛盾(完了報告の申告は変更後にしか無い)/「+20分前後」が基準本文に無い数字であることの出典明記(TICKET-143 の1回実測)/「構成で保証」の無条件化(順序を守ったときに限る)/自動版の条件の限定列挙(4条件中2つが欠落)/レビュー段の👤に「理解レベルの確定」が欠落(「👤の一覧=人間が担当した範囲そのもの」と宣言するページでの範囲の虚偽申告だった)/着手段に返事の期限・不可逆系の明示承認が欠落/用語集に「出力との突き合わせ」が欠落(本文で2回使用)。
- **構成: 👤 9ゲートの一覧**(「人間が止まる場所はこれで全部」)と**地図のライフサイクル**(初版→検収→PR同梱→障害後→月イチ)を独立の囲みに切り出し、PR作成・レビューの過密セルを箇条書きに分解。用語集を既定で開いた。
- **ダークテーマの実害を修正**: /flow 説明箱のインライン色(`#f8fafc` 等)が唯一トークン化されておらず、ダークで明色文字がほぼ白の背景に乗って読めなかった。

変更ファイル: skills/flow(SKILL・テンプレート)、README.md(並列節)、flow-comparison.html(全面)、standard.md(版表記のみ)。

## v0.34: 親ファイルに「起点コミット」列——未コミットの実装中ブランチを「マージ済み」と誤判定していた(2026-08-29)

v0.33 を反映した直後に、同じデモリポジトリで4場面を流して見つけた。**規範の追加・削除ゼロ**(判定条件の修正)。

### デモで分かったこと

- 場面1(DEMO-1 未マージのまま DEMO-2 が委譲に来る): 正しく止まった。未追跡ファイル(チケット本文・`__pycache__/`)は無関係になった(v0.30 の誤発火は消えた)。
- 場面2(人間が flow の外でマージ → DEMO-2 が再度来る): DEMO-1 の行が黙って消え、委譲が通った。人間は表に触っていない。
- **場面3・4で誤判定。** 委譲直後でまだコミットが無いブランチは切り元と同じコミットを指すので、`merge-base --is-ancestor` が真になり「マージ済み」として消された。**実装サブエージェントが作業中で最初のコミットを打つ前の窓**——最も守りたい期間——が素通りする。

### 変えたもの

1. 行に**起点コミット**(委譲時点のブランチ先端)を持たせ、消す条件を「先端が起点から動いている ∧ 切り元の祖先」にした。先端が起点のままの行は「実装中か放置」として残し、報告に出す。fast-forward マージ(先端が切り元の先端と一致する)でも、先端は起点から動いているので正しく消える——デモで確認。
2. 止まったときの選べる手に「放置なので行を消す」を追加(未コミットのまま放棄されたブランチはこれ以外に消えない)。

### 残っている限界

- 未コミットのまま放棄されたブランチの行は、人間が「放置なので消す」と答えるまで残る。委譲日時で判断する。これが頻発するなら v0.31 の反証(放置行で毎回止まる)が現実になったということで、その実測で再検討する。

変更ファイル: skills/flow/SKILL.md、standard.md・README.md・flow-comparison.html(版表記のみ)。

## v0.33: 同時進行の管理を親ファイル `_active.md` に。作業ツリーの git 検査を廃止(2026-08-29)

v0.30〜31 の「委譲直前に `git status` と切り元との差分を見る」検査を、2チケットのデモで初めて動かした結果から。**規範の追加・削除ゼロ**(止まる条件の実装を差し替えた)。

### デモで分かったこと

- **`git status --porcelain` は毎回誤発火する。** チケット本文をリポ内ファイルに置くと(基準はこれを許容している)未追跡ファイルとして残り、実装サブエージェントが作る `__pycache__/` も同じ。中身を見ずに「構わず」と答え続ける偽のHITLになった。
- **切り元との差分検査は、切り元の選択を間違えたとき(feat/T201 のまま T202 を切った)だけ効いた。** 正しく main から切れば空になる(それで正しい)。「同じ作業ツリーで2本の変更が混ざる」という v0.30 の説明は言い過ぎで、実態は「間違った親から分岐したことの検出」だった。
- 状態ファイルをリポ外に置いた v0.32 は、ブランチを5回切り替えても状態ファイルが消えず、効いた。

### 変えたもの

1. **親ファイル `~/.flow/<リポジトリ名>/_active.md`**(チケット | ブランチ | 切り元 | 委譲日時)。委譲直前に flow がこれを読み、**各行のブランチが切り元にマージ済みかを git に聞いて、済んでいる行は人間に聞かずに消す**。残った行があれば止まって報告(選べる手は従来どおり)。委譲したら自分の行を足す。
2. `git status` の検査を廃止。テンプレートのヘッダから「ブランチ(切り元)」行を削除(親ファイルに移った)。README の並列節を書き換え。版表記3箇所。

### v0.31 で親ファイル案を退けた理由が、なぜ今は当たらないか

v0.31 の反証は「行を消すのは人間かマージ後の 6.記録で、その動機が手順に無い。放置行で毎回止まる」だった。これは**消す作業を人間の操作(マージ・push)に結びつけた設計**への反証で、正しかった。本版は消す作業を **flow 自身が委譲のたびに git に確認して行う**形にしたので、人間の動機に依存しない。「マージされたか」の真実は git にしかない、という v0.31 の結論も変えていない——git の役割を「作業ツリー全体を見て判断する」から「表の行ごとにマージ済みかを裏で確認する」に縮めただけである。

### 残っている限界(実測で見る)

- 2セッションが数秒差で同時に委譲直前に到達すると、両方が空の表を見て両方書く(原子性なし)。人間が手番を持つ運用では起きないと見て、塞いでいない。
- 切り元を間違えて分岐したこと(デモで唯一 git 検査が効いた場面)は、本版では検出しない。表に書かれた切り元は申告であり、実際の分岐元とは照合しない。これが実際に起きたら、その実測で再検討する。

変更ファイル: skills/flow(SKILL・テンプレート)、README.md、standard.md・flow-comparison.html(版表記のみ)。

## v0.32: 状態ファイルをリポジトリの外へ(2026-08-29)

「`docs/flow/` はリポジトリの外でしょ?」という問いから。基準は状態ファイルを `docs/flow/<ID>.md`(リポからの相対パス)と書きながら、**コミットするのか .gitignore するのかをどこにも決めていなかった。** 未定義のまま置くと、ブランチにコミットされた状態ファイルは切り替えた瞬間に見えなくなる。**規範の追加・削除ゼロ**(置き場所の確定のみ)。

### 変えたもの

1. **状態ファイルの置き場所を `~/.flow/<リポジトリ名>/<TICKET-ID>.md` に確定**(skills/flow「状態ファイル」)。理由3つ: ①ブランチを切り替えても消えない ②他チケットの実測表(見込みの参照値)が常に全部見える ③実装サブエージェントがリポジトリを探索しても読まない(照合メモをリポ外に置いたのと同じ理由)。リポジトリには何も残らない。
2. 照合メモも同じディレクトリ配下でよいが**別ファイル**にする、と明記(状態ファイルの所在は委譲プロンプトに書きうるため)。
3. `docs/flow` の参照を全箇所差し替え(flow SKILL・テンプレートの「docs/flow 中央値」→「他チケット中央値」、README、skills/README、flow-comparison.html)。版表記3箇所。

### 経緯

v0.31 で退けた親ファイル案の反証の1つが「リポ内に置くとブランチ切替で消える」だった。この反証は状態ファイル本体にも同じように当たる——親ファイル固有の欠点ではなく、置き場所が未定義だったことの欠点である。親ファイル案そのものは、置き場所を外にしても「マージ後に行を消す作業が正常経路で起きない」(v0.31)が残るので採らない。

変更ファイル: skills/flow(SKILL・テンプレート)、README.md、skills/README.md、standard.md・flow-comparison.html。

## v0.31: 委譲直前の作業ツリー検査を「切り元ブランチ」基準に(2026-08-29)

v0.30 の検査が「既定ブランチ(main 等)に無いコミット」を見ていたのは**ブランチモデルの決め打ち**で、develop や親ブランチから切る運用では常に発火する、という指摘から。**規範の追加・削除ゼロ。**

### 変えたもの

1. 検査の基準を**切り元ブランチ**にした(skills/flow「セッションの分離」)。委譲時に現在ブランチと切り元を状態ファイルのヘッダに記録し(切り元は人間に聞く)、`git log <切り元>..HEAD` を見る。T202 を feat/T201 から意図的に切った場合に発火するのは誤検出ではなく「T201 の未マージ変更の上に委譲する」の正しい報告で、人間が「構わず」と答えれば進む。
2. テンプレートのヘッダに「ブランチ(切り元)」行。README の並列節の「main に無いコミット」を「切り元に無いコミット」に。版表記3箇所。

### 検討して採らなかったもの(敵対的検証で反証成立)

**親ファイル `docs/flow/_active.md` で「実装中のチケット」を明示管理し、git 検査を廃止する案。** 一見素直だが:

- **正常経路で毎回誤発火する。** 行を消すのは「👤 マージ `[x]` 後の 6.記録」だが、マージは人間が flow の外で行い、README の手順は「マージしてから2本目の `/flow`」。1本目の 6.記録のために `/flow` をもう一度呼ぶ動機が無いので、2本目は毎回1本目の行で止まり、人間は毎回「放置なので消す」と答える。v0.30 が状態ファイル案を退けた理由(偽のHITLを1個足す)と同型。
- 行の日時ではレビュー待ちと放棄を区別できない。区別するには `git branch --merged` を見る——結局 git 依存が戻り、親ファイルの存在理由が消える。
- ブランチ上でコミットされていると、切り替えた瞬間に消えて素通りする。未追跡の強制が要り、footprint が .gitignore に伸びる。`docs/flow/` を列挙する既存手順(未完了チケットの提示・実測の中央値)に誤って拾われる。
- 「状態を1つのファイルに記録する」(flow 冒頭)を破って2ファイルになる。

v0.30 の欠陥は「既定を決め打ちした」ことで、検査の構造ではなかった。`git status --porcelain` にはブランチの仮定が無く、人間の作業中の編集も拾うので残す。

変更ファイル: skills/flow(SKILL・テンプレート)、README.md、standard.md・flow-comparison.html(版表記のみ)。

## v0.30: 実装を委譲する直前に作業ツリーを見る(2026-08-29)

v0.29 の並列運用(設計並行・実装直列)で、「1本ずつ」が人間の守るルールでしかなく、`/flow` の2回目を順番を間違えて打てば他チケットの未マージ変更の上に委譲される、という指摘から。**規範の追加・削除ゼロ**(flow は実行係。止まる条件を1つ足した)。ゲートの数(11)は変えていない——これは `👤` ではなく、上限超過と同じ「`👤` ではない止まり方」。

### 変えたもの

1. **委譲の直前に `git status --porcelain` と既定ブランチに無いコミットの有無を見る**(skills/flow「セッションの分離」)。どちらかがあれば委譲せず止まり、差分のファイル数・ブランチ名・選べる手(マージを待つ / 作業ツリーを分ける / 構わず委譲)を報告する。判断はしない。「構わず」なら委譲して実測表に既存の「同時進行 / 実装順(決めた者: 人間)」を残す。
2. テンプレートの「実装の委譲」に注記1行。README の並列節に「順番を間違えても止まる」を1文。版表記3箇所を v0.30 に。

### 検討して採らなかったもの(敵対的検証で反証成立)

最初の案は **他チケットの状態ファイルを見る**(「実装の委譲 `[x]` ∧ 実装の完了確認 `[ ]`」なら止まる)だった。反証:

- **代理指標が外れている。** その状態は「実装が走っている」ではなく「flow が完了報告をまだ受け取っていない」で、期待値の書き出しで止まっている全チケットの**正常状態**。実装4分は人間が期待値を書く間に終わっているのが普通で、報告は事実と違うことを言う。
- **危険な窓の1/4しか覆わない。** 危ないのは委譲〜マージの全体(README の規則も「マージしてから」)。完了確認 `[x]` 以降の PR作成・レビュー・マージ待ちの未マージ変更は検査を通る。
- **放置ファイルで毎回止まる。** 「消すか実測表だけ残す」の規約どおり実測表だけ残しても `[x]/[ ]` は残り、規約に従っても誤検出する。skills/README の言う「毎回素通りする空欄=偽のHITL」を自分で1個足すことになる。
- **2セッションの同時読みに原子性が無い。** 双方が `[x]` を書く前に検査を通過すれば両方委譲される。

作業ツリーを直接見る形は、実行状態そのものを見るのでこれらを全部解く(別 worktree なら作業ツリーが別なので発火しない——並列モードでは発火しなくてよい)。欠点は人間の無関係な未コミット編集でも止まることだが、それも「その上に委譲してよいか」を聞くべき場面である。

### 実測ゼロで足すことについて

v0.29 で「並行運用の実測が出てから」と書いた同日に構造を足すのは姿勢の矛盾ではないか、という指摘があった。保留しなかった理由: 検証で見つかった欠落は「並行時の負荷」ではなく **flow が作業ツリーの状態を一切見ていない**ことで、並列運用の有無に関係なく、未コミット編集の上に委譲する事故の原因になる。実測を待つ対象(欄の厚さ・上限・フローの分割)とは別の種類の穴と判断した。事故は未発生であり、この判断自体が実測で検証されるべきものとして記録する——発火した回数と、そのうち「構わず」と答えた割合を実測表から集計し、常に「構わず」なら偽のHITLとして削る(§12)。

変更ファイル: skills/flow(SKILL・テンプレート)、README.md、standard.md・flow-comparison.html(版表記のみ)。

## v0.29: 複数チケットの同時進行を実測表に記録する(2026-08-29)

「フローを使って3チケットを同時に進められないか」から。**規範の追加・削除ゼロ**。記録項目を1つ足しただけ。

### 変えたもの

1. **実測表の備考に「同時進行: <他チケットID> / 実装順: n番目(決めた者)」を残す**(flow テンプレートの注記・skills/flow「守ること」)。並行で回した回と直列で回した回を同じフローで記録し、`👤` の人間の待ちが並行時にどう変わるかを比較可能にする。ヘッダではなく実測表に置くのは、状態ファイルは閉じたら実測表だけ残す(flow「守ること」)ため。「決めた者」を併記するのは、実装順の決定を機械が黙って行わない(flow「判断は一切しない」)ため。
2. 見込みの参照値を取るとき、同時進行の有無が同じ行を優先する(skills/flow「守ること」)。
3. **README「使い方」に「複数チケットを同時に進める」節を追加。** 設計並行・実装直列(既定)と実装まで並行(領域が別・作業ツリーが別のときだけ)の手順、共通の注意(セッションはチケットごと・照合メモはIDで分ける・実測表にタグ)。規範ではなく運用手順なので README に置いた。
4. 版表記3箇所を v0.29 に。

### 検討して採らなかったもの(敵対的検証で反証成立)

並行運用の動機は「設計は機械が並行で安く回せる。実装は作業ツリーが1つなら直列。だから設計段階でコードを深く読ませて直列区間を短くしたい」だった。これを受けて2件を提案し、サブエージェントによる敵対的検証に回した結果、**どちらも反映しない**。

- **design-memo の「読むファイル5個まで」を並行運用では外す。** 反証: 上限の理由を「人間が同期で合意を待つ前提の数字」としたが、その記述はリポジトリのどこにも無い(提案者の創作)。書かれている理由は「方針が書ける程度でよく、深追いしない」+幻視防止(design-memo 手順2)で、v0.6 で「調査予算」として導入された(CHANGELOG v0.6)。しかも既に「目安」なので、超えたければ超えて理由を実測表に残せばよく、規範の変更は不要。
- **深く読んだ成果を「契約」欄と「触る範囲」欄に厚く書いてよい(事実と契約は汚染にならない)。** 反証: 基準は契約欄を「意図」と定義しており(design-memo「守ること」)、意図は著者の決定であって事実ではない。触る範囲は crosscheck が汚染源と名指ししている(共通節)。設計メモは実装サブエージェントの委譲プロンプトに入る(flow「セッションの分離」)ので、そこに失敗の仕方・不変条件・落とし穴を厚く書くことは、§4 が禁じる「期待値を設計メモに書いてエージェントに読ませると一致が自動生成される」経路を、欄名を変えて踏むだけである。§4 の検出力の源(エッジケース・失敗時の挙動・境界値で著者とエージェントの読みが食い違う)を、まさにその3種の細部を書くことで削る。
- 共通の反証: **実測ゼロで規範を変える**。v0.28 が同系統(並行・停止回数)の変更を実測待ちにした直後であり、§12 の手続き(測ってから改訂)に反する。本版はその測定器だけを入れ、上限や欄の厚さは並行運用の実測が3件出てから議論する。
- 副次的な指摘: 直列区間の支配項は実装(初期値4分)ではなく人間の手番(標準ケースで5回止まる)。実装を数十秒縮めても総リードタイムは変わらない。深読みで得た既存コードの事実を残したいなら、置き場は設計メモではなく地図 Level 2〜3・コードコメント(§9・§4 種別B)。

変更ファイル: skills/flow(SKILL・テンプレート)、standard.md・README.md・flow-comparison.html(版表記のみ)。

## v0.28: flow の止まり方——現在地の報告・次のゲートまでの見込み・待ちの上限(2026-08-29)

`/flow` を回した実感から。「ちょくちょく止まる」「長いときはずっと動き続けて、どれくらいかかるか分からない」。**規範の追加・削除ゼロ**(flow は規範を持たない実行係。変えたのは報告の書式と見積もりの手順だけ)。ゲートの数(11)と順序は変えていない。

### 変えたもの

1. **止まるたびに「現在地」を固定書式で先頭に出す**(skills/flow 手順5)。6フェーズの中のどこか・完了/全項目の数・👤 の通過数・**この後に機械が止まらずに走る工程の件数と見込み分**・残りの 👤 の一覧。止まる回数はゲートの数で決まるので減らせないが、「あと何回止まるか」が見えれば体感は変わる、という判断。
2. **次の 👤 までの見込みを出す手順**(skills/flow「見積もりと上限」)。参照値は ①本チケットの実測表 ②`docs/flow/` の他チケットの中央値 ③初期値 の優先順で、根拠を1語で併記。幅で出す(下限=参照値、上限=2倍)。初期値は TICKET-143(基準を1周回した記録)の**1回の実測・架空題材**で、実装 4 分・first-review 6 分・他は 1〜3 分。自分の実測が3件たまったら使わない、と明記。
3. **「実装の完了確認」を唯一の上限なし項目として扱う。** 所要が規模に比例する工程はここだけ(他は入力の長さがほぼ一定)。委譲時にサブエージェントへ「変更ファイル数と見込み(分)」を最初に報告させ、ヘッダの見込み欄に書く。
4. **待ちの上限。** 実装・first-review・自動版の待ちに上限(=見込みの上限値、無ければ30分)を置き、超えたら **👤 ではない止まり方**で報告する(経過・最後の報告・選べる手)。続けるかどうかを機械が決めないのは既存の「判断は一切しない」の延長。超過は実測表に残して初期値を直す材料にする。
5. **10分を超える機械区間に入る前に1行予告する。**「ここから約○分、止まらずに走る」。止まらずに走ること自体は問題ではなく、予告なく走ることが問題だった。
6. テンプレートのヘッダに「見込み/待ちの上限」行を追加。実測表の注記に「この表が次のチケットの見込みの参照値になる」を追記。README の /flow 説明に1文追記。版表記3箇所を v0.28 に。

### 変えなかったもの・検討中(未採用)

- **ゲートの統合による停止回数の削減。** 標準ケース(不可逆系なし・地図あり・一人運用)で実際に止まるのは 設計メモ合意/理解検査の期待値/採らなかった選択肢の選別/理解レベルの確定/マージ の5回。このうち2回は隣接する機械工程との依存が無いので、順序を変えれば同じ停止で聞ける可能性がある——(a)「領域を決める」を設計メモ合意と同じ停止で聞く(領域の決定は地図に触らないので §3 に置く理由は「地図に触るのはここだけ」の近傍性だけ)、(b)「採らなかった選択肢の選別」を first-review の後に回し、§4 の理解レベル確定と同じ停止にする(first-review は選別の結果に依存しない)。これで 5→3〜4 回。**ただしフローの順序変更は状態ファイル・skills/README の図・flow-comparison.html の3箇所に波及し、本文 §7 のゲート定義の並びとも突き合わせが要る。**次に flow を1周回して「止まった回数・各停止で人間が要した分」の実測が出てから判断する(本版で実測表がその根拠を出せる形になった)。
- **見込みの精度を上げるための計測の追加**(トークン数・サブエージェントの進捗ポーリング)。§7 の手順書肥大化の警告に自分が当たる。実測3件で初期値を捨てる、で十分。

変更ファイル: skills/flow(SKILL・テンプレート)、README.md、standard.md・flow-comparison.html(版表記のみ)。

## v0.27: 規範と根拠の分離——出典の引用ブロックを参考文献へ、読み手の明記(2026-08-28)

「standard.md はAIが読むのか。読むなら出典の引用は邪魔な文脈ではないか」という指摘から。**規範の追加・削除ゼロ。** 文言の移動と読み手の明記のみ。

### 変えたもの

1. **読み手を冒頭に明記した。** 想定読者は採用を判断する人間。エージェントが日常的に読むのは skills/ で、standard.md は節アンカー(`#s4` 等)で必要な節だけ参照される。ただし README が「クローンして『導入したい』と言えばエージェントがやる」導線を置いている以上、本文が丸ごと読まれる経路は存在する。v0.26 で足した §7「手順書自体の肥大化」の警告がこの文書自身に当てはまる、と本文で認めた。
2. **出典の引用ブロック2つを末尾「参考文献」(`#refs`)へ移した。** §1 の comprehension debt / Triple Debt Model(R1)、§4 の DiffSpec / Galápagos(R2)。本文には1行の参照だけ残す。規範として読ませたい引用ブロック(§4「エージェントは実装に合わせたテストを書く」、§6「オンコール責任者にはなれない」)は引用のまま——これらは根拠ではなく規範の強調。
3. **README の standard.md 説明に読み手と出典の置き場を追記。** 版表記3箇所(standard / README / flow-comparison.html)を v0.27 に。

### 変えなかったもの

- **§11「根拠と先行例」小節。** g3doc・Backstage・AGENTS.md・SREプレイブック・ADR調査の列挙は根拠だが、「隣接配置は既定にしないが両立する」「PR同梱・AI一次レビュー・freshnessヘッダーはADR調査の失敗モードへの対策」という運用の互換条件と分離できない。参考文献から所在を示すだけにした。
- **本文の分量そのもの**(386→398行。参考文献節の追加分)。削るのは §12 の削除手続きの対象であり、今回の射程は配置の整理まで。

## v0.26: 実運用報告からの取り込み3件——手書きの前に疑う順番・新規参加者による地図検査・手順書の肥大化監視(2026-08-28)

木村健一郎「[AI駆動開発をチームに根付かせる——「1行も書かない」チームがHarnessを育てた1年](https://speakerdeck.com/kenichirokimura/embedding-ai-driven-development-into-your-team)」(2026-08-27)を本文と突き合わせた。**規範の追加は3件、削除ゼロ。** 出典は1チームの1年間の実運用報告であり定量評価ではないので、本文中でもそう明記した。ゲート(`👤`)の数・フローの順序は変わらないため、flow-comparison.html は版表記のみ更新。

### 取り込んだもの

1. **§8: 実装の委譲を「原則」にする理由と、手で書く前に疑う順番。** これまで「実装は原則エージェントに委譲」は開示の簡略化の文脈にしか置かれていなかった。もう1つの理由——著者が「自分で書いた方が早い」と手で直すと、手順書(スキル・指示・地図・チケットの切り方)の欠陥がその場で吸収されて §12 の改訂ループに材料が届かない——を明記し、疑う順番を①指示 ②文脈 ③設計 ④タスクの切り方 ⑤役割分割と定めた。**手で書くこと自体は禁止しない**(行レベル理解対象で著者が自分で書くのは正当。書く/書かないと理解の深さは独立の軸)。禁止するのは記録の無い手書きだけ。記録の置き場は flow の実測表(skills/flow「守ること」に1行追加)。
2. **§11 訓練: 新規参加者による障害シミュレーション。** 本文にオンボーディング・途中参加の言及が無かった。既存メンバーは地図に無い知識を頭から補えるので地図の欠落を検出できない——検出できるのはその知識をまだ持たない人だけ。途中参加者に地図と手順書だけを渡して障害シミュレーションを通し、通らなければ欠落は地図側(新人の力量の問題にしない)。月イチ矛盾検査・定期シミュレーションに並ぶ第三の検査手段として置いた。
3. **§7 測定リスト・§12 手順3: 手順書自体の肥大化の監視。** 「基準自身の運用コスト」は人間の時間しか指していなかった。手順書(スキル・指示・地図)はエージェントに毎回読み込まれる文脈として積み上がり、コンテキスト増・トークン増・応答遅延・無関係な指示の読み込みの4症状で手順書自体がボトルネックになる(出典 Stage 4)。手順書の分量・トークン消費・応答待ち時間を §7 の測定項目に加え、§12 手順3の計上を「人間の時間」「エージェントの文脈」の2系列に分けた。**この基準自身(スキル6本・本文386行)に当てはまる警告**として置いている。

### 取り込まなかったもの

- **「1行も手で書かない」の規範化。** §2 行レベル対象は「AI以前と同じ基準」で、手で書く/書かないと理解の深さは独立。出典の原則1は改善圧力の話なので、上記1の形でその部分だけ切り出した。
- **PoC 段の要件探索**(仕様が書けない段階で動くものを作って要件を発見。アーキテクチャにはガードレール)。この基準の射程(理解・PR・マージ・レビュー)の外。
- **人材育成の観察**(育ったのは読む・選ぶ・評価する・説明する力。白紙から手で書く力は育ったと言い切れない)。§11 訓練が引く Anthropic RCT と整合する証言だが、逸話なので本文には引かず、README の先行事例に1段落として置いた。

### 一致していたもの(規範の変更なし)

原則2「生成コードの責任は指示した人間」「なぜこのコード/設計かを説明できる」「Review の指摘が正しいかを自分で判断」「分からなければ手で書かず分かるまで AI に質問」は、§3・§8 説明責任・first-review「所見は候補」・§11「地図への尋問」と同一。Stage 5 の「設計・調査/実装/レビュー Agent の分割」「どの Context がどの処理に必要か」は skills の役割分割そのもの(v0.25 #2 の材料規定修正と同じ問題)。

## v0.25: 整合性修正——レビューで見つかった矛盾15件(2026-08-28)

v0.24 までの本文・スキル・図を突き合わせて矛盾を洗い出し、まとめて直した。**規範の追加は2件(一人運用の独立照合の充足手段・新領域のドメインオーナーの決め方)、削除ゼロ。** 他は既にある規範に他の箇所を追随させただけ。

### 重大(読み手を誤らせる)

1. **flow-comparison.html が v0.11 のまま13版分止まっていた。** README のトップ導線がこれなのに、「独立照合の既定は人間版」(v0.12 で理解検査が標準に)、マージ確認が「独立照合が『未実施』でないこと」(v0.19 が欠陥として潰した文言そのもの)、「一番怪しい箇所」を人間が書く(v0.22 で機械転記に)、「出力との突き合わせ」(v0.17)が不在、「空欄2つ」(実数3)。v0.17 で「見出しの版はリリースごとに更新する」と定めていたのに、対象に図が入っていなかった。**以後、版を上げるときは standard.md・README・flow-comparison.html の3箇所を同時に更新する。**
2. **crosscheck の材料規定が「設計メモと一緒に承認された地図 Level 1」を挙げていたが、そのような地図は存在しない**(§4・design-memo が「地図に先に書かない。地図の更新は実装後のPRの中」と定めているため)。「設計メモの契約欄」+「既存の地図 Level 1(前回までの現実。変えない契約の期待値にだけ使う)」に書き直した。
3. **一人運用で不可逆系に触ると、必須の独立照合を満たす手段が無かった。** 人間版の書き手は不在、自動版への昇格条件に「第二解釈者を確保できない」が無い。実測の多くが一人導入なのに、§7 の一人運用の例外は領域決定にしか無かった。→ **昇格条件に「第二解釈者を確保できない(一人運用)」を追加**し、記録に「第二解釈者不在のため自動版」と残す(§4・crosscheck 手順0・flow テンプレート・README)。
4. update-map が「一人運用の例外は flow『絶対にやらないこと』」と案内していたが、そこにある例外は領域決定のもので、地図初版の先送りの例外ではない。参照を削除。

### 整合の綻び

5. マージ要件が §3「Level 0〜3 まで検収済み」と §11「最低ラインは Level 2・3」で割れていた。§3 を §11 の最低ラインに寄せた。
6. PRテンプレートの「採らなかった選択肢(**1つ以上**)」が水増しを要求していた(design-memo「検討していない案を並べない」・pr-guide「無ければ空欄で返す」と衝突)。必須個数を外し「無ければ『なし』」に。
7. §9「本人の判断を要求する欄を**1つ**残してある」が実態(選別・裁定・質問)と合わない。単数表現を外した。
8. crosscheck「first-review はこのスキルより前の段で走る」は独立照合・人間版(レビュー段)にしか当てはまらない——理解検査は実装と並列で first-review より前。対象を明記。
9. 理解検査の対象「構造理解以上」は、実施時点(実装と並列)では理解レベルが未確定(確定はレビュー段の 👤)。「設計メモの提案レベルで判定」と明記。
10. README の「答えるだけのもの」に地図初版の検収が入っていたが、最低ラインが「Level 2・3 の通読と**修正**」なので書く作業。5/4 に分け直した(合計9は不変)。
11. flow テンプレートの「不可逆系の確定はドメインオーナー(§8)」——§8 の表がドメインオーナーに割り当てているのは行レベル該当・ドメイン中核で、不可逆系の確定は設計メモの承認(§7)で担保される。参照先を直した。
12. PRテンプレートの節見出し「確認してほしい項目(理解検査)」が独立照合の種別Aの置き場所を読み取れなくしていた。「理解検査・独立照合」に。crosscheck 手順C-4 も A→欄、記録→テスト欄と明記。

### 弱かった点

13. **§10(カバレッジゲート)がどの強制点にも接続していなかった**(skills・flow・§12 に登場ゼロ。v0.19 が「出力との突き合わせ」で直したのと同じ形)。§12 手順2に「ラチェット方式で同時に置く。ミューテーションは行レベル対象が出た時点で」を追加。
14. **新領域のドメインオーナーを誰が決めるかが未定義だった**(領域を決めるのはドメインオーナー、ドメインオーナーは領域ごと——循環)。§8 に「持ち込んだPRの著者が候補を提案し、チーム(一人運用なら著者自身)が確定」を追加。update-map モード1にも。
15. update-map の「CLAUDE.md に地図を埋め込まない」理由①(汚染経路)が弱かった——同じ内容が `docs/map/` にあれば材料として可なので、判定軸はファイルの場所ではない。「自動注入は、材料を絞りたいセッション(ツール制限サブエージェント)からも外せない」に書き直した。理由②は変更なし。
16. crosscheck の frontmatter description が384字でリンク・太字を含んでいた(次に長いもので158字。スキル選択に使われる欄なのでリンクは効かない)。150字程度に圧縮。規範の変更なし。

変更ファイル: standard.md、README.md、flow-comparison.html、skills/crosscheck、skills/update-map、skills/pr-guide(SKILL・テンプレート)、skills/flow(テンプレート)。

## v0.24: 設計メモの著者専用欄を聞き取りで埋められるようにした(2026-08-25)

**きっかけは v0.22 の構造的な懸念。** 「懸念点」を著者専用にしたが、白紙の欄を著者が書く運用は、「一番怪しい箇所」が毎回は書かれなかったのと同じ構造で、書かれずに残る可能性が高い。参考にしたのは mattpocock/skills の [grill-me](https://github.com/mattpocock/skills)(計画・設計をAIが執拗にインタビューし、決定のツリーを層ごとに質問して曖昧さを露わにする)。**向きが基準と逆**——基準の設計メモは「機械が下書きし、人が直す」、grill-me は「機械が問い、人が答える」。基準は理解検査(crosscheck 手順U)で後者の向きを既に採用しているので、その規律を設計メモの著者専用欄に広げた。

### 決定: design-memo に手順4「聞き取り方式」を追加

- 対象は著者専用の欄(懸念点・採らない選択肢の判断・未決定事項の「いつ誰が」)。1問ずつ質問し、答えをそのまま書き留める——**転記であって生成ではない**(理解検査と同じ規律。新しい規範ではない)
- **推奨回答は添えない**(grill-me は添える)。添えると機械の案に○を付ける形に倒れ、選択式の条件(v0.13)と同じ問題になる
- **10行を超える前に止める**(grill-me は曖昧さが尽きるまで続ける)。設計メモの目的は完全性ではなく先払いの速さで、残りは「未解決の論点」へ
- 「特に無い」も答え。その欄は行ごと削る

採らなかった2点は、基準側に既に反対の規範があるため(選択式の条件・10行の目安)。

変更ファイル: skills/design-memo、skills/flow(テンプレート注記)。

## v0.23: 精査——重複・二重化した手順・過剰な根拠を削除(規範の追加ゼロ・削除ゼロ)(2026-08-25)

v0.12〜v0.22 の11版を2日で重ねた結果、同じ規範・同じ実測・同じ根拠が複数箇所に増殖していた。標準本文とスキル群を敵対的に監査させ(候補: 標準59件・スキル約80件)、HIGH・MED を適用した。**規範(守るべきルール)は1つも足さず、1つも消していない。** 削ったのは次の5種類だけ。

| 種類 | 例 |
|---|---|
| **同じ規範の反復** | 「著者は設計図を書いた当事者」×3、「理解検査は独立照合ではない」×3、「理解が足りないシグナル」×3、「不可逆系の列挙」×3(用語表に一本化)、「実行された≠検証された」×3 |
| **標準本文とスキルの二重化** | §4「標準の検査」の器・聞き取り・転記様式・選択式条件(crosscheck/pr-guide に逐語で存在)→ 規範1文+リンク。§11 の地図メタデータ様式(update-map に存在)。§8 の欄突き合わせ手順(flow に存在) |
| **同じ実測の重複語り** | 一人運用の領域誤選択(§7と§11)、31/32欄空白(§4で2回+crosscheck)、「一番怪しい箇所」を著者が書かない(§9で2回+pr-guide)→ 各1回。スキル側の実測は全削除(0件)。標準には規範ごとに1つ残す |
| **過剰な根拠** | 種別Bの理由①②③→①のみ、AI開示欄の経緯(承認年月・外部貢献の文脈・合成解釈の実測)、g3doc の配置論、ベースライン計測の滞留時間論 |
| **履歴・死文・古い数値** | 「旧名 gen-tests」「以前は複製を置いていた」「かつてそうだったが転記に変えた」「OKF v0.2 と同形(判断は v0.11)」、`/flow` の「最大10回」(実数9)、README「全10個」→9 |

テンプレート(flow-template・pr-template)は注記を1行に圧縮——利用者のリポジトリにコピーされ、PR本文に貼られる文書に根拠は要らない。

**分量**: standard.md 70.5KB→55.1KB(-21%)、crosscheck -26%、pr-guide -31%、pr-template -26%、flow-template -20%、design-memo -20%、update-map -16%、first-review -16%、skills/README -13%。全体で約 -21%。

検証: 相対リンク・§アンカー全件解決、flow-template からの手順参照(手順0/U/U-4・U-5/A-1・A-2/A-4/B、モード1/2)は全て実在、👤 項目は9件のまま。

変更ファイル: standard.md(全節)、skills/*(6本すべて)、skills/README、README。

## v0.22: 設計メモに懸念点・未決定事項を追加し、「一番怪しい箇所」を著者記入から転記に変更(2026-08-25)

### きっかけ2つ

1. **一人導入の実測: PRの「一番怪しい箇所」は著者が毎回は書かない欄になっていた。** v0.21で実装側の申告の転記を認めたが、確定と記入は著者に残していた。守られない欄は無いより悪い(§12)——自動化か削除かの二択で、**自動化(転記)**を選んだ。削除しなかったのは、転記元が2つ揃ったから(下記)
2. **[pospome「優秀なエンジニアが書くDesign Docは何が違うのか?」](https://www.pospome.work/entry/2026/08/24/223309)(2026-08-24)の3区分**——代替案(選ばなかった案。不安の無い内容)・懸念点(選んだ案に残る不安、助けてほしいこと)・未決定事項(あえて今決めない。なぜ今決めないか・いつ誰が決めるか)。設計メモは「採らない選択肢」と「未解決の論点」しか持っておらず、**「決めたが不安が残る箇所」の置き場が無かった**。「未解決の論点」は「助けてほしい」と「あえて決めない」を混ぜていた

### 決定1: 設計メモに「懸念点」「未決定事項」を追加(design-memo・§7)

- **懸念点は著者だけが書く**(機械は生成しない——採った方針に残る不安は著者の判断の残留物で、機械が書けば捏造)。無ければ行ごと削る
- **未決定事項は「考えていない」と区別するために書く。**「いつ誰が決めるか」が書けないものは未解決の論点。書けたものはflowの後段のゲートに対応する(理解レベル→レビュー時にドメインオーナー、領域→PR作成時)
- 「採らない選択肢」に判断の軸を1つ添える(例: 何年持たせる前提か)
- 記事の「エンジニア評価」「記載量の多寡ではない」「ドキュメンテーション能力」は射程外として取り込まない

### 決定2: 「一番怪しい箇所」は機械が転記で埋め、著者は見て消す(§9・pr-guide・flow・README)

転記元は2つ、出所ラベル必須——**「実装側の申告」**(v0.21。実装セッションの完了報告)と**「設計時の懸念」**(承認済み設計メモの懸念点=著者が実装前に書いた不安)。**どちらも記録として既に存在するので、著者に改めて書かせる必要が無い。** 著者の仕事は違うと思うものを消すだけ(決定ではなく検査、v0.21 §7の形)。両方無ければ行ごと落とす。diffからの生成は引き続き不可(AI一次レビューの所見と区別がつかなくなる)。

追随: flowテンプレートから 👤 項目を削除(pr-guideの段に吸収)、§9「本人の判断を要求する欄」は2つ→1つ(採らなかった選択肢の選別)、口頭確認の問いを「なぜそこが一番怪しいのか」→「なぜその案を捨てたのか」に、README「書く作業があるもの」は4個→3個、skills/README の判断一覧・4区分表を更新。

**Issue #9 決定1(v0.21)の上書きにあたる**——v0.21は「申告を下書きにし、確定は著者」、v0.22は「転記で埋め、著者は消すだけ」。1版で戻したのは実測(書かれていない)が先にあったから。

変更ファイル: standard.md(§7・§9)、skills/design-memo、skills/pr-guide(SKILL・テンプレート)、skills/flow(SKILL・テンプレート)、skills/README、README。

## v0.21: 担当を知識と判断で切り直す——実装側の申告・転記の義務化・「決めて止める」形の許可([Issue #9](https://github.com/HawkClaws/agent-first-dev-standard/issues/9)、2026-08-25)

**きっかけは一人導入の実測**(Issue #9)。実装をエージェントに委譲する前提のもとで、「著者専用」「人間が決める」とされた項目のうち担当がずれるものが1チケットで3件出た。いずれも著者が「なぜ自分に聞くのか」と止まった箇所である。共通の構造は、**欄や工程の担当は「著者か機械か」の2区分ではなく、知識と判断のどちらに基づくかで決まる**こと。skills/READMEに4区分の表を追加した。

| 種類 | 担当 |
|---|---|
| 作業から得られる知識 | やった側(=実装したエージェントの申告) |
| 記録からの転記 | 機械(生成は不可のまま) |
| 価値判断・裁定 | 人間 |
| 選択肢が実質1つの決定 | 機械が決めて根拠を記録し、人が見て止める |

### 決定1: 「一番怪しい箇所」に実装側の申告の転記を認める(§9・pr-guide・flow)

実測: 著者の反応は「実装したのはエージェントではないか」で、正当だった。欄が求める情報は2種類が混ざっていた——**作業から得られる知識**(実装してみて不安だった場所)と**レビュー配分の判断**(どこを見てほしいか)。委譲前提では前者は実装した側にしかなく、空欄で返す運用はこの情報を失うだけになる。

- 実装セッションが完了報告に残した「迷った箇所・仕様が薄く補完した箇所」を**「実装側の申告」と明記して**転記してよい。**確定(残すか・書き換えるか)は著者**のまま
- flowの委譲プロンプトに「完了報告に迷った箇所・補完した箇所を残すこと」を追加(申告の転記元を作る)
- **pr-guideがdiffから怪しい箇所を生成することは引き続き不可**——申告(作業中に実際に迷った記録)と生成(後からの推測)は別物で、AIは自分の出力を怪しいと思わない。転記の規律は「採らなかった選択肢」と同じ**出所で切る**に揃った
- 口頭確認「なぜそこが一番怪しいのか」は維持——申告をそのまま残した著者が答えられないなら、それ自体がシグナル

### 決定2: 禁止と義務を同じ強さで併記する——転記は機械の義務(§9・pr-guide・flow)

実測: 「AIによる生成・整形は不可」に引きずられ、機械が安全側に倒れて「採らなかった選択肢」の転記の可否を著者に確認し、やるべき転記まで止まった(著者の反応は「会話履歴を見て転記してほしい。なぜ聞くのか」)。**規範が禁止側だけを強く書くと、機械はやるべき作業も止める。**

- §9・pr-guide手順3に「**会話ログが参照できるなら、転記は機械の義務である——著者に可否を確認せず、候補を全部転記して提示する**」を禁止と同じ強調で併記
- flowテンプレートの項目名を「採らなかった選択肢の**選別**」に変更——著者の仕事は選別だけであることを項目名で示す

### 決定3: 選択肢が実質1つの「人間が決める」項目は、決定ではなく検査として運用してよい(§7・flow・update-map)

実測: 一人運用ではどう切っても検収先・境界の持ち主が変わらない領域決定で、著者が「決めるのはそちらの仕事ではないか」と止まった。機械が決めたところ誤った領域を選んだが、**著者が見て違和感を述べて止め、作り直された**——👤 は決定としては空だったが、**検査としては機能した**。

- §7に規範を追加: 選択肢が実質1つなら**機械が決めて根拠を記録し(「機械が決めた」と明記)、人間は選択を見て止める**。選択肢が複数に戻ったら決定に戻す
- flowの「領域を勝手に決めない」にこの例外を追記

### 副次2件

- **領域の境界を機械が見分ける手掛かりが無かった**(§11・update-map): 実測では「同じファイルを触るから同じ領域」と誤読し、既存の地図の守備範囲を不当に広げて撤回になった。手掛かり2つを明記——①その地図の `description` 1行で今回の変更も説明できるか ②更新条件が混ざらないか。迷ったら広げるより分ける
- **機械がスコープを理由に義務を先送りできた**(flow・update-map): 「新しい地図は別途起票」という提案が著者の指摘まで通りかけた。「**地図の初版は当該PRに同梱。『スコープ外なので別途起票』に逃さない**」をflowの「絶対にやらないこと」とテンプレートに追加(§12 手順2の強制点化)

変更ファイル: standard.md(§7・§9・§11)、skills/pr-guide(SKILL・テンプレート)、skills/flow(SKILL・テンプレート)、skills/update-map、skills/README、README。

## v0.20: 理解検査の器に適用範囲を入れ、成否の軸が無い変更に1問1答を許可([Issue #8](https://github.com/HawkClaws/agent-first-dev-standard/issues/8)、2026-08-25)

**きっかけは一人導入の実測**(Issue #8)。理解検査の器である契約3行(成功したら/失敗したら/前提)が、ある類型の変更で**成立しなかった**。分量が重いのではなく、**器の軸そのものが存在しなかった**。

### 実測: 32欄中31欄が空のまま止まった

契約8件 × 4欄で器を用意したところ、著者は最初の1欄で止まり、反応は「意味が分からない」だった。理由は「失敗した場合でも答えは変わらない。そもそも実行前に決まる話だ」——**参照先の選択は処理の実行前に確定する**ので、以降の処理が成功しようが失敗しようが選ばれたものは同じである。作り直しは2段階になった。

| | 欄数 | 何をしたか |
|---|---|---|
| 初版 | 32 | 契約8件 × 4欄 |
| 2版 | 13 | 契約3件 × 3欄 ＋ 不変条件4件 × 1欄 |
| 3版 | **7** | **全問1行**(成否の軸を捨てた) |

2版でも著者は止まったままだった。**欄を減らしても書けるようにならず、成否の軸を捨てて初めて書ける形になった。** 後から初版の8件を見直すと、成否で分かれるものは1件も無かった。

これは [Issue #1](https://github.com/HawkClaws/agent-first-dev-standard/issues/1)(v0.13で対応)とは別の指摘である。あちらは**分量**の話で、こちらは**形**の話——「軽くする」方向の改訂では拾えない。

### 決定1: 契約3行の適用範囲を明記し、成否の軸が無い変更には1問1答を許す(§4・crosscheck手順U-1)

契約3行は§2の「この入力を渡せば、この出力が返る。失敗するときはこう失敗する」=**呼び出しの約束**の形から来ている。**入出力の契約を変えず、処理が参照するデータの出どころだけを変える変更**(設定値の取得元をコード同梱から設定ストアへ移す/どの定義セットを使うかを呼び出し側から選べるようにする/参照先が無いときは同梱の既定へ倒す)では、参照先が実行前に確定するため成否と直交する。**分かれるのは状態であって成否ではない。**

- 判定の問いは1つ:「**失敗したら、この答えは変わるか**」。変わらないなら成否は軸ではなく、**1問1答**(仕様の言葉で書いた問い1つに1行で答える)を器にしてよい
- 1問1答では、成否の代わりに**状態で分かれる点**を問いにする(どの出どころを見るか/見つからないときどこへ倒れるか/切り替えの単位は何か/切り替えても変わらないものは何か)。**「仕様に書かれていない点」は器を替えても残す**——仕様の穴の検出はここでしか出ないため
- 採った器はPRのテスト欄に1語で残す(器: 契約3行/1問1答)。替えた事実が次の改訂の材料になる

### 決定2: 空欄が「器の不一致」か「理解不足」かの判別を入れる(§4・crosscheck手順U-3)

失格条件の「書けない欄」(v0.12)は理解不足・仕様の曖昧さのシグナルだが、**器が構造的に合っていないときも見た目は同じ**になる。判別は空欄の理由が揃うかで行う——**全契約が同じ理由(「その軸が存在しない」)で空くなら器の問題**で器を替える、契約ごとに理由が散るなら理解の問題で仕様・設計メモに戻る。器の不一致を著者の理解不足として扱うと、**検査そのものが止まる**(実測がまさにそれだった)。

### 決定3: 不変条件の変換例を開いた形に統一する(§4・crosscheck共通節)

§4は「不変条件は『その状況が起きたらどうなるか』の問いに変換して出す」(v0.13)と定めているのに、**crosscheckに並ぶ2つの変換例は片方が「どうなるか」で開き、もう片方が「〜は入るか」で閉じたYes/No形**だった。実測では、変換した側が閉じた形を真似て問いを「〜か」で終わらせ、書き手にYes/No質問として読まれて器に載らなかった——**§4が警告している失敗を、規範の直後の例示が自分で誘発していた**。

- 閉じていた例を「他テナントの行は**どう扱われるか**」に書き換え、2例とも開いた形に統一
- **「問いをYes/Noで答えられる形にしない」**を規範として1行追加(「どうなるか」「何が起きるか」「どう扱われるか」で開く)。**片方でも閉じた形を混ぜると、閉じた方が真似される**ことを明記

変更ファイル: standard.md(§4)、skills/crosscheck(共通節・手順U-1・U-3・U-5)、skills/pr-guide(SKILL・テンプレート)、README。

## v0.19: 整合性修正——v0.12〜v0.18の連続改訂で割れた箇所21件(2026-08-24)

7版の連続改訂の後、全ファイルの整合を敵対的にレビューさせた結果(高2・中8・低11)への対応。共通する型は**「規範を変えたのに、それを強制・実行する側の条文が追随していない」**。

### 高1: マージゲートの空洞化(§8・flow・skills/README)

v0.12でテスト欄が「独立照合: 未実施/人間版/自動版」から「検査: 未実施/**理解検査(著者)**/独立照合・人間版/自動版」に拡張された際、マージ確認の条文が「独立照合が『未実施』でないこと」のまま残った——**不可逆系のPRで「理解検査」を選ぶと文字列が「未実施」でないためゲートを通ってしまい**、「理解検査は独立照合を代替しない」(§4)が唯一の強制点で緩んでいた。3箇所を「検査欄が『独立照合・人間版/自動版』のいずれかであること」に修正。あわせて**v0.17の「出力との突き合わせ」(必須)がどの強制点にも接続されていなかった**問題(中8)も同じ検査に追加: 計測・集計・エクスポート系なら「対象外」でないこと。pr-guideに該当判定と「未実施」明記の規定を追加。

### 高2: 聞き取りの汚染ルールとflowの単一セッション設計が両立しない(flow・crosscheck・§4)

crosscheckは「聞き取りをしたセッションに実装を委譲しない」と定めたが、人間と対話できるのは**メインセッションだけ**であり、聞き取りを別セッションに置くことはそもそもできない。分離の向きを逆にした——**分離するのは聞き取りではなく実装**:

- 聞き取りはメインセッション(構造上そこにしか置けない)
- **実装はサブエージェントに委譲し、委譲は期待値の書き出しより先に行う**——委譲プロンプトを書いた時点で期待値が存在しなければ、「渡さない」が指示ではなく構成で保証される。逆順になったら「独立性は指示ベース」と記録(独立照合の既存の二段表記と同じ規律)
- **照合メモはリポジトリの外に置く**(実装サブエージェントはリポジトリを探索する)
- この順序で実装と期待値の書き出しが本当に並列になり、「実装と並列」がflowの逐次進行で直列化していた問題(中7)も同時に解消

flowに「セッションの分離」節を新設、テンプレート§2を「委譲が先・期待値が後」に並べ替え。

### 中(残り)

- **「書けなかった欄」の記録先の消失**: 理解検査の失格条件の筆頭なのに記録場所が「確認してほしい項目」節の内側にあり、v0.18の「Aが0件なら節ごと落とす」で一緒に消えていた → テスト欄1行に「書けなかった欄 ___ 件」を追加
- **flow §3の順序**: pr-guideが突き合わせより先に走り、存在しない件数を書く手順になっていた → 突き合わせ→pr-guideに入れ替え
- **40字上限と逐語転記の衝突**: 整形禁止(v0.14)の下で40字に収める手段がなかった → 逐語転記は上限適用外(長文は details)と明記
- crosscheckのdescriptionがv0.18前の「無条件でPR欄に転記」のまま → 種別で行き先を分ける記述に更新
- README「全9個」→実数の10個、/flow「最大8回」→10回(v0.12のゲート増に未追随)

### 低(11件)

§12手順番号の参照ズレ(update-map)、§9への誤参照、不可逆系の列挙の表記統一(スキーマ変更・データ移行・課金・外部送信・削除)、用語表の理解検査定義にAI一次レビューを追加、語彙置換で対応が読めなくなった不変条件例に出所を補完、「コード修正ではなく」→「挙動の修正ではなく」(Bの行き先「コードへの反映」との字面衝突)、§3に検収の最低ラインへの導線、テンプレの「(上の欄)」注記、flowテンプレの節参照の曖昧さ、README実測表に理解検査の行、skills/READMEフロー図に突き合わせ2種を追加。

変更ファイル: standard.md(見出し・用語表・§3・§4・§8)、skills/flow(SKILL・テンプレート)、skills/crosscheck、skills/pr-guide(SKILL・テンプレート)、skills/update-map、skills/README、README。

## 実測記述の語彙を架空題材に一般化(版番号なし・2026-08-24)

v0.12〜v0.18で実運用の知見を反映した際、実測の例示に運用元の内部語彙が数カ所残っていた(内部コンポーネント名・実在の契約名・機能構成が推測できる不変条件の組み合わせ)。READMEのContributing規定「組織が識別できる情報を書かない」に従い、該当箇所を架空題材(注文・テナント・レポート生成・上流サービス)に置き換え、分布や行数の生の実測値は結論を変えない範囲で丸めた。**以後、実測を記録するときは、固有の語彙を逐語で例示せず、架空題材に写してから書く**(逐語転記の規律は照合メモ・PR欄という当事者間の文書のためのもので、公開文書の例示には適用しない)。

## v0.18: 種別Bの既定の行き先をPR本文からコード・地図への反映に変更([Issue #7](https://github.com/HawkClaws/agent-first-dev-standard/issues/7)、2026-08-24)

**きっかけはv0.16適用後の実測**(Issue #7、#5の続報)。v0.16の様式(時制ラベル・前置き・A/B分類)を公開PR 3本に適用しても、レビュアーには「なぜこれがPRにあるのか」が伝わらなかった。前置きで「指摘リストではない」「直すべき箇所ではない」と**否定を2回重ねること自体が、欄の存在理由を説明できていないことの表明**になっていた。

### 分析: 種別Bは構造的にPRの判断材料にならない

1. Bは**著者の内面の記録**であって、レビューの判断材料ではない
2. 「他の人も誤解する」の根拠が著者1人のサンプルしかない
3. v0.16が定めたレビュアーの行動(コメント・地図・命名の改善を求める)は、**著者が先にやればよい**——レビュアーに頼む筋合いがない

v0.15の「PR説明の欄はレビュー時点でしか効かない。将来の参照は地図かコードコメントへ」とも整合する——Bの内容は将来の読者向けの情報であり、まさに地図・コメントの領分だった。

### 決定: 行き先を種別で分ける(§4・crosscheck手順U-5・pr-guide・テンプレート)

- **種別A(仕様が2通りに読めた)→ 従来どおりPRの「確認してほしい項目」欄へ。** 裁定が要るので判断の経緯が残る価値がある。欄はA専用になり、「指摘リストではない」の二重否定の前置きが不要になった(Aは本物の裁定依頼である)。**Aが0件なら節ごと落とす**
- **種別B(著者が知らなかった)→ 既定はコード・コメント・地図(Level 2〜3)への反映。** PRに現れるのは反映の差分だけ。反映先を探して既に書かれていたなら重複で何もしない(それも確認の成果)。どこにも書かれていないBだけが「コメントを足すべき」シグナル——**種別Bの価値は、PRに並べることではなく反映先を探させることにある**
- **実施記録はテスト欄1行**(段・版・A/B別の件数・一致の件数)。実態より強いラベルを避ける規律は維持

### 実測(判断の根拠)

全件が種別BだったPR 3本で節ごと落とすと、3本とも本文が2〜3割縮んだ。落とした内容のうち3件は**既にコードのコメントか地図に書かれていた**(=PR本文にあったのは重複)。残る1件はどこにも書かれておらず、「コメントを足すべき」シグナルとして機能した。なお、これは「空欄が続いたら削除」の回数判定ではなく構造の指摘であり、1回の実測で採用した。

変更ファイル: standard.md(見出し・§4)、skills/crosscheck(手順U-5)、skills/pr-guide(SKILL・テンプレート)、skills/flow(テンプレート)、README。

## v0.17: 「出力との突き合わせ」を新設——計測・集計系の変更は実データの分布を見てから通す(2026-08-24)

**きっかけは一人導入の実測**(計測用の列挙型列とエクスポート列を追加する変更。報告Issueは記載情報の都合で削除済み——内容はこの項に一般化して残す)。理解検査で差2件(いずれも種別B)を処理し、テストも通った**あと**、検証環境の実データを1本エクスポートして読んだところ、既存のどの検査でも原理的に出ない欠陥が見つかった:

- 「稀な異常」としてコードに文書化された列挙値が、実データでは**全体の数割を占める通常経路**だった
- その列で成功率を出すと、分母の取り方で数十ポイント変わる。**値は正しく、嘘は書かれていない。壊れていたのは、値を読んだ人間が引く結論だった**
- 理解検査・独立照合・テスト・地図はすべて「期待値↔実装」または「記述↔コード」の突き合わせであり、実装が設計どおりである以上、何度回しても出ない。見つかったのは実データを読んだからで、見えたのは値の正しさではなく**分布**だった

§4末尾の「AIの間違いは仕様・コンテキストの層に移動している」よりさらに一段外側——**成果物の受け手の解釈**の層の欠陥である。

### 決定: §4に「出力との突き合わせ」を新設、梯子に1行追加

**規範: 計測・集計・エクスポート系(記録された値を人間が読んで判断する成果物)の変更は、実データを1本通し、値の分布と代表的な集計(分母を明示)を実際に出してから通す。** 要点:

- 見るのは正しさではなく**分布**(稀な異常として設計した値が多数派になっていることは珍しくない)
- 通すのは**計測の利用が始まる前**(誤った分母の集計・アラートは「守るべき回帰」として保護される——裁定前テストへの書き戻しと同じ構造)
- **現れなかった値は未検証として列挙して残す**(実測でも、理解検査で扱った設計1件が実データでは一度も観測されていなかった)
- 食い違いへの行動は種別Bと同じ(命名・コメント・地図の修正。値の意味を変えるかは分布を見た後の判断)
- 適用範囲は「読み手が値を解釈して判断する成果物」に限定(全変更に課すと形骸化する。§12)。書き手は著者でよい——分布は解釈の余地が小さく、自分の読みを正解にできない。コストはエクスポート1回分

PRテンプレートのテスト欄に実施記録の行(データ範囲・観測された値と件数・現れなかった値)、flowテンプレートのPR作成段に項目を追加。

### 副産物: standard.md の見出し版がv0.11のまま5版分ずれていた

freshnessを説く文書の本体の版表記が古いのは自己矛盾なので、v0.17に追随させた。**以後、見出しの版はリリースごとに更新する**(このCHANGELOGの版番号と常に一致させる)。

変更ファイル: standard.md(見出し・用語表・§4)、skills/pr-guide(テンプレート)、skills/flow(テンプレート)、README。

## v0.16: 「確認してほしい項目」の反転を様式で抑える+AI利用の開示欄を内部開発から削除([Issue #5](https://github.com/HawkClaws/agent-first-dev-standard/issues/5)、2026-08-24)

**きっかけは公開PR 3本での実測**(Issue #5)。理解検査の転記を、基準を何も知らないレビュアー役(別セッション)に読ませたところ、**「確認してほしい項目」欄が本文中の最大リスクと判定された**——同じ内容が、読み手の前提によって「検証の起点」にも「著者が実装を理解していない証拠」にも反転する。v0.13・v0.14で強化した逐語性の規律が、断片的な回答をそのまま公開の場に出すことでこれを強めていた。欄の効果自体は実測で確認済み(差が複数件、うち1件は中心仕様)なので、**転記は止めず、書き方と掲載先を様式で規定する**(§9の「構造で抑える」と同じ発想)。

### 決定1: 反転を様式で抑える(§4・pr-guide・テンプレート)

- **時制をラベルに固定**:「着手前の予想(YYYY-MM-DD時点)」。「著者の認識」のような現在形の名詞は、拾い読みで「今もそう思っている」と読まれる
- **指摘リストではない旨の前置き**を欄の冒頭に固定文で置く
- **差だけ本文に出し、一致は `<details>` に畳む**(見てほしい差を埋もれさせない)
- **件数はそのPRの列挙数と一致させる**(実測: 複数リポ変更で「全体の件数」を書き、3本すべてで数字が本文と不一致になった)

### 決定2: 差をA/Bに分け、レビュアーの行動を様式で定義する(§4・テンプレート)

導入者の疑問「これを見てレビュアーはどうするのか」に対し、差の種別で行動が違うことを明記:

- **A: 仕様が2通りに読めた** → 裁定の対象。コードを見る価値がある
- **B: 著者が単に知らなかった** → 実装は設計どおりで裁定するものは無いが、**著者が誤解したことは他の人も誤解する証拠**。行動はコードの検査ではなく、コメント・地図(Level 2〜3)・命名の改善要求

行動が定義されない欄は読み飛ばされ、「著者の無能の開示」だけが残る。種別の提案は機械がしてよいが、確定は著者(Aは裁定対象になるため)。

### 決定3: 掲載先は選べる(§4・pr-guide)

差の詳細は当事者間で閉じた場所(レビュー依頼のメッセージ等)に置いてもよい——著者の誤読を公開の場に出すコストは組織の文化に依存し、基準はそこまで決めない。ただし実施の記録(段・版・件数)はPRのテスト欄に必ず残す(実態より強いラベルを避ける既存規律の維持)。

### 決定4: AI利用の開示欄を内部開発から削除する(§8・テンプレート)

この基準は実装のエージェント委譲を前提にしているため、チーム内部ではこの欄が**毎回同じ値になる**(実測: 3本とも同値。レビュー判断に使えない)。毎回同じ値の欄は情報を持たず「偽のHITLポイント」であり、今回は理解検査の欄と隣接して**「AIが書いたものを著者が把握していない」という合成解釈の材料**にすらなった——情報量ゼロの欄が害だけを持った。§8が引くFedoraの先行例は**外部貢献**への開示の文脈であり、置き場所を分ける:

- **外部貢献を受けるリポジトリ**: CONTRIBUTINGとコミットトレーラー(`Assisted-by:`)で開示を求める
- **チーム内部**: PRごとの欄は置かず、**リポジトリ単位で一度宣言**(README/CONTRIBUTINGに「実装は原則エージェントに委譲し、著者が検証する」)。責任の所在は開示欄ではなく§3(著者は自分)とマージ確認責任で固定されている
- 「開示はレビューの深さを変える材料ではない」は明文で維持

### 決定5: 公開前の部外者読み直しを手順に追加(pr-guide手順6)

今回の検査手法(基準を知らない読み手として読み直す)自体を正規手順にした。エージェントに読ませるなら実装とは別セッション・基準を渡さない。見るもの: 社内用語が定義なしで並んでいないか(初出に基準へのリンク)、注記が欄の価値を打ち消していないか(実測:「自己申告では確定しない」がレビュー指定表を読む意味を消していた——スキル内の著者向け注記をPR本文に転記しない)、無能の開示に反転して読める箇所がないか。

変更ファイル: standard.md(§4・§8)、skills/pr-guide(SKILL・テンプレート)。

## v0.15: 欄・工程の「有効範囲」を明記——効く期間・空でよい条件・検収の最低ライン([Issue #4](https://github.com/HawkClaws/agent-first-dev-standard/issues/4)、2026-08-24)

**きっかけは一人導入の実測の続き**(Issue #4)。4点に共通する構造は「その欄・工程が何のために存在し、**いつ効かなくなるのか**」が書かれていないこと。基準は各工程が何をするかは書いていたが、有効範囲を書いていなかったため、導入者が毎回それを判断させられていた——一人導入ではその判断コスト自体が摩擦になる。

### 1. PR説明の欄はレビュー時点でしか効かない(§9)

エージェントが「一番怪しい箇所は半年後の障害時に一次容疑者になる」と説明し、導入者に「PRのdescriptionを半年後に読むか?」と否定された。そのとおりで、PR description は検索されず、到達経路は `git blame` →コミット→PR(「この行がなぜこうなったか」専用)。§9に明記: **この欄はレビュー時点で効く。将来の参照に残したい事実は地図(§11 Level 3)かコードコメントに書き戻す。「PRに書けば将来の資産になる」という説明は誤り。**

### 2. 「レビュアーへの質問」は設計メモが効くほど空になる(§9・テンプレート)

導入者の指摘「合意を取ってからPRを出すのが普通では」は正しい——§7の先払いが機能しているほど方針レベルの未決は残らず、残るのは実装の細部だけ。テンプレートに「無ければ空でよい」を明記し、§9に「空が続くなら偽のHITLポイント判定(skills/README)に回して削除を検討」を追加。

### 3. 「自分で足したケース」はカバレッジの話ではない(テンプレート・crosscheck)

導入者の反応「カバレッジ100%なら書くものが無いのでは」は§10が警告している混同(カバレッジは「実行された」しか測らない)を欄名が誘発していた。テンプレートに「カバレッジの話ではない。**突き合わせが終わった後に**、誰も思いつかなかったケースを1〜2個」を明記。crosscheck側にも順序を明記(見る前に書くならそれは理解検査であり、この項の目的——両解釈の外のケース——とは別物)。

### 4. 地図の初版検収に最低ラインと機械検証を導入(§11・update-map)

**実測**: 「さらっと読んで無修正」で検収を通過した初版に、別セッションの機械検証で**誤り6件**(不変条件2・危険領域2・Level 0構成2)。「このデータは削除されない」と書かれていたが実際は削除経路が2つあった——障害時に「消えないはず」で切り分けると確実に誤る種類の誤りが通過していた。「無修正の受け入れは読まれていない疑い」という基準の警告はそのまま当たったが、一人導入で200行の全通読は「守られない項目」(§12)に育つ。対応:

- **検収の最低ライン = Level 2(不変条件)・Level 3(危険領域)の通読・修正**。障害時の切り分け根拠はここで、Level 0・1 の誤りは実害が小さく月イチ矛盾検査でも拾える
- **機械による事実検証の併用を許可**: 別セッションのエージェントが各主張をコードと突き合わせ「誤り/疑わしい/確認済み」に分類(モード3と同じ機構。生成セッション自身は不可)。**検証は検収の代替ではない**——`reviewed` を書くのは人間だけ、は維持
- 「疑わしい」で確定できない記述は**消さずに「(未確認)」と明記して残す**(消すと同じ調査が繰り返される)

変更ファイル: standard.md(§9・§11)、skills/pr-guide(テンプレート)、skills/crosscheck、skills/update-map(モード1手順3)、skills/flow(テンプレート)、README。

## v0.14: 「採らなかった選択肢」の禁止を出所で切る——著者本人の発言の逐語転記を可に([Issue #3](https://github.com/HawkClaws/agent-first-dev-standard/issues/3)、2026-08-24)

**きっかけは一人導入の実測**(Issue #3)。導入者から「採らなかった選択肢は会話履歴から自動でやってくれるものだと思っていた——AIの提案を打ち返したところは、すでに会話の中で自分で考えている。転記するだけではだめなのか」。実際にセッションログを調べると、**捨てる判断が本人の発言として逐語で残っていた**(そのチケットで4箇所)。

### 分析: 2つの著者専用欄は「埋められない理由」が違う

- **一番怪しい箇所**: AIが埋められる情報が**そもそも存在しない**(AIは自分の出力を怪しいと思わない)→ 逐語転記の例外も無い
- **採らなかった選択肢**: 著者の発言として**記録に存在しうる**。禁止が想定していたのは「AIが生成した案の水増し」(§9)と「AI下書きの機械転記」で、本人の発言の転記はその射程に入っていなかった

v0.12が理解検査に聞き取り方式(答えをそのまま書き留める=転記であって生成ではない)を正規手段として認めた以上、同じ規律が適用できる欄をこちらだけ禁じるのは不整合だった。

### 決定: 出所で切る(§9・pr-guide・flow)

- **可**: 著者本人の発言(会話ログ・設計メモの著者記述部分・口頭の聞き取り)からの**逐語転記**。転記の旨と出所(いつの会話か)を欄に明記
- **不可**: AIが生成した案の列挙、設計メモのAI下書き部分からの転記、**整形・言い換えを伴う転記**(整形した時点で著者の言葉ではなくなる)
- **選別は著者がする**: 候補が複数あれば機械は全部提示し、どれを載せるかは著者が選ぶ(選別が判断そのもの)
- 会話ログが無い・拾えない場合は従来どおり空欄で返す(著者が書く、に落ちるだけ)
- 出所の判別と逐語性は機械検査できないため、実効性は引き続き口頭確認(§9)に置く

変更ファイル: standard.md(§9)、skills/pr-guide(SKILL)、skills/flow(テンプレート)、README。

## v0.13: 理解検査の器を独立照合から分離——契約3行・仕様の言葉・不変条件の変換・選択式の規律([Issue #1](https://github.com/HawkClaws/agent-first-dev-standard/issues/1)、2026-08-24)

**きっかけは v0.12 の実測**(実チケット1本・複数リポジトリにまたがる・DBスキーマ変更あり、Issue #1)。器(4欄の箇条書き)が一人導入で最初に折れる箇所だった——ただし**検査自体は機能した**(10契約中、著者の理解と実装の食い違い4件、うち1件はチケットの中心仕様。後付け適用=盲検が崩れた状態でこの検出数であり、「著者の期待値にも部分的な検出力が残る」の傍証)。

### 実測で折れた箇所と対応

1. **契約名が実装語彙(内部イベント名まじりの `C1 〜の受領(値あり)` 形式)で著者が着手できなかった** → **契約名は仕様の言葉で書く**(「レポート生成が成功したとき、何が残るか」の形)を手順U-1・A-2に明記。機械が埋めてよい唯一の欄なので、ここが実装語彙だと検査全体が止まる
2. **不変条件が4欄に載らず、著者が「入力: はい」とYes/No質問として読んだ** → **「その状況が起きたらどうなるか」の問いに変換して出す**を共通節に追加(「同じ注文が2回記録されない」→「保存が2回走ったらどうなるか」)
3. **「入力」という欄名が対象の言い換え(`入力: 上流サービスからの結果` のような)を誘発した** → 理解検査の器を4欄から**§2の契約3行(成功したら/失敗したら/前提)**に変更

### 決定1: 器を分離する(§4・crosscheck手順U)

4欄は独立照合(第二解釈者が期待値を**生成**する足場)には妥当だが、理解検査が求めるのは著者の理解の**出力**=「契約を自分の言葉で言えること」であり、それは§2の契約3行そのものだった。「仕様に書かれていない点」は任意欄に落とす。PRの「確認してほしい項目」欄も3行形式に追随。

### 決定2: 選択式は条件付き・名乗りの規律(§4・crosscheck手順U)

導入者からの「自由記述が重いので選択式にできないか」に対し、単独では不成立と判断——**再認は再生の代わりにならない**(§3のPRゲートが求めるのは後者)、**選択肢を作った側の読みの外から食い違いは出ない**(「仕様に記述なし」が構造上出てこない。選択肢の元が実装なら自作自演が戻る)。条件付きで許可: ①選択肢は仕様から作る ②「上のどれでもない」「わからない」を必ず入れる(選ばれること自体が検査結果)③選んだ理由を1行だけ自分の言葉で。**理由なしの選択式のみは「理解度チェック(選択式)」と別表記**(版表記と同じ規律)。

変更ファイル: standard.md(§4)、skills/crosscheck(共通節・手順U-1・手順A-2)、skills/pr-guide(テンプレート)、README。

## v0.12: 理解検査を標準に——検査の既定の書き手を「レビュアー」から「著者」へ(2026-08-24)

**きっかけは一人導入の質疑2点。**「期待値の書き出しをレビュアー既定にするのは求めすぎではないか。一人で導入したい場合の障壁になる」、そして逆側から「レビュアーに全部渡すと、**実装者(著者)が仕様を理解しないままマージまで行ける**のが怖い」。

### 分析: 期待値の書き出しには2つの機能が混ざっていた

1. **検出機能** — 別人の解釈をぶつけて仕様の曖昧さを炙り出す。非著者でないと成立しない
2. **理解の担保** — 著者が契約を自分の言葉で言えることの証明。**著者がやらないと意味がない**

旧§4は1に全振りし、2はPRの「一番怪しい箇所」欄と「エッジケースを1〜2個考えろ」に押し付けていた。エージェント前提開発の最大リスクは「仕様が2通りに読めた」より手前の「著者が成果物を理解しないまま出す」なので、2を標準の検査に据えるべきだった。

さらに、団体戦の理屈(「方針を書いた人=コードを書いた人」)はエージェント前提では崩れている。**コードを書いたのはエージェントであり、著者の期待値にも部分的な検出力が残る**——設計メモに書いた読みは共有されて盲点になるが、メモに書かなかった細部(エッジケース・失敗時挙動・境界値)では普通に食い違う。

### 決定: 梯子の再定義(§4)

| 段 | 対象 | 検査 | 書き手 |
|---|---|---|---|
| 標準 | 構造理解以上の全変更 | **理解検査**(新設) | 著者 |
| 昇格(必須) | 不可逆系 | 独立照合 | 実装方針を書いていない人 |
| 任意 | 契約理解レベル | (テストの回帰固定で足りる) | — |

- **名前を分けた。** 著者の期待値書き出しは独立照合を名乗らない(ラベルが実態より強く読まれるのを防ぐ。自動版の「独立性は指示ベース」表記と同じ規律)。用語表に「理解検査」を追加
- **失格条件を明確にした**: 書けない欄があること=理解不足か仕様の曖昧さ。エージェントに埋めさせた時点で検査は消える
- **汚染ルール**: 書いた期待値をエージェントの入力に入れない(設計メモ経由で読ませると、期待値に合わせた実装になり一致がトートロジーになる)。聞き取り方式では、聞き取りをしたセッションに実装を委譲しない
- **成果指標の差し替え**: 著者 vs エージェントでは一致して当然。成果は「書けなかった欄」「仕様に記述なし」「食い違い」の件数であって一致件数ではない
- **PRテンプレートに「確認してほしい項目」欄を新設**(pr-guide): 照合メモと突き合わせ結果の転記先。レビュアーはこの欄を起点に著者の読みごと検証できる。pr-guideは転記のみ可・新規生成は禁止欄に追加
- **不可逆系の独立照合は一切緩めていない。** 理解検査は独立照合を代替しない(著者は自分の読みを自分で正解にできる)

### 決定: 人間が埋める欄の器はMarkdown表をやめる

人間の直接編集にMarkdownの表(特にセル内編集)は重い。期待値メモとPRの「確認してほしい項目」欄は**契約ごとの箇条書き**に変更し、**聞き取り方式**(エージェントが1問ずつ質問し、答えをそのまま書き留める)を正規の記入手段として明記した。聞き取りは転記であって生成ではない——人間が言っていない期待値を補完の名目で足さない。答えられない欄は空欄のまま「書けなかった」と記録する(それ自体が結果)。

変更ファイル: standard.md(用語表・§4)、skills/crosscheck(手順0の三段化・手順U新設・手順A-2の器変更)、skills/pr-guide(SKILL・テンプレート)、skills/flow(テンプレート§2〜4)、skills/README、README。

## 地図の様式に `type` キーを追加(版番号なし・2026-08-23)

v0.11 で「OKFは互換に留める。使いたければ `type: Domain Map` を1行足せばよい」と記録したが、**その1行を各自の運用に委ねると、キーが有る地図と無い地図が混在する**——互換は「いつでも渡れる」ではなく「地図によって渡れたり渡れなかったりする」になる。様式に入れるかの判断材料:

- 今の重要度は低い——基準の機構で `type` を読むものは無く、パス(`docs/map/`)が既に識別の役割を果たしている
- 一方でOKF互換の実効性はこの1行が全て(OKFの必須キーは `type` のみ)
- 決定打は**死文化リスクがゼロ**であること。固定値で更新義務が無いので、`reviewed` と違って「守られない項目」になりようがなく、§12の削除文化と衝突しない

よって様式に入れた(update-map・§11)。あわせて4キーそれぞれの意味と更新規則を update-map に明記した。OKF準拠は引き続き名乗らない。

## v0.11: 敵対的検証で見つかった§11の穴2件と引用の自己矛盾を修正、OKF検討の記録(2026-08-23)

**きっかけは一人導入の質疑。**「マルチリポのとき、システム全体の Level 0 はどこに置くのか」への著者側AIの回答(呼ばれる側リポの `docs/map/README.md` を正本にし、他方はリンクのみ)を、反証専任のサブエージェント3体に検証させたところ、**回答の側が反証された**。同時に基準側の欠落も判明した。(注: v0.9の表記ルールに従い、検証の独立性は指示ベース——全て単一著者が起動した同一ベンダーのエージェントである)

### 反証されて撤回した案(記録)

「呼ばれる側リポを全体 Level 0 の正本にし、呼ぶ側はリンクのみ」は成立しない:

- 呼ぶ側のPRでも Level 0 の内容は変わる(呼び始める・やめる=辺の増減)のに、正本が別リポでは**「地図の更新は当該PRに同梱」を満たす手段が物理的に無い**
- 呼ぶ側リポから見た「別リポの正本+リンク」は、§11の根拠が実証データ付きで排除した「別管理のWiki」と構造的に同一
- Webhook・外部SaaS呼び出し・バッチが混ざると「呼ばれる側」が一意に決まらない。§8の条文は契約を**裁く人**の規定であって、ファイルの**置き場**の規定ではない(所有と配置の混同だった)

### 修正1: 「領域はリポジトリをまたがない」を明文化(§11・update-map)

領域=オーナー担当単位という定義上、リポをまたぐ切り方は正規に読めるが、地図ファイルは片方のリポにしか置けず、他方のリポのPRが同梱ルールを満たす手段を失う(first-review 項目6が**正しい切り方でも恒久警報を吐く**)。マルチリポでは**各リポが自リポ視点の Level 0 を持つ**(重複を恐れない)。単一の全体ファイルは作らず、契約の実物(OpenAPIスキーマ等)は呼ばれる側リポに置いて双方から参照。両リポの Level 0 間の齟齬は月イチ矛盾検査の対象に含める。

### 修正2: 矛盾検査の片側盲点(update-map モード3)

境界をまたぐ契約は呼ばれる側リポに載るため、検査が「呼ばれる側のコード」としか突き合わせず、**呼ぶ側の実際の呼び方が恒久的に検査圏外**だった。呼ぶ側の領域の検査に「自コードの呼び方 vs 参照先の契約」を追加した。

### 修正3: §11のg3doc引用の自己矛盾

v0.10で足した根拠節が、g3docの「コードの隣に置くことで事実上の所有権が確立する」を引用しながら、仕様は地図をコードの隣に置いていなかった——**採らないメカニズムの効能を自説の根拠として引用していた**。引用を落とし、借りたもの(リポ同梱・レビュー経由・freshness)と借りないもの(隣接配置)を明示。領域がディレクトリ部分木とほぼ一致するリポでは、隣接配置バリアント(Level 1〜3 をコード近傍、中央は Level 0 とインデックスのみ)も両立すると追記した——「中央が常に正しい」は過剰一般化だという反証を受け入れた。

### 記録: OKF(Open Knowledge Format)は「互換」に留める

Google Cloud の [OKF v0.2](https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md)(Markdown+YAML frontmatter の知識表現仕様)への準拠を検討した。trustファミリー(`generated`=書いた者 / `verified`=確認した者の分離、`human:`/`process:` のactor規約、`stale_after`)は§11の設計思想と一致し、語彙は本基準の `reviewed` より細かい。それでも**準拠しない**:

- (a) frontmatterの意味論だけでなく**ディレクトリ規約ごと追従義務を負う**——`index.md`/`log.md` が書式指定付き予約名、予約外の全 `.md` に frontmatter+`type` 必須、など
- (b) 仕様が若い(2026年6月公開・1.0未満。0.1→0.2で既に破壊的変更2件)
- 互換は1行で足りる: 地図に `type: Domain Map` を足せばOKFリーダーから読める(未知キーの保持はconsumer側の義務なので `owner`/`reviewed` は生きる)
- 再検討条件: OKFが1.0に到達したとき、またはチームの実ツールがOKFを読むようになったとき。そのとき `reviewed` → `verified`+`stale_after` へのマッピングを再検討する
- 教訓: 二次情報(okf.md)は「v0.1 Draft」と書いており、一次ソースは v0.2 だった。**仕様の判断は一次ソースで行う**(今回それで評価が1回変わった)

## v0.10: 地図をコミットする根拠・先行例を§11に明記し、freshnessメタデータを様式に追加(2026-08-23)

**きっかけは一人導入の想定問答。** チームが合意していない段階で `docs/map/` がPRに同梱され始めたとき、最初に聞かれる質問は「なぜドキュメントをリポジトリにコミットするのか。Wikiや個人メモではだめなのか」である。基準はこの置き方を主張していたが、**根拠と先行例を示していなかった**——「地図の維持を善意に頼らせない」と書きながら、その設計判断が何に基づくのかは書き手の頭の中にしかなかった。

### 追加1: §11に「根拠と先行例」節

答えの核は1行——**コードと同じPRで更新されるドキュメントだけが、鮮度を保てる**。先行例4件(Google g3doc / Spotify Backstage `catalog-info.yaml` / AGENTS.md / SREプレイブック)と、反対側の実証(ADRの900超リポジトリ調査で約50%が5件未満で死文化。arXiv:2604.27333)を明記した。

書き方の規律として守ったこと:

- **反対側の実証を先行例と同格で置いた。** ADRは「docsをコミットする」文化の先行例であると同時に、善意ベース保守が死ぬことの最良のデータ源である。都合のよい側だけ引くと、この基準自身の反証可能性の主張と矛盾する
- **「この形そのものの実証は存在しない」と明記した。** Level 0〜3 をAI生成・人間検収・PR同梱で回す組み合わせにRCTは無い。構成要素の先行例+失敗モード側の実証に基づく設計判断だと書き、検証は§12の運用に接続した
- **Googleの「MTTR約3倍」はGoogle自身の推定値でありRCTではないと注記した**(出典: SRE本・序章)

### 追加2: 地図ファイルの freshness メタデータ(g3doc式)

各地図ファイルの冒頭に `description` / `owner` / `reviewed` を置く様式を §11 と skills/update-map に追加した(『Software Engineering at Google』10章の freshness 運用に倣う)。

- `reviewed` は**人間が検収した日**。書き換えてよいのは初版検収と矛盾検査の齟齬判定後だけで、スキルは下書きに `(未検収)` と書き、勝手に日付を入れない
- 動機は死文化対策のコスト削減。「地図が生きているか」が善意でも記憶でもなく**日付のgrep(CI 1行)で機械判定できる**ようになる。月イチの矛盾検査が発火しなかった事実も、古い `reviewed` として自動的に露呈する
- 「検査せずに日付だけ進める」偽装は update-map の禁止事項として明記した(「未検査」と「検査したが齟齬なし」の区別は既存ルール。日付はその区別を壊す新しい経路になり得るため)

規範の削除はゼロ。追加は説明節1つと様式1つで、地図の分量上限(1領域200行)にヘッダー4行が食い込む影響は無視できる範囲とした。

## §12型の削除3件目: 地図を先に用意する工程をやめた(版番号なし・2026-08-23)

**きっかけは「design-memo が生成されるなら着手時点で地図いらなくない?」という指摘。調べたら正しかった。**

### 着手の時点で地図を消費する工程は1つも無かった

- **独立照合の材料は設計メモの「契約」欄に移してある**(数時間前に「地図に先に書かない」と決めた)。地図が着手で必要だった唯一の理由は、その移動で消えていた
- 直前に私が主張した「**変更前の基準線を固定するため**」は成り立たない。`変更前の地図` `旧版の地図` `地図の差分` を全文検索して**0件** — **変更前後の地図を比較する工程がどこにも存在しない**。比較しないものの基準線を取る意味はない
- 地図を要求しているのは**マージゲート([§3](standard.md#s3))とマージ確認責任([§8](standard.md#s8))、および障害時の一次対応**だけ。どれも着手ではない

### 直したこと(順序の変更+項目の削除)

- **§12 から手順「地図の初版を作る」を削除**(5手順 → 4手順)。**地図を先に用意する工程を置かない**
- **初版生成のトリガーを「その領域に初めて触るPRの中」に変更**(update-map モード1)
- **flow のテンプレートから §0(導入時)を削除**し、地図の工程を**PR作成段に集約**。領域の決定・初版生成・検収・更新が1箇所に揃った
- 図・skills/README・README の導入と使い方も追随

### 何が良くなったか

1. **着手の先頭から人間待ちのゲートが消えた。** 「その領域の最初のチケットにオンボードのコストが全部乗る」問題(独立検証が『一括生成のコストをPR著者に転嫁しただけ』と指摘したもの)が緩む
2. **検収する人が既にその場にいる。** ドメインオーナーはPRレビューで関わっているので、着手時に呼び出すより自然
3. **地図が「現実の記述」として正しくなる。** PR段で作れば変更後の現実を書くことになる。着手時に作ると、その領域の初回は必ず契約が変わるのでマージ前に古くなっていた
4. **地図に触る場所が1箇所になった。** 「初版は着手・更新はPR」で散っていたのが両方PR段に揃い、図と skills/README の矛盾(「初版を生成する」と「地図はここでは触らない」の同居)も消えた

### 却下された提案との違い(記録)

数時間前に「初版生成を Level 0 だけに縮める」提案が却下されている。**今回は別物である**——初版生成の**内容**(領域全体の調査)は一切減らしていない。変えたのは**いつ走らせるか**だけである。却下の理由(更新モードは差分しか拾わないので初版生成の代わりにならない)は、今回の変更では発生しない。

## 図の分類ミスと追随漏れを修正(版番号なし・2026-08-23)

**きっかけは「『領域の初回』の行は必要か。段階ってのはフローだけど、これはフロー外な気がする」という指摘。** レビューに出したら、指摘は正しく、**同種の分類ミスがもう1行**あり、さらに**直近2コミットの追随漏れ**が見つかった。

### 追随漏れ(私が html を触らずにコミットしていた)

- **`66fc1bb`(独立照合を実装と並列にした)が html を触っていなかった。** 図は「独立照合の既定は人間版で、**実行はレビュー段**」「この段でやるのは自動版だけ」という旧フローを表示し続けていた。入口文書で「期待値はレビューの席で書く」と読ませると、**同じコミットで潰した誤解(クリティカルパスに乗る・first-review の出力で汚染される)をそのまま再生産する**
- **`2d85ecb`(地図の初版を領域ごと・チケット内にした)が図の2箇所を残していた。** マージ段「地図の初版は**導入時**に生成・検収されている」、障害対応段「初版生成は**導入時**」。**同じ図の中で自己矛盾**していた(領域の初回行は「独立した前工程ではない」と書いていた)
- **`skills/crosscheck` 自身が内部矛盾していた。** description・冒頭・守ることの3箇所が「レビュー段に呼ぶ」と書き、手順Aだけが「実装と並列」だった。`skills/README` も「既定は下のレビュー段の人間版」のまま。**正本(手順A)に揃えた**

### 分類ミス — 表から2行を外した

図の列見出しは「段階」なのに、段階でないものが2行混ざっていた。

- **「領域の初回」**: セルの第一文が「**独立した前工程ではない**」と自分の配置を否定していた。散文で視覚配置を打ち消しているので図として壊れている。**行を削除し、着手段の導入後セルに条件付き注記として畳んだ**(「その領域に初めて触るチケットなら、まず地図の初版を生成・検収」)。従来側の痛点は障害対応段の従来セルと重複していたので失うものはない
- **「進め方」**(`/flow` の紹介): 下の全行のオーケストレータであって工程ではない。**表の外の注記に降ろした**。あわせて「**下の全段**を進める」が過大だったので「着手からマージ前の確認まで」に修正——flow のテンプレートに障害対応の節は無く、マージ・プッシュ・デプロイもしないと明記されている

**結果、表に残るのは本当の7段階だけになった**(着手・実装・テスト・PR作成・レビュー・マージ・障害対応)。

### 従来列の不公平を1件修正

「進め方」行の従来列が「**工程という概念がない**」だった。同じ表の従来列は各段階で従来チームの行動を記述している(レビュー「全行を同じ濃度で読む」等)のに、ここだけ「概念がない」と書くのは**従来側を不当に悪く描いている**。「やるかやらないかが、その日の余裕で決まる」に修正した。他の行は実装段で従来=「AIに委譲」と置いており、公平だと確認された。

### その他

- lede の「スキル**5本**」→ **6本**(`/flow` 追加時の数え漏れ)
- レビュー段「裁定して**仕様に**書き戻す」→ **3箇所**(仕様/設計メモ・裁定前に書かれたテスト・地図)。§4 が「書き戻す先は仕様だけではない」と明示している部分形を、図がそのまま言っていた
- 障害対応の段名に「と定期検査」を追加(月イチの矛盾検査が同居していたが行名に出ていなかった)
- 「払う場所」に**期待値の書き出し(実装と並列)**を追加し、**裁定を定常運用から各チケットのレビュー段に移した**
- 「独立照合」の初出(着手段)に一行の説明を添えた(図中6回使われるのに初出に説明がなかった)

### 検証済みで問題なしだった点(レビューより)

スキル名・チップは全て現行6本と一致 / `docs/flow/<チケットID>.md`・「契約」欄・「地図は着手では触らない」・PR同梱・マージ時の欄の突き合わせ・「10行の方針メモ」「10〜15分」・+20分の収支・v0.9表記 — いずれも本文と一致。配分図が2本を同じ長さにして「減ると主張できる実測がないから」と自制している点も正確。

## 独立照合の盲検フェーズを実装と並列にした(版番号なし・2026-08-23)

**きっかけは「crosscheck はどっちかにしたい。スキルを動かすのに時間がかかる」という指摘。** 実測で切り分けたら、**遅いのは crosscheck ではなかった。**

### 実測(基準を1周実走したときの記録)

| 工程 | 所要 |
|---|---|
| 地図の初版生成+検収 | 20秒 |
| 設計メモ+不可逆系の明示承認 | 25秒 |
| 実装+テスト | 3分45秒 |
| **独立照合(人間版)** | **並列(クリティカルパス0分)** |
| 裁定+書き戻し | 2分(人間) |
| **AI一次レビュー** | **6分** |

**独立照合はクリティカルパスに0分だった。** 実走とA/B比較の両方で、実装と並列に走らせていた——実装ファイルがまだ存在しない時点で回すと隔離が構成的に保証されるため。

**にもかかわらず、フローには逐次で書いてあった。** 自分の実践より自分のフローが遅い状態だった。

### 直したこと

- **flow のテンプレート**: 材料の提示と期待値の書き出しを**実装段に移し、実装と並列**にした。突き合わせだけがレビュー段に残る
- **skills/crosscheck / §4**: 実行時期を明記——「実装と並列。実装の完了を待たない」。理由2つ:①クリティカルパスに乗らない ②**実装ファイルがまだ存在しないので、隔離が指示ではなく構成で保証される**

**この変更は前エントリの汚染バグも同時に解く。** 期待値が実装より前に書かれるので、first-review の出力が存在しない時点で完了している。指示で守らせる必要がなくなった。

### 統合しなかった理由(記録)

first-review と crosscheck は**必要な情報が正反対**である。first-review は実装を読まないと仕事にならず(規約・テスト整合・Happy Path偏り・地図との整合を検査する)、crosscheck の人間版は実装を読んだら成立しない。1つのスキルにすると実行者が必ず実装を読むので、**crosscheck の側が起動と同時に死ぬ**。

そして**時間の観点でも統合の利益がない**——crosscheck は0分、first-review が6分。減らしたいのは first-review 側だが、A/B比較でバグを捕まえていたのは first-review だけだったので、切るのは悪い取引である。

呼び出しの手間は `/flow` が解いている(1回呼べば機械工程を連続実行し、人間の判断で止まる)。**統合すべきは呼び出しであってスキルではない。**

## 汚染経路の追加: AI一次レビューの出力は独立照合を汚染する(版番号なし・2026-08-23)

**きっかけは「first-review の中に crosscheck を含めてほしい」という要望。** 統合はできないが(理由は下記)、検討中に**フローの順序バグ**が見つかった。

### バグ

`skills/flow` のフローで、**`first-review` は PR作成段で走り、`crosscheck` の人間版はその次のレビュー段で走る。** つまり first-review の出力が先に出る。

first-review の出力は**実装の挙動・行番号・欠陥を詳細に記述する**。それを読んだ人は、もう「実装を経由していない解釈」を作れない。にもかかわらず、crosscheck の汚染源リストには実装コード・テストコード・実装後の仕様サマリ・PR説明文しか挙がっておらず、**AI一次レビューの出力が入っていなかった**。

**正規のフローを順番どおりに実行すると、レビュアーが普通にPRを開いた時点で独立照合が死ぬ**——これまで塞いできた「前提物なき義務」と同型で、今回は「順序による無効化」だった。

### 直したこと

- **§4 の汚染ルールに「AI一次レビューの出力」を追加。** 実装コードを見せるのとほぼ同じ汚染であると明記
- **skills/crosscheck**: 使わないものリストに追加し、「**first-review はこのスキルより前の段で走るため、これが一番踏みやすい経路である**」と明記。手順A-1 の禁止事項にも追加。守ることに「書き手がPRを開く順序」を追加——期待値を書き終わるまで差分・テスト・first-review の出力を読まない
- **skills/first-review**: 守ることの先頭に「**この出力は独立照合の汚染源である。人間版を担当する人には、期待値を書き終えるまで渡さないこと**」を追加。自分が汚染源だと自己申告させる
- **flow のテンプレート**: レビュー段の冒頭に「この段は上から順にやる。期待値の書き出しが済むまで、前の段で出た first-review の出力を読まない」を明記

### 統合しなかった理由

**first-review と crosscheck は、必要な情報が正反対である。**

- first-review は**実装を読まないと仕事にならない**(規約・テスト整合・Happy Path偏り・地図との整合を検査する)
- crosscheck の人間版は**実装を読んだら成立しない**

1つのスキルにすると、実行する人は必ず実装を読むことになり、**crosscheck の側が起動と同時に死ぬ**。実際、A/B比較のデモで first-review 自身が「このスキルの実行を独立照合の実施として数えないでください」と出力してきた。

呼び出しの手間は `/flow` が解いている——1回呼べば機械工程を連続実行し、人間の判断で止まる。**統合すべきは呼び出しであってスキルではない。**

## §12型の削除2件目: reviews/ を削除(版番号なし・2026-08-23)

**`reviews/adversarial-review-v0.5.md`(96行・16KB)を削除した。** 内容はgit履歴のコミット `0ec7ce8` に残る。

理由3つ:

1. **古くなって誤読を招く。** v0.5 に対するレビューであり、**現在は存在しない名前を使っている**——`gen-tests`(→ `crosscheck` に改名)が5箇所、「同語反復」(→ v0.7 で「自作自演」に改称)が4箇所。節の行番号も v0.5 のもの。公開リポジトリでこれを読んだ人は、**4版以上前の批判を現行の欠陥として受け取る**
2. **どこからも参照されていない。** 本文・README・スキル・図からの参照はゼロ。唯一のリンクは CHANGELOG の v0.6 エントリだけだった(プレーンテキスト+コミットハッシュに置き換えた)
3. **レビュー記録の実践が一貫していない。** v0.5 のレポートだけがあり、v0.6〜v0.9 と構成改訂6本の記録は無い。**1本だけ残っている状態は「レビュー記録を残す運用がある」と読ませるが、実際には無い**。沈黙と合格を区別せよという自分のルール(first-review)に照らして、片方だけ残すのは誤解を招く

**レビューが行われた事実は CHANGELOG の v0.6 エントリに残る**(4方向・重大6件+中位9件+引用3件、およびコミットハッシュ)。§12 の削除手続きに乗せると、(a)原因=**古くなって誤読を招くコスト**、(b)記録=このエントリ、(c)再追加条件=**レビュー記録を全版で残す運用を始めるとき**(そのときは v0.5 だけでなく全部を置く)。

これが §12 型の削除の2件目である(1件目はPRテンプレートの二重管理)。

## 撤回: 地図の契約記述の「前倒し」をやめ、設計メモに契約欄を置いた(版番号なし・2026-08-23)

**直前に入れた設計が間違っていたので撤回した。** きっかけは「update-map って実装後じゃないのか」という指摘。

### 何が間違っていたか

独立照合の材料(「実装前に書かれた契約の記述」)が存在しない問題に対して、**地図 Level 1 の契約記述を設計メモと一緒に承認する=地図の更新を前倒しする**という解き方をしていた。これが間違い。

**地図は現実の記述である。** `skills/update-map` のモード3(矛盾検査)は「地図の記述と**コードの現状**を突き合わせ、齟齬を列挙する」と定めている。実装前の**意図**を地図に置くと、実装が終わるまでの間、地図は現実と乖離した状態になり、**月イチの矛盾検査がそれを齟齬として誤検出する**。自分で「地図はコードと突き合わせるもの」と定めた設計に、意図を混ぜていた。

そして本当の欠落は別の場所だった。§4 は最初から材料を「チケット、**承認済み設計メモの要求部分**」と定めている。つまり**規範は設計メモに契約の記述があることを前提にしていた**のに、`skills/design-memo` の様式に契約欄が無かった。**規範に合わせてスキルを直すのが正しく、地図の工程を動かすのは筋違いだった。**

### 直したこと

- **`skills/design-memo` の様式に「契約」欄を新設**(契約・不変条件・危険領域を変えるチケットのみ)。書式は §2 の契約の定義どおり「この入力を渡せばこの出力が返る。失敗するときはこう失敗する」の3行
- **§4 から前倒しの記述を削除**し、材料は設計メモの契約欄と明記。あわせて「**地図に先に書くのではない**」理由(地図は現実の記述で、矛盾検査がコードと突き合わせる)を明記
- **設計メモの契約(実装前の意図)と、PRで更新された地図の契約(実装後の現実)の差も裁定の対象**であることを明記した。実装が逸れたのか、契約の書き方が曖昧だったのかを人間が裁く。前倒し案では意図と現実が同じ場所に入るので、この差自体が観測できなくなっていた
- フロー図(skills/README・flow-comparison.html)の着手段から `/update-map` を外した。地図に触るのは**導入時・PR作成時・障害対応後・月イチ**の4箇所で、着手時には触らない
- flow のテンプレートの着手項目を「設計メモの『契約』欄の下書き」に変更

## /flow を実際に走らせて、6件直した(版番号なし・2026-08-23)

**`/flow` を初めて実行した。** それまでは「テンプレートの項目を過去の実走記録と紙の上で突き合わせた」だけで、**手順が実行可能かを一度も確かめずに「作った」と報告していた**。実行させたら6件壊れていた。

実行自体は成立した——状態ファイルを作り、地図の初版を下書きし、最初の `👤`(地図の検収)で止まり、「何が必要か/誰が/なぜ機械で埋められないか/判断材料」を出して終わった。以下は同時に見つかった問題。

1. **領域の決定が機械にできないのに `👤` になっていなかった**(最大の問題)。update-map モード1 は「まず領域を決める(ドメインオーナーの担当単位)」だが、リポジトリにドメインオーナーの割り当ては存在しない。しかも**領域の切り方は、地図の検収先・マージゲートの発効単位・境界をまたぐ契約の持ち主を全部決めてしまう**。実質は人間の判断なので `👤` にした
2. **単一領域のときの領域名が未規定**だった(`docs/map/<領域>.md` の `<領域>` が決まらない)。「リポジトリ名かサービス名」と明記
3. **「不可逆系」の判定者が未規定**だった。ヘッダの更新を命じているのに、判定が機械なのか人間なのか書いていない。しかも不可逆系かどうかは §7 の承認ゲートの重さを決める入力である。「diff からの当てはめは機械が**提案**、**確定はドメインオーナー**」と明記
4. **実測の測り方が未規定**だった(エージェントの実行時間か人間の待ち時間か、単位も書式も無し)。集計が目的なのに集計できない。列を「工程/エージェント(分)/人間の待ち(分)/備考」に確定
5. **コピーされるテンプレート内の基準へのリンクが、コピー先で全部切れていた。** `assets/` からの相対パス `../../../standard.md#s4` が、`docs/flow/` や `.github/` にコピーされた時点で無効になる。**直前の「§参照を112箇所リンク化」で私が壊した**。コピーされる2つのテンプレート(flow / PR)はリンクを張らず平文 `§4` に戻した
6. **「既に済んでいる」項目の扱いが無かった。** セクション0(領域ごとに一度だけ)はチケット単位の状態ファイルでは毎回未チェックで再登場する。スキップ理由は「重すぎた/不要だった/やる気がなかった」しかないので、**既に検収済みの地図に対して偽の「不要だった」が溜まる構造**だった。「既に済んでいるものは `[x]` にして根拠を書く。`[~]` にしない」と明記

**副産物**: 生成された地図の下書きが「TICKET-301 はウォッチ登録を要求しているが、そのコードはリポジトリに存在しない」を検収時の確認点として挙げてきた。地図が、チケットが存在しない箱を前提にしていることを検収前に出した。

## §参照をリンクにした + 地図の発効条件と所有者を確定(版番号なし・2026-08-23)

### §参照をすべてリンクにした

`§3` `§8` のような参照が112箇所あり、**どの節を指しているのか本文を探さないと分からなかった**。すべてクリック可能にした。

- standard.md の各節見出しの直前に `<a id="sN"></a>` を挿入した。GitHub の自動アンカーは日本語の句読点の扱いが実装依存なので、**明示アンカーにして見出しの改称でリンクが切れないようにした**(v0.7 で節タイトルを7件改称した実績があるため)
- standard.md 内は同一ページ内リンク、skills/ と README からは `standard.md#sN` への相対リンク

### 採らなかった提案とその理由(記録)

**提案**: 導入時の地図の一括生成を **Level 0 だけ**に縮め、Level 1〜3 は「その領域に触る最初のPR」で起こして育てる。

**動機**: 大規模で一括生成が重い。「全体の地図ができるまで全PRがゲート未発効」を避けたい。

**採らなかった。** 独立検証(サブエージェント。同一ベンダー・指示ベースの隔離)で、根拠3つのうち2つが事実に反し、1つは解決済みだと分かった。

- ❌ **「更新モードが既にPRごとに育てるので、一括生成は重複」** — 更新モードが拾うのは導入後に**変わった**事実だけである。領域全体を調査して起こす手順は初版生成モードにしかない。変わらないまま残る既存コード(§1が負債の典型に挙げる「誰も意味を説明できないDBのデータ変換ロジック100行」)は更新モードでは永遠に地図に載らない。**重複ではなく相補だった**
- ❌ **「一括生成の中身はAI推測で信頼度が最低」** — Level 別に見ると逆。Level 3(不可逆系の位置)はスキーマ変更・課金呼び出し・外部送信の機械的検出でほぼ確実に拾えるので、**地図の中で最も生成信頼度が高い層**である。推測が混ざるのは Level 1(契約)
- ❌ **「大規模でゲート未発効になる問題が消える」** — 領域単位化(前エントリ)で既に解決済みだった。**解決済みの問題を解こうとしていた**
- ⚠ **提案が依拠した非対称が成立しない** — 「障害時に必要なものは網羅が要るから Level 0 は前払い」という論法は、Level 2(不変条件)と Level 3(危険領域の位置)にも同じく当てはまる。障害の夜に要るのは「触った不可逆系」ではなく「全部の不可逆系がどこにあり、どう戻すか」である。**提案は地図が最も要る場所(長く触られていない領域)から順に地図を欠落させる分布を作る**
- ⚠ **ゲート述語が循環する** — 「そのPRが触る範囲の Level 1〜3 があるか」を判定するには、触っている範囲を知る必要があり、その判定に使う道具が地図である。**ゲートが最も必要な場所でちょうど判定不能になる**

**検証が掘り当てた実在の穴3件は、提案とは独立に塞いだ**(下記)。

### 塞いだ穴3件

1. **マージゲートの発効条件を確定した(発効条件の明示)。** `skills/update-map` が「マージゲートも領域ごとに発効する(基準§12 手順2)」と書いていたが、**§12 にその規定は無かった**——§12 の「領域ごと」は検収者の文脈だけで、発効は「手順2が終わるまで」= 全領域の完了待ちだった。**スキルが規範に無いことを規範の名で語っていた。** 領域ごと発効を §12 手順3 に明記し、スキルの参照先を手順3に直した
2. **全体 Level 0 の所有者を明示した(実行主体の明示)。** §8 の地図の維持責任は「**自領域**の地図(§11 **Level 1〜3**)」で、領域が2つ以上あるときの全体 Level 0(`docs/map/README.md`)は**誰の自領域でもなく、Level の範囲からも字義上外れていた**。境界をまたぐ契約は「**呼ばれる側の領域のドメインオーナー**が持つ」と定めた——契約の当事者は呼ぶ側と呼ばれる側で、決められた出力を返す義務を負うのは呼ばれる側だから(§2 の契約の定義に従う)。責任範囲も Level 0〜3 に直した
3. **マージゲートの地図要件を一本化した(正本の一本化)。** §3 は「地図(§11)を見てどの箱が壊れたか特定できる状態」、§11 の表は Level 1〜3 を「いつでも参照できる」と定めており、**どちらがゲート要件の正本か曖昧だった**(上の提案はこの曖昧さの産物である)。§3 に「要件は**触る領域の地図が Level 0〜3 まで検収済みであること**」と明記した

### あわせて2件

- **領域に属さないコード(共通基盤・共有ライブラリ・認証ミドルウェア)も1つの領域として扱い、ドメインオーナーを決める**ことを `skills/update-map` に明記。担当が決まらないコードは領域ファイルを持たないので、first-review の検査項目6が恒久的に「未検査(地図なし)」を吐き続ける——**領域の切り方の漏れが、沈黙ではなく反復として出る**
- flow のテンプレートの「リポジトリに一度だけ」を「**領域ごとに一度だけ**」に修正(領域単位化の追随漏れ)

## 地図を領域単位にした(版番号なし・2026-08-23)

**矛盾の修正。** §8:219 は「**自領域の**地図(Level 1〜3)の初版検収」、§12:287 は「**領域ごとの**検収者はドメインオーナー」と、規範側は**領域単位**を前提にしていた。一方 `skills/update-map` は「置き場所: `docs/map.md`」「分量: **全体で**200行以内」と**単一ファイル・プロジェクト全体**を指定していた。別の単位を前提にした記述が両方あった。

きっかけは「巨大なプロジェクトでも200行でまとまるのか」という質問。まとまらない——Level 1 が「主要コンポーネントごとに1〜3行」なので、コンポーネントが100個あればそれだけで上限を超える。規範側に揃えた。

- **置き場所を `docs/map/<領域>.md` に変更**(領域=ドメインオーナーの担当単位)。領域が1つのプロジェクトでは `docs/map/` にファイル1枚でよい
- **分量は1領域あたり200行以内。** プロジェクト全体で200行ではない
- **上限を設計の計器として使う**ことを明記: 「ある領域の地図が上限に収まらないなら、それは**領域の切り方が大きすぎるというシグナル**である(1人が担当しきれない範囲になっている)」
- 領域が2つ以上あるなら、全体の Level 0 には**領域の境界と、境界をまたぐ契約だけ**を置く。領域の内部は各領域の地図へ
- **初版生成を領域単位にした**: 「まず領域を決める。**全領域を一度に作らない**」。領域を1つずつ生成・検収でき、マージゲートも領域ごとに発効する。大規模で「全体の地図ができるまで全PRがゲート未発効」になるのを避けるため
- 検収者を「その領域のドメインオーナー」に明示
- 参照を6箇所で差し替え(first-review / pr-guide / design-memo / PRテンプレート / flow テンプレート / flow-comparison.html)

## ライセンスを CC BY 4.0 に確定(2026-08-23)

README の「TODO: 公開時に決定」を解消した。リポジトリは既に公開されているので、TODO自体が古かった。

- `LICENSE` を新設(CC BY 4.0、SPDX 識別子つき)。著作権者は `HawkClaws`——Contributing が「特定の組織が識別できる情報は書かない」と定めているため、公開リポジトリの同一性で表記する
- README の License 節に**帰属の書き方の例**を追加(社内ドキュメントへの取り込み / スキルを改変した場合 / 記事での引用)。「CC BY 4.0 です」だけでは、社内で使う人が判断できない
- **実務上の注意を明記**: 社内の非公開ドキュメントに取り込んで自チームで使うだけなら再配布に当たらないので帰属義務は発生しない。それでも出典と版を1行書くことを勧める——義務ではなく、**改訂に追従するかを後から判断できる**ようにするため。スキルは `cp -r` で他リポジトリに入る前提なので、この一文が無いと導入時に必ず止まる

## スキル追加: /flow(版番号なし・2026-08-23)

**6本目のスキル `flow` を追加した。規範はゼロ個追加していない。** 他の5本を基準が定めた順序で呼び、状態を1つのファイルに記録する進行係である。

**動機**: スキルを1本ずつ手で呼ぶ運用は、忙しい日に最初に消える。`/flow <チケットID>` は機械でできる工程(下書き・機械検査・突き合わせ・記録)を連続実行し、**人間の判断が必要な項目で止まる**。人間が空欄を埋めてもう一度呼べば続きが走る。1チケットあたりの呼び出し回数が15回以上から**最大8回**(人間のゲートの数)になる。

- 状態は `docs/flow/<チケットID>.md` のチェックリストに持つ。**この `👤` 付き項目の一覧が、そのチケットにおける Human-in-the-Loop の定義そのもの**である(§7・skills/README の「空欄」の議論を、チケット単位で実体化したもの)
- **絶対にやらないこと**を明記した: `👤` にチェックを入れない・埋めない / 不可逆系のゲートで「無返事なら進む」をしない / 設計メモの合意より前に実装を走らせない / **スキップの判断も人間のものなので勝手に `[~]` にしない** / マージ・プッシュ・デプロイをしない
- **スキップは削除せず `[~]` + 理由**を表に残す。理由は「重すぎた/不要だった/やる気がなかった」に寄せて書く——これが §12 の削除手続き (a)「守られない原因の確認(コストなのか動機なのか)」の材料になる。**この表が溜まらないと、重すぎる項目を削る根拠が永久に手に入らない**
- 実測表(各工程の所要・人間の待ち時間)も状態ファイルに持つ。§12 手順1 が要求するベースラインの実体
- flow-comparison.html に「進め方」の段を追加

**追加してよいと判断した理由**: 直前のレビュー3本が「規範を足すのをやめろ(純削減になるまで)」と勧告している。`flow` は義務・閾値・責任割当を一つも増やしていない実行層であり、かつ**実測が溜まらないと §12 の削除が永久にできない**ため、これは削るための道具でもある。

**あわせて §4 の欠落を1件修正した(実走で確認済み)**: 「裁定の結果は仕様に書き戻す」しか書いていなかったが、書き戻し先は3つある——仕様/設計メモ、**裁定前に書かれたテスト**、地図 Level 1〜2(契約を変えた場合)。実装と同じセッションが書いたテストは裁定前の読みを固定しているので、仕様だけ直すと**間違った読みが「守るべき回帰」として保護される**(§2 でそのテストの目的を回帰検知と定めているため)。基準を1周実走したときに実際に起きた(83件のテストが裁定前の読みを固定していた)。crosscheck 手順C と flow テンプレートも揃えた。

## スキル改名: gen-tests → crosscheck(版番号なし・2026-08-23)

**`skills/gen-tests` を `skills/crosscheck` に改名した。コマンドは `/gen-tests` → `/crosscheck`。**

**移行**: 既にスキルをコピーしている場合は、`skills/gen-tests` ディレクトリを `skills/crosscheck` に置き換える(SKILL.md の `name:` も `crosscheck` に変わっている)。中身の手順は変わっていない。

理由:

- **既定パスがテストを1本も生成しない。** v0.8 で人間版を既定にした時点で、名前は少数派の経路(自動版)だけを指すようになっていた。この時点では同期コストを理由に据え置いたが、それは判断ミスだった
- **その2の変更で、名前が積極的に誤解を招く状態になった。** 人間版をレビュー段に移した結果、このスキルは**2つの段で、2人の別人に呼ばれる**ようになった——自動版は実装・テスト段で著者が、人間版はレビュー段でレビュアーが呼ぶ。「テストを生成するスキル」という名前では、レビュアーが自分の呼ぶものだと分からない
- 分割案(人間版と自動版を別スキルに)は採らなかった。共通の「仕様の切り出し(汚染遮断)」と「手順C 裁定」が2ファイルに重複し、その1で排除した重複を再導入することになる

あわせて SKILL.md の冒頭に、**このスキルが2つの段で2人に呼ばれること**と、**既定の人間版はテストを生成しないこと**を明記した。description にも実行主体と段を入れた(スキル一覧から選ぶ側に届くのは description なので)。

## 構成改訂 その4(版番号なし・2026-08-23)

**追随漏れの機械検査を回して、残っていた4件を直した。** §参照の宛先(全12節・宛先なしゼロ)、用語の残存、PRテンプレートの欄と参照側の一致、図と本文のフロー一致を検査した。

- **図の用語集が「濃淡」を用語として定義していた。** standard.md の用語表には無い語であり、同じ概念に「理解レベル」「レビュー指定」と合わせて3つの名前があった。図の定義を「理解レベル(=レビュー指定)」に統一し、本文中の使用箇所も揃えた(§3 の「§2の濃淡に従う」→「§2の理解レベルに従う」)
- **図の障害対応段の注記が古かった**(「地図の生成・更新・矛盾検査はスキル化済み」)。初版生成は導入時の工程として分離済みなので、この段は障害で判明した事実の書き戻しだけになった。あわせて**月イチの矛盾検査が図のどこにも出ていなかった**ので、この段の注記に入れた(担当と実行手段をカレンダーかCIで決めることも)
- **update-map の汚染経路の記述が自動版限定だった。** CLAUDE.md にアーキテクチャ記述を置くと汚染されるのは、自動版のサブエージェントだけでなく**人間版で人間が読む材料も同じ**である。「独立テスト生成の汚染経路」→「独立照合の汚染経路」に直した
- skills/README のフロー図の段名を図と揃えた(「実装」→「実装・テスト」)

## 構成改訂 その3(版番号なし・2026-08-23)

**flow-comparison.html を本文のフローに追随させた。** その2で standard.md と skills/ は直したが、図が古い状態のまま残っていた。

- **「導入時(一度だけ)」の工程を新設した。** 地図の初版生成・検収が、図では**マージ段の注記1行**にしか存在せず、`/update-map` のチップは障害対応段にしかなかった。つまり図として読むと依然「地図は障害対応のときに触るもの」だった。v0.9 で standard.md §12 に手順を足したのに、入口文書である図には反映していなかった
- **着手段に契約記述の前倒し承認を反映**(その2の変更)。`/update-map` が図に4箇所現れるようになり、スキルのモード数と対応した
- **テスト段を自動版だけにし、人間版をレビュー段に移した**(その2の変更)。図はまだ「期待値の書き出し」をテスト段(=著者のセッション)に置いていて、その2で直した「書き手は実装方針を書いていない人」と矛盾していた
- マージ段に、独立照合の実施確認(不可逆な副作用が「なし」以外なら「未実施」でないこと)を追記
- 「濃淡」の残りを「レビュー指定」に統一

**反省として記録**: その1・その2で本文とスキルだけ直して図を後回しにした。この図は README の入口に置かれた導入判断用の文書なので、**本文より先に読まれる**。追随漏れは本文の矛盾より読者に届く。

## 構成改訂 その2(版番号なし・2026-08-23)

**独立照合(§4)の周辺に残っていた「守られなくなる構造」を4件閉じた。** 規範の追加・削除はゼロ。使ったのは「正本の一本化」「順序の変更」「実行主体の明示」だけである。v0.9 で地図について塞いだ穴(前提物なき義務・順序の矛盾)と同型のものが、§4 の側に残っていた。

**1. 規範とスキルが正反対だった(正本の一本化)**

- §2 は「行レベル対象に当たるかと、ドメイン中核かの判定はドメインオーナーの確認事項とし、**著者の自己申告では確定させない**」と定めているのに、`skills/pr-guide` は「**最終決定は著者**」と書いていた。日常的に実行されるのはスキルなので、ドメインオーナー確認は誰のフローにも現れていなかった
- §2 自身が「低いレベルに倒すほど著者が得をする構造」と特定している通り、これは注意力ではなくインセンティブの問題で、唯一の歯止めがスキル文言で外れていた
- pr-guide と design-memo を規範に揃えた

**2. 独立照合の材料を作る工程が無かった(順序の変更)**

- §4 は照合の材料を「チケット、承認済み設計メモの**要求・契約の記述部分**」に限っているが、`skills/design-memo` の様式にその欄は無く、あるのは方針・触る範囲(=独立照合が汚染源として遮断する側)だけだった。チケットが薄いと、**実装前に書かれた契約の記述がどこにも存在しないまま、不可逆系の必須義務が発効する**
- 契約・不変条件・危険領域を変えるチケットでは、**地図 Level 1 の契約記述を設計メモと一緒に承認する**ことにした。地図の更新はもともとPRに同梱する既存の工程なので、**前倒しするのは下書きの時期だけ**である
- gen-tests の「使うもの」にこれを明示し、材料が空のときは「照合は成立しない。設計メモ側に戻す」と定めた(全欄「仕様に記述なし」の照合は実施ではない)

**3. 人間版の書き手が、フロー上で著者に落ちていた(実行主体の明示+順序の変更)**

- §4 は人間版を「レビューに入る前に」と書いていたが、`skills/README` のフロー図は `/gen-tests` を**実装段=著者のセッション**に置いていた。フロー通りに回すと書き手が著者になり、**正規フローを忠実に実行するほど独立照合が「実施記録付きの自己照合」に縮退する**
- 人間版の書き手を「**実装方針を書いていない人**(レビュアー。不可逆系ならドメインオーナー)」と明示した。著者は設計メモで実装の設計図を書いた当事者なので、実装を経由していない解釈を作れない
- フロー図で人間版をレビュー段の冒頭に移した(自動版はツール制限で独立性が担保されるので実装段のまま)
- これは基準を1周実走したときに実際にぶつかった摩擦でもある(著者である自分が人間版を実行できず、別セッションに渡した)

**4. 最重量の義務に、実施を確認するゲートが無かった(実行主体の明示)**

- 「不可逆系の独立照合は必須」は、§3 のマージ基準にも first-review の検査項目にも §12 の測定リストにも現れていなかった。しかも PRテンプレートの「未実施」は非不可逆PRでは正しい正常値なので、大多数のPRで並んで正規化され、**不可逆PRの「未実施」が異常として浮かばない**
- 摩擦が最大(人間の書き出し待ちでブロックする唯一の工程)で検査ゼロという組み合わせだった
- §8 の**マージ確認責任の対象に、既存の欄同士の突き合わせを追加**した——「不可逆な副作用」が「なし」以外なら、テスト欄の独立照合が「未実施」でないこと。新しい検査項目ではなく、既にテンプレートにある2欄の整合確認を既存の責任に割り当てただけである
- first-review の BLOCKER 枠には足さなかった。単独枠の設計意図(最重要の警告を1つだけ置く)を薄めるため

**未対応(記録)**: 同レビューは他に5件を挙げている——裁定結果の書き戻し先が3箇所あって正本が未指定/不可逆系設計メモの承認者が「チーム」=誰でもない/「採らなかった選択肢」の二重記入/同一判定がPRで3〜4回走る警報疲れ/導入先の実PRテンプレートとの三重化。**これらは「回したらこうなる」の予測であって、実運用で確認したものではない**ため、§12 の削除手続きと同じ考えで実測を待つ。

## 構成改訂(版番号なし・2026-08-23)

**版番号を進めない。** 規範の追加はゼロ。実運用の実測が一つも無いまま42時間で9版まで進んだこと自体が、§10で警告したグッドハートの法則(指標を目標にすると腐る)の自己適用漏れだったため。§12手順1「数字がなければ、この後に何をやっても効果を示せない」に従い、実測が出るまで版は据え置く。

**内容は「全体の見直し」で、骨格は変えていない。** 15節構成・節番号・すべての規範をそのまま維持し、重複を正本1箇所に寄せ、矛盾を潰した。

**矛盾の修正:**

- `flow-comparison.html` の「裁定」の定義が**自動版限定に狭まっていた**(「実装を見せずに別セッションが書いたテストが落ちたとき」)。v0.8 で人間版を既定にしたのに、図では既定側の裁定が定義から漏れていた。用語表の文言に一致させた
- 同図の「濃淡」を「レビュー指定」に統一。同一概念に4つの呼称(レベル/レビュー指定/理解レベルの提案/濃淡)があった

**重複の解消(規範は削っていない。正本1箇所+参照にした):**

- §4 の「自動版に上げる条件」3項が skills/gen-tests 手順0 と逐語だった → gen-tests を正本に
- §4 の汚染ルールの詳細・幅アサーション対策の詳細 → gen-tests の該当節を正本に
- §9 の「なぜツールでは守れないか」の説明が skills/README・pr-guide と三重 → skills/README「空欄がHuman-in-the-Loopの定義」を正本に。§9には規範(実効性は口頭確認に置く)だけを残した
- §11 の「§3のゲートが空文になる」理由句 → §3への参照に(同梱の規範は維持)
- §2 の「テストの最低品質を機械検査する」→ §10 を正本に
- §5 の §4 結論の再説明 → 参照に
- skills/update-map の警報疲れ理由句 → first-review を正本に
- §11 冒頭の理解負債の定義が用語表と二重 → 用語表を正本に(実例と出典は本文に残した)

**§12型の削除を初めて実行した(1件):**

- **PRテンプレートの二重管理をやめた。** `templates/pr-template.md` を削除し、`skills/pr-guide/assets/pr-template.md` を唯一の正本にした。「編集時は両方を同期すること」という**手動同期の義務そのものを削除した**——(a)守られない原因=コスト(v0.8 で実際に同期漏れが起き、別コミットで追い直している)、(b)記録はこのエントリ、(c)再追加条件=リポジトリ直下にテンプレート置き場が必要になる要望が出たとき

**表記の撤回:**

- v0.9 のエントリから「**独立**監査」の語を撤回した。全コミットが単一著者・PRなし・人間レビュアーなしで、検証は著者が起動した同一ベンダーのサブエージェントである。skills/gen-tests 自身が「ツール制限なし・ログ確認なしの場合は『独立性は指示ベース』と明記する」を課しているので、その表記に従う

**分かったこと:** 重複排除では量は減らない(16,621字 → 16,541字、0.5%)。2日で5.4倍になった原因は重複ではなく内容の蓄積であり、量を減らすには規範か論拠のどちらかを削るしかない。

## v0.9 (2026-08-23)

**地図(§11)のフローに空いていた穴を塞いだ。** 規範の追加を含む(§8 に責任の行が1つ増え、§12 に手順が1つ増えた)。

きっかけは「地図って障害時に作るの? なんで障害時だけでいいの?」という質問。調べたら**その読みが正しかった**——地図は §3 マージゲートの実体・§8 一次対応の前提・§11 の中心概念として最も多く参照される成果物なのに、**その出生(初版生成)だけがどの文書のフローにも存在しなかった**。v0.6 で update-map を新設したとき「書き戻す手段が無い」穴を塞ぎ、「最初に作る工程が無い」穴が残っていた。サブエージェントによるレビュー(著者が起動。同一ベンダーのモデルで、隔離は指示ベース)で7件を検出し、全件を修正した。**これは独立した検証ではない**——skills/gen-tests 自身が「ツール制限なし・ログ確認なしの場合は『独立性は指示ベース』と明記する」を課しているため、その表記に従う。

- **§12 に手順2「地図の初版を作る」を新設**(以降の手順を繰り下げ)。初版生成 → 人間の通読・修正による検収まで。検収者は領域ごとのドメインオーナー
- **§12 手順3にマージゲートの発効条件を明記**: 「マージゲート(§3)は地図の初版検収をもって発効する」。地図が無いまま2ゲートの運用を始めると、**基準に従うほど規範違反になる**状態(最初のマージから §3 違反)だった。**なし崩しの棚上げと、明示した保留を区別する**ことを条件にした
- **§12 手順3に月イチ矛盾検査の担当決めを追加**。担当を決めない定期検査は発火しない
- **§8 の責任表に「地図の維持責任」を新設**(ドメインオーナー)。マージ確認責任が「地図が§3の基準を満たしていることの確認」を要求する一方、**地図を維持する責任者がどの行にもいなかった**。全員が確認して誰も所有しない状態=§11 が排除したはずの「善意に頼る」の再発。月イチ検査の実施はオンコール輪番やCIに置いてよいが、**齟齬の判定は委譲できない**と明記
- **地図更新のタイミングの矛盾を解消**: 規範(§11)は「変えるPRに地図の更新が含まれているか」= マージ前を要求していたのに、`skills/README.md` のフロー図と `update-map` のトリガー文言は「変更の後」= マージ後だった。**規範側に統一**し、「地図の更新は当該PRに同梱する。マージ後に回すとマージ時点で追跡可能性を確認できず §3 が空文になる」を §11・update-map・skills/README・flow-comparison.html に明記。事後更新になるのは**障害対応で判明した事実の書き戻しだけ**
  - フロー図通り(マージ後更新)に運用すると、契約を変えるPRが**毎回** first-review 項目6の指摘を受け、常態化して無視される——このリポジトリ自身が BLOCKER 枠で防ごうとしている警報疲れと同型だった
- **skills/README のフロー図に3モードを配置**: 導入時(初版生成)/ PR作成段(更新・同梱)/ マージ後(障害対応の書き戻し・月イチ矛盾検査)。update-map は3モード持つのに、フロー図の登場は1箇所だけだった
- **skills/update-map**: description に導入時の初版生成を追加。モード1・2の見出しにトリガーを明記
- **skills/first-review**: 「地図なし」を報告するとき、初版生成から始められる旨を添えるようにした。**「地図なし」を正常系として黙って通すと、地図が無い状態から抜けるきっかけがフローのどこにも無くなる**
- **PRテンプレート**(`templates/` と `skills/pr-guide/assets/` の2箇所): 「地図(docs/map.md)」欄を新設(更新不要 / このPRに含む)。マージ確認責任と first-review 項目6の両方が地図更新の有無を問うのに、**著者が申告する欄が無かった**(不可逆な副作用にはチェックボックスがあるのと非対称)
- **skills/pr-guide**: 機械記入欄に地図の判定・提案を追加
- **flow-comparison.html**: PR作成段に `/update-map` と同梱の注記、マージ段に「初版は導入時に検収済み」の前提を追記。版表記を v0.9 に

## v0.8 (2026-08-23)

**§4 の規範を「別セッションでテストを生成する」から「実装を経由していない解釈と突き合わせる」に一段抽象化した。** 規範内容の変更を含む(必須項目の実装手段が2択になった)。

きっかけは「普通に実装を依頼すればテストまで作られるし、そこはレビューで担保する部分ではないか」という指摘。検討の結果、**コスト論は妥当だが「レビューで代替できる」は成立しない**と判断した。レビュアーは実装を読んでからテストを読むため、読み方が「テストは実装と合っているか」に固定され、仕様の別の読み方は原理的に出てこない(判断のアンカーが実装側にある)。必要なのはテストの本数ではなく**実装を経由していない2つ目の解釈**であり、それは人間が作ってもよい。§7 の外部証拠(AIレビュー提案の採用率16.6%、判断は速くなるが品質は上がらない)も「レビューで担保」の側に不利である。

- **§4**: 規範を一行に置き直した——「不可逆系の契約は、実装を経由していない解釈と突き合わせてから通す」。これを**独立照合**と呼び、実装手段を2つ示した
  - **人間版(新設・既定)**: レビュー前に実装前仕様とインターフェースだけを読み、期待する入出力と失敗時挙動を**自分で書き出してから**テストと実装を読む。契約1本あたり数分
  - **自動版(旧・独立テスト生成)**: 実装非開示のツール制限セッションにテストを生成させる。**自動版に上げる条件を明記**(契約数が多い / 仕様が長く期待値を書き出せない / 過去に裁定が発生した領域)
  - 適用範囲の文は維持(不可逆系は必須・構造理解以上は推奨・契約理解は過剰装備)。ただし**「必須なのは独立照合であって、自動版ではない」**と明記。これは v0.7 までの「不可逆系には自動版が必須」の実質的な緩和である
  - 汚染ルール(実装前仕様に限る・設計メモの方針欄は使わない)を両版共通の節に集約
  - 「それでも人間に残る仕事」に**人間版固有の弱点**を追加:実施したかを機械検査できない(自動版はツール使用ログで確認できる)。守らせる力が仕組みではなく規約にあることを明記
- **用語表**: 「独立照合」を新設。「独立テスト生成」はその自動版と定義し直した。「裁定」の定義を独立照合基準に合わせた
- **§2 / §10 / §11**: 「独立テスト生成」への参照を「独立照合」に変更(§2:95 正しさの検証、§10:251 ミューテーションテストとの併用、§11:281 裁定の義務)
- **skills/gen-tests**: 手順を再構成した。手順0(版の選択)→ 共通(仕様の切り出し)→ 手順A(人間版)/ 手順B(自動版)→ 手順C(裁定)。人間版では**仕様面だけを提示して期待値メモを空欄で返す**(実装・テスト・挙動説明を出力に含めない)のがスキルの仕事になる
- **PRテンプレート**(`templates/` と `skills/pr-guide/assets/` の2箇所): テスト欄を「独立照合(§4): 未実施 / 人間版 / 自動版 ・ 食い違い ___ 件 ・ 裁定」に変更。**人間版で通した場合が「未実施」に見えない**ようにした
- **skills/README・skills/pr-guide・flow-comparison.html**: 語と既定版の記述を追随。flow図のテスト段に人間の空欄「期待値の書き出し」を追加
- **skills/first-review**: 「このスキルは独立照合ではない」を明記。検査項目2(テストと実装の整合)は**実装を読んだうえで**テストを見ているのでアンカーが実装側にあり、別セッションで実行してもこの性質は変わらない。不可逆系で first-review を回したことを独立照合の実施と取り違えないため

**改訂後にデモ実行して判明した追記(同日):** 不可逆系(課金の日割り返金)を題材に、実装 / 人間版 / 自動版を別セッションで走らせた。テストの赤は0件(3セッションが4つの曖昧点で同じ読みをした)だったが、人間版の「決められなかった点」から**仕様の内部矛盾**(税抜の月額を日割りした額を、税込で請求済みの refund API へ渡す=約10%の過少返金)が出た。テストでは原理的に出ない種類の欠陥である。同時に、**自動版が曖昧な箇所に幅を持たせたアサーション**(`expected - 1日分 <= result <= expected + 1`)を書き、曖昧さをテストの中に吸収して検出力を失っていた。これを §4「それでも人間に残る仕事」と skills/gen-tests に反映した(自動版には「幅を持たせるな、決められないならリストに出せ」を明示指示する)。

## v0.7 (2026-08-22)

standard.md の**表現のみ**の改訂。規範内容(義務・閾値・責任割当・出典)の変更はなし。基準本文は人間が読む文書であり、v0.6 までの圧縮された書き方が理解の障壁になっていたため。全 URL・全数値は旧版とバイト単位で一致(サブエージェント2本で機械照合済み)。

- 漢語の名詞化を動詞に開いた(「理解コストの請求だけが残り」→「理解の分の請求書は消えていない」等)。1文1論点に分割
- 造語・難語に説明を付けた:**用語表を冒頭に新設**(理解負債/契約/不可逆系/地図/独立テスト生成/裁定)。「同語反復」→「自作自演」、「利益相反」→「自分の解釈を自分で正解にできてしまう」、グッドハートの法則に一行説明
- 冒頭に **「この基準を3行で」** を追加(README の思想3行を踏まえた3点。3行目は§12・末尾イタリックの「先払い・減らす・計上」側を採った)
- 長い箇条書きを見出し付きの塊に分割(§4「自作自演を断つ」/「それでも人間に残る仕事」、§7 の測定項目をリスト化、§12 の削除手続きを小見出しに)
- 表の見出し語とセルを平易化(§2 見出し「要求される理解」→「何ができれば『理解した』か」、§3 セル「説明可能性/トレース可能性」→「説明できるか/追跡できるか」、行ラベル「PR提出/マージ」→「PRを出すとき/マージするとき」)
- §2 のレベル判定を、混在した1段落から**番号付きの3ステップ**に整理
- **§3 の参照ミスを修正**(「トレース責任は地図レベル(§10)」→ §11。§10 はカバレッジゲート)
- 「トレース可能性」を「追跡できるか」に、「重心移動」の説明を平易化。§1 の三つの問いに「①と②が最も混同されやすい」の一文を追加
- **節タイトルを7件改称**(§1「三つの判断を分離する」→「三つの問いを混ぜない」、§2「AI生成コードの理解基準」→「どこまで理解するか」、§5「ライブラリ扱いの条件」→「『ライブラリ扱い』していい条件」等)。旧タイトルを参照する他ファイルは無く、参照切れなし
- 「権限移譲の原則」「訓練:使わない理解は腐る」を太字段落から `###` 見出しに昇格(`skills/README.md` の引用が実見出しと一致するようになった)
- **「裁定」は用語表に残した。** 一度「判定」に統合したが、§2 のレベル判定と二義になり、かつ PR テンプレートの欄名(`templates/pr-template.md`)ほか計14箇所が語の裏付けを失うため撤回。用語表で「レベル分類の『判定』とは別物」と明示する形にした
- 他ファイルの語を standard.md に追随:`flow-comparison.html`(同語反復→自作自演 2件、トレース→追跡 1件)、`skills/first-review`・`skills/gen-tests`(同語反復→自作自演)

**flow-comparison.html の可読性改善:**

- **冒頭の図を作り直した。** 等サイズの箱を並べた SVG では、見出し(「理解コストの引っ越し」)の主題である**コストの量**が見えていなかった。棒の高さ=そこで人間が払う理解コストとし、従来(レビューとマージに集中)と導入後(着手・PR・レビュー・マージに分散)を同じ時間軸で並べる形に変更。**総量は減らない**ことを図中に明記
- 実装を SVG から HTML+CSS に変更。860幅・11px の固定 viewBox がスマホで縮小されて判読不能だった問題を解消(文字が実テキストになり、狭い幅では図だけが横スクロール)
- **「この図で使う言葉(5つ)」を折りたたみで追加**(契約/濃淡/不可逆系/地図/裁定)。README でこのページを入口に昇格させたため、基準本文を読む前に単体で読めるようにした
- 走り書き状態だった凡例(痛点/skill/人間/変更なし)を、説明付きの凡例リストに分離
- **図を再度作り直した(1回目の失敗)。** 段階ごとに棒を立てる形にしたところ、従来2本・導入後4本になり「導入後の方がコストが増えている」と読めてしまった(実際に長さも 118 対 122 で導入後が長かった)。配分を語る図が量の比較に見えるのは失敗なので、**合計を両方100に固定し、色で「見積りに入っている作業/見積り外の不意打ち」を見せる形**に変更。従来=計上6・見積り外94、導入後=計上92・見積り外8
- 図に **「合計を同じ長さにしてあるのは、減ると主張できる実測がないから」** と明記し、短期では +20分前後で増えることを図中から「正直な収支」へ繋げた(§12 の反証可能性の要求に合わせた)
- **§2 の「契約」に実例を置いた。** 用語表と抽象的な説明だけでは初見の壁が残っていたため、`createUser(email, plan)` の成功/失敗/前提の3行を先に見せ、「中身ではなくこの3行を理解する」に繋げる形に。語は残した(§4「条文の妥当性」の比喩と、skills/テンプレート14箇所の語彙を壊さないため)

## v0.6 (2026-08-22)

敵対的検証(4方向・重大6件+中位9件+引用3件)への対応。レポート本体は `reviews/adversarial-review-v0.5.md` にあったが後に削除した(git履歴のコミット `0ec7ce8` に残る)。

**中核メカニズムの修正:**

- §2: 契約レベルの「テストで固定」の目的を**回帰検知**と明記(正しさの保証ではない——§4の同語反復テーゼとの矛盾を解消)。アサーション有無の機械検査を全レベルに適用(§10にも追記)
- §2: レビュー指定の自己申告に**下方分類のインセンティブ**があることを明記し、行レベル該当・ドメイン中核判定をドメインオーナーの確認事項に。分類比率をKPI化
- §2: 判定軸を一本化(影響範囲×可逆性が下限、変更頻度・ドメイン重要性は引き上げのみ、複数該当は最高レベル)。§1の別軸リストを削除
- §4/gen-tests: 隔離の汚染経路を明示(実装後生成の仕様・設計メモの方針欄・CLAUDE.md)。仕様は**実装前に書かれたもの**に限定。プロンプト指示は防壁でないため**ツール制限付きサブエージェント定義**(例を同梱)を要求。ツール制限なしの場合は「独立性は指示ベース」とPRに明記。裁定者の利益相反(仕様の持ち主=著者)に対処
- §5: 隔離してもドメイン中核の理解義務は下がらない(§2優先)ことを明記。再生成後の**出力差分確認**(Hyrumの法則)を追加
- §8: 一次対応責任を「マージした人」から**オンコール(ロール)**に変更。マージ者の責任は「マージ時点の確認」に限定(§3も同期)。AI利用の**開示欄をPRテンプレートに新設**(レビュー指定は開示を兼ねない)
- §7/§12: KPIに分類比率・AI警告却下PRの障害率・障害対応時間/切り分け成功率を追加。**権限移譲の判断に速度系指標単独を認めない**。基準自身の運用コストを計上対象に。滞留時間≠注意時間の注記。**削除の手続き**(原因確認・明示的合意・再追加条件)と削除不能な憲法級項目を明定

**スキルの修正:**

- pr-guide: テンプレートを `assets/` に同梱(コピー後のリンク切れ解消)。設計メモの下書きを「採らなかった選択肢」欄へ転記させない(洗浄経路の遮断)。「埋めない」が強制でなく規約であることを明記
- first-review: 出力を**BLOCKER(不可逆系誤分類・単独枠)/機械的指摘/所見**の3ブロックに分離(警報疲れ対策・「機械検査」の看板と実態の乖離解消)。**実装とは別セッションで実行**を必須化。「指摘なし」と「未検査」の区別
- design-memo: 応答期限欄を追加(合意の着手通知化を防ぐ。§7にSLAルール追記)。触る範囲のパス実在確認・調査予算(5ファイル目安)・10行は「目安」に
- skills/README: 「物理的に人間に残す」→ 空欄は**規約であり強制ではない**と正直に記述。実効性の所在(口頭確認)を明示
- **update-map を新設**(5本目): 理解の地図(§11)の初版生成・更新・矛盾検査。docs/map.md 配置(CLAUDE.md 直埋めは gen-tests の汚染経路になるためリンクのみ)、全体200行以内、追加時は削除候補を同時提示、Level 3 の齟齬は単独枠で報告。フロー図が地図への書き戻しを要求するのに生成手段が無かったギャップを解消
- first-review: 検査項目6「地図との整合」を追加(Level 1〜3を変えるdiffに地図更新が無ければ指摘。地図が無ければ「未検査(地図なし)」)

**引用の修正:**

- §7: tenki.cloud を「第三者ベンチマーク」から「競合ベンダーのベンチマーク」に訂正、存在しない数値「29〜44%」を実測値36.1%に差し替え、「各ベンダーが自社ベンチで勝つ」現象の出典(DeepSource)を追加
- §10: SWE-Mutation の対象を「LLM生成テストスイート」と限定
- §8: Fedoraポリシーの承認報道(LWN)へのリンク追加(リンク先が提案版のため)
- flow-comparison.html: 本文にない義務(incidents.md)の削除、gen-tests適用範囲・「のみ」表現・マージ責任の同期、運用コストの正直化

## v0.5 (2026-08-22)

- `skills/` を新設: 基準の運用部分をエージェント用スキル4本として実装(design-memo / gen-tests / pr-guide / first-review)
- 設計思想「空欄=Human-in-the-Loopの定義」を skills/README.md に明文化。スキルは準備と機械検査のみを自動化し、判断(合意・怪しい箇所の名指し・裁定・分類の確定・マージ)は空欄として人間に残す
- 素通りされる空欄の観察による権限移譲の運用(偽のHITLか、萎え始めた本物かの判定)を追加
- 基準本文は§7にスキルへの参照を追加したのみ

## v0.4 (2026-08-22)

- 理解負債に先行概念(comprehension debt / Triple Debt Model)への参照を追加
- 独立テスト生成に学術先行研究(DiffSpec / Galápagos)を引用
- 権限移譲の外部証拠を拡充:ベンダーベンチマークと第三者評価の乖離、段階的オーバーサイト(arXiv:2606.22484)との違い
- 責任の所在にAI利用の開示(FedoraのAssisted-by慣行)を追加
- カバレッジゲートに実測例(カバレッジ100%でミューテーション80%、SWE-Mutation 71%→40%)を追加
- 訓練の節に実証根拠(AnthropicのRCT:AI補助学習で17ポイント低下・デバッグ最弱/Fekete & Porkoláb:2ヶ月で自分のコードも忘れる)を追加
- READMEに「関連する先行事例・研究」を新設(OSSポリシー6件の比較と調査結果)
- 引用はすべて一次ソースとの突き合わせ検証済み

## v0.3 (2026-08-22)

- 「三つの判断の分離」(検査の委譲=実績/理解の必要性=理解負債/理解のレベル=3段階)と**理解負債**の定義を新設
- 判定軸にドメイン重要性を追加
- 権限移譲の原則(実績が出た方向に権限を移す)と、2026年の実証研究群を外部証拠として追加
- カバレッジゲート(diffカバレッジ・ラチェット・ミューテーションテスト)を新設
- 理解の地図(抽象化レイヤー Level 0-4 と訓練)を新設
- 計測項目(KPI)を追加

## v0.2 (2026-08-22)

- 判定軸に可逆性を追加し、行レベル対象を「不可逆な副作用」で一般化
- Happy Path Bias(手動UI確認の限界)を注記
- 独立テスト生成(別セッション・実装非開示・不一致=仕様曖昧さ検出器)を追加
- 責任の所在、PRテンプレートを新設

## v0.1 (2026-08-22)

- 初版:前提認識(実装時間は理解の時間を兼ねていた)、理解基準3段階、2ゲート、テストの循環問題、隔離ルール、オンコール原則、レビュー運用、導入の順序
