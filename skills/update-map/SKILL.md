---
name: update-map
description: 理解の地図(Level 0〜3)の初版生成・更新・コードとの矛盾検査を行う。基準の導入時(初版生成、基準§12 手順2)、契約・不変条件・危険領域を変えるPRの中、障害対応の後、および月イチの定期検査で使う。地図の所有者は人間で、このスキルは下書きと検査だけを行う。
---

# 理解の地図の生成・更新(基準§11の実装)

コード量が増えても、人間が理解すべき情報量が増えない構造にするための地図を維持する。コードとドキュメントの齟齬は必ず発生する——このスキルの目的は「齟齬が起きない」ことではなく、**「齟齬に気づける」**ことである。

## 地図の仕様

- **置き場所: `docs/map.md`。** CLAUDE.md 等のエージェント設定ファイルには本文を埋め込まず、リンクだけを置く。理由は2つ:①全セッションに自動注入されるファイルにアーキテクチャ記述を置くと、独立テスト生成([gen-tests](../gen-tests/SKILL.md))の汚染経路になる。②人間が読む場所とAIが常時読む場所を分けることで、分量管理が独立にできる
- **分量: 全体で200行以内。** 超えたら追記ではなく、削る対象を探す。読まれない地図は理解負債の別の形にすぎない(基準§11)
- **構成:**
  - Level 0 システム地図 — 何がどこにつながっているか(mermaid 1枚)
  - Level 1 契約 — 主要コンポーネントごとに入出力・副作用・失敗時挙動を1〜3行
  - Level 2 不変条件 — 絶対に壊してはいけないこと(箇条書き)
  - Level 3 危険領域 — 不可逆系(スキーマ・データ移行・課金・外部送信・削除)の位置と、失敗時に何が起きるか・戻し方
- Level 4(実装)は地図に書かない。必要時にAIに絞り込ませて降りる

## モード

### 1. 初版生成(トリガー: 基準の導入時。地図が無いリポジトリで更新・矛盾検査を求められたとき)

1. リポジトリを調査し、上の構成・分量で下書きを生成する。取捨の基準は「保守する人間が**障害・変更・意思決定のとき**に必要な知識か」。網羅ではない
2. 生成後、**人間に通読と修正を求める**。この初回修正が地図の検収であり、所有(基準§11)の実体。無修正で受け入れられた地図は読まれていない疑いがある——直す箇所が1つもない生成物は稀である

### 2. 更新(トリガー: 契約・不変条件・危険領域を変えるPRの中/障害対応の後)

1. 判明した事実(裁定結果・障害で判明した依存・新しい危険領域)を該当Levelに反映する下書きを作る
   - **契約・不変条件・危険領域を変える変更の場合、地図の更新は当該PRに同梱する**(マージ後に回さない)。マージ時点で「地図で追跡できる状態」を確認できないと、基準§3のマージゲートが空文になる
   - 事後の更新になるのは、**障害対応で判明した事実の書き戻し**だけである
2. 分量上限を守る。追加するなら、**古くなった記述の削除候補を同時に提示する**

### 3. 矛盾検査(定期: 月イチ目安。CIジョブ化も可)

1. 地図の記述とコードの現状を突き合わせ、齟齬を列挙する——地図にあるが存在しないコンポーネント、変わった契約、地図に載っていない不可逆系
2. 出力は「齟齬リスト+修正案」。**勝手に直さない**——コードが正しく進化したのか、地図が正しくてコードが逸脱したのかの判定は人間の仕事

## 守ること

- 地図の所有者は人間。このスキルは下書きと検査だけを行う
- 矛盾検査で**Level 3(危険領域)に関する齟齬**が出た場合は、他の齟齬と混ぜず先頭に単独で出す([first-review](../first-review/SKILL.md) のBLOCKER枠と同じ理由)
- 「検査したが齟齬なし」と「未検査」を区別して報告する
- 地図がまだ無いリポジトリで更新・矛盾検査を求められたら、初版生成から始めることを提案する
