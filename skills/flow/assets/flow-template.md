# フロー状態: <TICKET-ID>

- チケットの在り処: <URL / パス / Issue番号>(2回目以降はここから辿る。flow 手順1)
- 設計メモの在り処: <チケットと同じ場所の URL / パス> / 承認: <YYYY-MM-DD 承認者の役割>(停止1の合意後に著者が申告。委譲プロンプトと crosscheck の材料はここから辿る。design-memo「出力の置き場所」)
- 不可逆系: **未判定** / なし / **あり**(スキーマ変更・データ移行・課金・外部送信・削除)
  ※ 機械は提案のみ。確定は設計メモの承認(不可逆系ありならドメインオーナーの明示承認)
- 理解レベル: **未判定** / 契約 / 構造 / 行
- 実データの所在: <計測・集計・エクスポート系のみ。停止1で著者に聞いた申告。突き合わせの前に diff の実際の参照先と照合する(不一致なら止まって聞く)>
- 結合テストの実行手段: <リポジトリから判明した手段(CI設定・既存の結合テスト・起動スクリプトのパス)/ 著者の申告 / **なし**(整備チケット: <在り処 / 未起票>)> / 結合テスト仕様書: 不要 / **要**(用途: 自動の実行手段が無い境界 / 納品 / 監査。実行先の実物相当: <サンドボックス・検証環境等。無ければ「なし」>。著者の申告。crosscheck 手順B)
- 実装の委譲先: ブランチ <名前> / 切り元 <名前> / 完了報告 <状態ディレクトリ配下のパス> / 見込み <分>(委譲時にサブエージェントが申告)
- 最終更新: <日時> / 次にやること: <下の最初の未チェック項目> / 👤で止めた時刻: <date の出力>

記法: `[ ]` 未了 / `[x]` 完了 / `[~]` スキップ(**理由を必ず併記**。判断は人間)/ `[-]` 対象外(判定の帰結。根拠を1語)/ 👤 = 人間の判断

**停止は3回**(標準ケース=不可逆系なし): 停止1=着手 / 停止2=PR確認 / 停止3=マージ。👤 は停止の中でまとめて聞く(ゲートの数は減っていない——聞く場所を束ねただけ)。委譲の完了待ちの後に**判断ゼロの継続呼び出し**が1回入る(完了通知が届く環境では人の操作なし)。独立照合(人間版)をやる場合は第二解釈者の停止が別に増える。

---

## 停止1. 着手(1回の停止でまとめて聞く)

- [ ] 設計メモの下書き — design-memo
      *著者専用の欄(懸念点など)は機械が生成しない。直接記入か、聞き取り(design-memo 手順4)*
- [ ] 設計メモの「契約」欄の下書き — design-memo ※契約・不変条件・危険領域を変える場合のみ(照合の材料。地図はここでは触らない)
- [ ] 👤 **設計メモへの合意** — チーム。**不可逆系ならドメインオーナーの明示承認**(無返事進行は不可)
- [ ] 設計メモの在り処と承認をヘッダに書く — 著者がチケットと同じ場所に置き、末尾に `承認: YYYY-MM-DD <役割>` を追記したものの URL / パス(知識の申告——👤 には数えない。**未記入のまま委譲しない**)
- [ ] 検査の段と版の判定 — crosscheck 手順0(標準=理解検査。**不可逆系は独立照合必須**。第二解釈者がいなければ自動版)。対象外になった後続項目はここで `[-]` にする
- [ ] 実データの所在を著者に聞き、ヘッダに書く(判断ではなく知識の申告——👤 には数えない。**代表データを自作しない**)※計測・集計・エクスポート系のみ
- [ ] 結合テストの実行手段を確認し、ヘッダに書く — リポジトリ(CI設定・既存の結合テスト・起動スクリプト)から判明しなければ著者に聞く(知識の申告——👤 には数えない。無ければ「なし」)
- [ ] 実装の委譲 — サブエージェント(別セッション)へ**送信**。**範囲は実装・単体テスト・結合テストまで**(実行手段が「なし」なら作らせず「未実施」と報告させる。不可逆系の境界は実物に通さない)。**聞き取りより先に送る**(委譲時点で期待値が存在しない=構成保証。flow「セッションの分離」)
      *ブランチ・切り元・完了報告の置き場所・結合テストの実行手段を指定してヘッダに書く。完了報告に「迷った箇所・仕様が薄く補完した箇所」「結合テストで固定した境界・固定しなかった境界と理由・落ちたまま残したテスト・実物の挙動が地図と違った箇所」を残させる(著者が停止2で読む)*
- [ ] 自動版の生成 — crosscheck 手順B-1 ※判定が自動版、または仕様書が「要」のとき。**委譲の直後に起動する**(実装がまだ無い=構成保証)。出力形式は境界ごとに手順Bで決める(テストコード / 結合テスト仕様書)。仕様書の要否・用途・実行先は著者に聞いてヘッダに書く(知識の申告——👤 には数えない)
      *生成物は照合メモと同じ場所に置く(リポジトリの外・委譲プロンプトに現れるどのパスの祖先でもない場所)*
- [ ] 👤 **理解検査:期待値の書き出し(著者)** — crosscheck 手順U ※標準。**委譲直後、同じ停止の中で続けて聞き取る**(実装と並列)
      *照合メモはリポジトリの外・**委譲プロンプトに現れるどのパスの祖先でもない場所**に置く(flow「セッションの分離」)。エージェントに埋めさせない*
- [ ] 独立照合の材料を切り出して提示 — crosscheck 手順A-1・A-2 ※独立照合(人間版)をやる場合のみ
- [ ] 👤 **独立照合:期待値の書き出し(第二解釈者)** — **実装方針を書いていない人**(不可逆系はドメインオーナー)※人間版のみ。**著者の停止に混ぜない**(独立の停止。実装と並列)
- 聞き取りが終わったらこの呼び出しを終える(実装の完了待ちに入る。待ちの監視はしない——次の呼び出しで経過を検査する)

## 継続呼び出し(判断ゼロ。完了通知が届く環境では自動で走る)

- [ ] 実装の完了確認 — ヘッダの完了報告の置き場所から受け取る(ヘッダの時刻からの経過が見込み上限を超えていたら「上限超過」を現在地と実測表へ)。**結合テストの有無と実行結果を diff と完了報告から確認する**——越えるかは機械の仮判定(根拠と「機械が判定した」を記録)。越えるのに無ければ「未実施(理由)」、越えないと判定したなら「対象外(機械が判定。根拠1行)」、落ちたままなら「赤 n 件」を停止2の提示に載せる(「対象外」に書き換えない。手段がCIだけなら「CI待ち」と書き、結果の確認は停止3に送る)。**完了報告に「固定しなかった境界(実物相当なし)」があり、人が実物相当で確認できるなら、その境界の結合テスト仕様書を crosscheck 手順B-1 で追加生成する**(ツール制限のまま。材料は停止1と同じ引用のみ——完了報告・diff の内容を混ぜない。停止2の実行へ送る)
- [ ] 自動版の実行 — crosscheck 手順B-2〜3 ※判定が自動版のときだけ。生成は停止1で済んでいる。**ここで実行するのはテストコードだけ**——結合テスト仕様書は人が実行するので停止2へ送る
- [ ] 領域の仮決め — **diff から**機械が提案し根拠を記録(一人運用は「機械が決めた」と明記)。設計メモの「触る範囲」と食い違ったら停止2の提示に「領域差分」を1行載せる
- [ ] その領域の地図が無ければ初版生成 — update-map モード1 ※**領域ごとに一度だけ**。このPRに同梱。検収は停止2
- [ ] 理解検査の突き合わせ — crosscheck 手順U-4。種別(A/B)は**提案**まで(確定は停止2で著者)
- [ ] 出力との突き合わせ — 実データを1本通し、分布と代表的な集計(分母明示)を出す。**その前に、diff の実際の参照先とヘッダの申告を照合する**——不一致なら突き合わせをせず止まって聞く(👤でない止まり方)※計測・集計・エクスポート系のみ
- [ ] PR下書きの機械記入欄 — pr-guide(選別候補は全転記。**種別・件数の欄は停止2の確定後に再生成する**)
- [ ] 地図の更新をPRに同梱 — update-map モード2 ※契約等を変える場合
- [ ] AI一次レビュー — first-review(実装とは別セッション)

## 停止2. PR確認(1回の停止でまとめて聞く。提示順を守る)

**提示順(この順で聞き、状態ファイルに記録し終えてから first-review の出力を開く)**: 完了報告の「迷った箇所」(結合テストの未実施/対象外の判定と根拠もここ) → 結合テスト仕様書の実行と結果(自動版の出力が仕様書のとき) → 突き合わせ結果と種別 → Bの反映先 → **固定した境界** → 選別 → 理解レベル → 領域と地図 → first-review 開示。開示順は指示ベースの規律である(構成では守れない)——記録の時刻を実測表に残す。

- [ ] 完了報告の「迷った箇所・仕様が薄く補完した箇所」と、結合テストの未実施/対象外の判定と根拠を著者が読む(「固定した境界」はまだ開かない)
- [ ] 結合テスト仕様書の実行と結果の記入 — 著者がヘッダの実行先(実物相当)で項目ごとに実行し、**観測を書いてから** 一致 / 食い違い / 実行できず(理由)を書く ※自動版の出力が仕様書のときだけ(作業から得られる知識の申告——判断ではないので 👤 には数えないが、**人の作業時間がかかる**。食い違いは裁定=レビュー段の 👤 へ。実行できずは一致に丸めない。全項目が実行できずなら「未実施」)
      *その場で実行できなければ止まる——現在地に「◀ 仕様書の実行(著者の作業。👤ではない)」と書く。実行の前後に `date +%s` を挟み、実測表に工程名「仕様書の実行(n 項目)」で1行、列は「人間の待ち」、備考に「作業時間」と明記(経過ではなく作業)*
- [ ] 👤 **種別(A/B)の確定とBの反映先の指定** — 著者。**first-review の出力を開く前に**(A=仕様が2通りに読めた→裁定へ / B=知らなかった→著者が反映先〔コード・コメント・地図 Level 2〜3〕を1件ずつ指定する。**反映先を探すのが検査の価値**——探す行為は機械化しない。既に書かれていたら重複=指定なし。転記は機械が行ってよいし、自ら反映してもよい)
      *この項目は v0.37 で新設だが、仕事は新設ではない——基準が著者に割り当てていた確定と反映の明示化(以前は機械項目「突き合わせと結果の反映」に埋まっていた)*
- [ ] 完了報告の「結合テストで固定した境界・固定しなかった境界と理由・落ちたまま残したテスト・実物の挙動が地図と違った箇所」を著者が読む ※種別確定の後(「固定した」は実装側の読み。先に読むと B に倒れる)。地図と違った箇所は呼ばれる側の領域のオーナーへ
- [ ] 👤 **採らなかった選択肢の選別** — 著者(候補は pr-guide が全転記済み。載せるものを選ぶ)
- [ ] 👤 **理解レベルの確定**(行レベル該当か・ドメイン中核か) — ドメインオーナー ※**A=0(裁定なし)のときのみここ。A>0 なら裁定後に送る**
- [ ] 👤 **この変更が触る領域の確定** — ドメインオーナー(機械の仮決め・diff実測・領域差分を見て。決め直したら初版も作り直して再検収)
- [ ] 👤 **地図の初版を検収** — その領域のドメインオーナー ※初版を作った場合のみ
      *最低ライン: Level 2・3 の通読・修正。reviewed を書くのは人間だけ。済むまでこの領域のマージゲートの地図要件は発効しない*
- [ ] first-review の出力の開示(上の記録が済んでから)

## 停止2の後(機械)

- [ ] Bの転記 — 著者が停止2で指定した反映先へ(指定が無いものは触らない)
- [ ] PR欄の再生成 — pr-guide の該当欄を停止2の確定で書き直す: テスト欄のA/B件数(列挙数と一致)・「確認してほしい項目」(Aのみ。0件なら節ごと落とす)・選択肢欄・自動版が仕様書なら一致/食い違い/実行できずの件数と「仕様書で人が確認した境界」
- [ ] 欄の突き合わせ検査 — ①「不可逆な副作用」が「なし」以外なら、検査欄が「独立照合・人間版/自動版」のいずれかであること(理解検査では満たさない。仕様書で一致・食い違いが0件、実行できずのみの場合も満たさない) ②計測・集計・エクスポート系なら「出力との突き合わせ」が「対象外」でないこと ③diff が箱の境界を越えるなら「結合テスト」の行が「対象外」でないこと(「未実施」なら理由、「実施」なら diff が越えた境界が「固定した境界」か、仕様書で人が確認した境界に含まれていること)

## レビュー(独立照合をやる場合のみ)

- [ ] 突き合わせ — crosscheck 手順A-4(第二解釈者の期待値と実装。first-review の出力を先に開かせない)
- [ ] 👤 **食い違いの裁定** — 仕様の持ち主。不可逆系はドメインオーナー同席
- [ ] 裁定の書き戻し — **3箇所すべて**: ①仕様/設計メモ ②**裁定前に書かれたテスト** ③地図 Level 1〜2(契約を変えた場合)
- [ ] 👤 **理解レベルの確定** — ドメインオーナー ※A>0 で停止2から送られた場合はここ

## 停止3. マージ

- [ ] 👤 **マージの決断** — マージする人(=マージ確認責任を負う人)。**Bの転記・PR欄の再生成の差分込みで見る**
      *ここで止まった時点で機械の記録は終わっている。マージ後に /flow を呼び直す必要はない。チケットが閉じたら人間がこのファイルを消すか、実測表だけ残す*

---

## 実測

単位は**分**。`date +%s` の前後差から計算する(推定で書かない。flow 手順4)。「エージェント」は実行にかかった時間、「人間の待ち」は 👤 で止めた時刻から次の呼び出しまでの経過(**経過時間であって作業時間ではない**)。
**停止の中に複数の 👤 があるときは、聞き取りの区切りごとに `date +%s` を挟み、ゲート単位で1行ずつ書く**(①合意/②期待値/⑥選別…のどれに時間が要ったかを分離する。欄単位に削る根拠はこの粒度でしか取れない)。**この粒度の「人間の待ち」は v0.36 以前の記録と直接比較できない**(以前は1停止≒1ゲートだった)。
集計するので、空欄にせず 0 か実測値を入れる。**この表が次のチケットの見込みの参照値になる。** 上限超過も1行残す。
**他チケットと同時に進めた場合は、最初の行の備考に「同時進行: <他のチケットID> / 実装順: n番目(決めた者: 人間 or 機械)」を書く**(並行と直列を分けて集計するため)。

| 工程 | エージェント(分) | 人間の待ち(分) | 備考 |
|---|---|---|---|

## スキップした項目と理由

| 項目 | 理由(重すぎた/不要だった/やる気がなかった) |
|---|---|
