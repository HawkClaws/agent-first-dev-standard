# フロー状態: <TICKET-ID>

- チケットの在り処: <URL / パス / Issue番号>(2回目以降はここから辿る。flow 手順1)
- 不可逆系: **未判定** / なし / **あり**(スキーマ変更・データ移行・課金・外部送信・削除)
  ※ 機械は提案のみ。確定は設計メモの承認(§7。不可逆系ありならドメインオーナーの明示承認)
- 理解レベル: **未判定** / 契約 / 構造 / 行
- 実装の委譲先: ブランチ <名前> / 切り元 <名前> / 完了報告 <状態ディレクトリ配下のパス> / 見込み <分>(委譲時にサブエージェントが申告)
- 最終更新: <日時> / 次にやること: <下の最初の未チェック項目> / 👤で止めた時刻: <date の出力>

記法: `[ ]` 未了 / `[x]` 完了 / `[~]` スキップ(**理由を必ず併記**。判断は人間)/ `[-]` 対象外(判定の帰結。根拠を1語)/ 👤 = 人間の判断

---

## 1. 着手

- [ ] 設計メモの下書き — design-memo
      *著者専用の欄(懸念点など)は機械が生成しない。直接記入か、聞き取り(design-memo 手順4)*
- [ ] 設計メモの「契約」欄の下書き — design-memo ※契約・不変条件・危険領域を変える場合のみ(独立照合の材料。地図はここでは触らない)
- [ ] 👤 **設計メモへの合意** — チーム。**不可逆系ならドメインオーナーの明示承認**(無返事進行は不可)

## 2. 実装(検査の盲検フェーズと**並列**に進める)

**委譲が先、期待値が後**(flow「セッションの分離」)。逆順になったら「独立性は指示ベース」と記録。

- [ ] 検査の段と版の判定 — crosscheck 手順0(標準=理解検査。**不可逆系は独立照合必須**。第二解釈者がいなければ自動版)。対象外になった後続項目はここで `[-]` にする
- [ ] 実装の委譲 — サブエージェント(別セッション)へ。**完了を待たずに次へ進む**
      *ブランチ・切り元・完了報告の置き場所を指定してヘッダに書く。照合メモの内容もパスも渡さない。完了報告に「迷った箇所・仕様が薄く補完した箇所」を残させる(flow「セッションの分離」)*
- [ ] 👤 **理解検査:期待値の書き出し(著者)** — crosscheck 手順U ※標準。実装の実行中に並列で
      *照合メモはリポジトリの外・別ファイル。エージェントに埋めさせない*
- [ ] 独立照合の材料を切り出して提示 — crosscheck 手順A-1・A-2 ※独立照合をやる場合のみ
- [ ] 👤 **独立照合:期待値の書き出し(第二解釈者)** — **実装方針を書いていない人**(レビュアー。不可逆系はドメインオーナー)※独立照合をやる場合のみ
      *著者が書くと、実施記録の付いた自己照合になる(その場合は理解検査として記録)*
- [ ] 実装の完了確認 — ヘッダの完了報告の置き場所から受け取る
- [ ] 自動版の実行 — crosscheck 手順B ※判定が自動版のときだけ

## 3. PR作成(地図に触るのはここだけ)

- [ ] 👤 **この変更が触る領域を決める** — ドメインオーナーの担当単位(§8)
      *選択肢が実質1つ(一人運用など)なら機械が決めて「機械が決めた」を記録し、人が見て止める(§7)。切り方は§11*
- [ ] その領域の地図が無ければ初版生成 — update-map モード1 ※**領域ごとに一度だけ**。このPRに同梱(§12 手順2)
- [ ] 👤 **地図の初版を検収** — その領域のドメインオーナー ※初版を作った場合のみ
      *最低ライン: Level 2・3 の通読・修正。reviewed を書くのは人間だけ。済むまでこの領域のマージゲートの地図要件は発効しない*
- [ ] 理解検査の突き合わせと結果の反映 — crosscheck 手順U-4・U-5 ※理解検査の場合
      *A(仕様が2通りに読めた)→「確認してほしい項目」欄へ。B(知らなかった)→コード・地図へ反映。記録はテスト欄1行*
- [ ] 出力との突き合わせ — 実データを1本通し、分布と代表的な集計(分母明示)を出す。現れなかった値も記録 ※計測・集計・エクスポート系のみ(§4)
      *実データの所在が不明なら止まって聞く。代表データを自作しない(flow「絶対にやらないこと」)*
- [ ] PR下書きの機械記入欄 — pr-guide ※突き合わせの後(件数・実施記録を欄に書くため)
- [ ] 地図の更新をPRに同梱 — update-map モード2 ※契約等を変える場合
- [ ] 👤 **採らなかった選択肢の選別** — 著者
      *候補は pr-guide が会話ログから転記済み(可否を聞かない)。著者は載せるものを選ぶ(§9)*
- [ ] AI一次レビュー — first-review(実装とは別セッション)

## 4. レビュー

期待値が未記入なら、PRの差分・テスト・first-review の出力を開く前に書く(§4)。

- [ ] 突き合わせ — crosscheck 手順A-4 ※独立照合の場合(理解検査の突き合わせは§3で済んでいる)
- [ ] 👤 **食い違いの裁定** — 仕様の持ち主。不可逆系はドメインオーナー同席
- [ ] 裁定の書き戻し — **3箇所すべて**(§4): ①仕様/設計メモ ②**裁定前に書かれたテスト** ③地図 Level 1〜2(契約を変えた場合)
- [ ] 👤 **理解レベルの確定**(行レベル該当か・ドメイン中核か) — ドメインオーナー

## 5. マージ

- [ ] 欄の突き合わせ検査 — ①「不可逆な副作用」が「なし」以外なら、検査欄が「独立照合・人間版/自動版」の
      いずれかであること(理解検査では満たさない) ②計測・集計・エクスポート系なら「出力との突き合わせ」が「対象外」でないこと
- [ ] 👤 **マージの決断** — マージする人(=マージ確認責任を負う人)
      *ここで止まった時点で機械の記録は終わっている。マージ後に /flow を呼び直す必要はない。チケットが閉じたら人間がこのファイルを消すか、実測表だけ残す(§12 の材料)*

---

## 実測

単位は**分**。`date +%s` の前後差から計算する(推定で書かない。flow 手順4)。「エージェント」は実行にかかった時間、「人間の待ち」は 👤 で止めた時刻から次の呼び出しまでの経過(**経過時間であって作業時間ではない**)。
集計するので、空欄にせず 0 か実測値を入れる。**この表が次のチケットの見込みの参照値になる。** 上限超過も1行残す。
**他チケットと同時に進めた場合は、最初の行の備考に「同時進行: <他のチケットID> / 実装順: n番目(決めた者: 人間 or 機械)」を書く**(並行と直列を分けて集計するため)。

| 工程 | エージェント(分) | 人間の待ち(分) | 備考 |
|---|---|---|---|

## スキップした項目と理由

| 項目 | 理由(重すぎた/不要だった/やる気がなかった) |
|---|---|
